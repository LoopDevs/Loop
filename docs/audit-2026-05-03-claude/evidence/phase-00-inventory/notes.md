# Phase 00 - Inventory and Freeze

Status: complete
Owner: lead (Claude)
Reviewer: lead (Claude)
Start: 2026-05-03
Finish: 2026-05-03

## Baseline

- Audit baseline commit: `13522bb4ee8279336fb143c5c0f33bbbf6ef205b`
- `git rev-parse HEAD` matches the baseline.
- Worktree state at audit start:
  - Codex audit (`docs/audit-2026-05-03/**`) has unstaged modifications and untracked scaffolds.
  - Claude audit workspace (`docs/audit-2026-05-03-claude/**`) is fully untracked at scaffold start.
  - No source-of-truth files (`apps/**`, `packages/**`, `scripts/**`, `.github/**`, `docs/**` outside the audit dirs) have local modifications.
- Codex tracked files in baseline (excluded from this audit's scope by isolation rule):
  - `docs/audit-2026-05-03/inventory/directory-map.txt`
  - `docs/audit-2026-05-03/inventory/git-status-short.txt`
  - `docs/audit-2026-05-03/inventory/tracked-files.txt`
  - `docs/audit-2026-05-03/inventory/workspace-files.txt`

## File counts

| Bucket                                           |                         Count |
| ------------------------------------------------ | ----------------------------: |
| Total tracked files in the working tree          |                         1,226 |
| Tracked under Codex audit dir (out of scope)     |                             4 |
| Tracked outside Codex dir (in scope, scaffolded) |                         1,222 |
| Tracked under Claude audit dir                   |        0 (untracked scaffold) |
| `apps/backend/src/**`                            |                           557 |
| `apps/web/app/**`                                |                           346 |
| `packages/shared/src/**`                         |                            28 |
| `docs/**` excluding audit-2026-05-03\*           |                           155 |
| Backend route files in `routes/`                 |                            18 |
| Backend admin handlers in `admin/`               |                          ~110 |
| OpenAPI registration files in `openapi/`         |                            67 |
| Drizzle SQL migrations                           | 29 (0000 - 0028) plus `meta/` |
| ADRs                                             |                29 (001 - 029) |
| GitHub workflows                                 |                             5 |

## Top-level structure confirmed

- `apps/backend` (Hono + Drizzle + Stellar SDK + Pino + Sentry)
- `apps/web` (React Router v7 + Vite + Capacitor wrappers in `app/native/`)
- `apps/mobile` (Capacitor v8 shell, native overlays, no node_modules audit)
- `packages/shared` (TS types + proto output `clustering_pb.ts`)
- `scripts/` (verify.sh, lint-docs.sh, check-bundle-budget.sh, check-admin-bundle-split.sh, check-audit-policy.mjs, ci-watch.sh, e2e-real.mjs, postgres-init.sh)
- `.github/workflows/` (ci.yml, codeql.yml, e2e-real.yml, pr-automation.yml, pr-review.yml)
- `docs/` (architecture, deployment, development, testing, standards, ADRs, runbooks, SLO, alerting, on-call, log policy, error codes, mobile-native-ux, third-party-licenses, roadmap, audit archives)

## Phase ownership

Lane assignment confirmed against `tracker.md`. The lead auditor (this Claude session) owns every lane in the absence of additional human reviewers; sign-off remains lead-only.

## Generated/binary policy

- Excluded from in-scope review:
  - `**/node_modules/**` (dependency code)
  - `**/dist/**`, `**/build/**`, `apps/web/build/**` (build output)
  - `apps/mobile/ios/App/Pods/**` and `apps/mobile/android/.gradle/**` (CocoaPods/Gradle generated)
  - `apps/mobile/ios/App/App/public/**` and `apps/mobile/android/app/src/main/assets/public/**` (synced web bundle)
  - `playwright-report/**`, `coverage/**`, `test-results/**` (transient runtime artifacts)
- In-scope generated files that DO require source-of-truth verification:
  - `packages/shared/src/proto/clustering_pb.ts` - regenerated by `npm run proto:generate`; must reconcile against `apps/backend/src/proto/clustering.proto` if a `.proto` source exists, or document the source.
  - `apps/backend/src/db/migrations/meta/_journal.json` - migration ordering manifest; must match SQL file inventory.
  - Drizzle SQL migrations themselves are tracked source even if migration tooling can re-emit.
  - `package-lock.json` at repo root - integrity verified by lockfile audit in Phase 04.

## Cross-audit isolation note

This audit does not read, cite, compare against, or merge findings from `docs/audit-2026-05-03/**`. If both audits independently rediscover the same defect, this audit will record fresh evidence and assign a `A4-###` finding ID without referencing the parallel workspace.

## Outputs

- Tracker updated: `tracker.md` Phase 00 row.
- File disposition register starting state: `inventory/file-disposition.tsv` (1,222 rows seeded with primary phases). Per-phase second pass will close dispositions.
- Planned-feature matrix seeded: `inventory/planned-feature-matrix.tsv` (PF-001..PF-008).
- Scaffold disposition register seeded: `inventory/scaffold-disposition.tsv` (Phase 25 self-review).

## Findings

- None. Phase 00 captured baseline only.
