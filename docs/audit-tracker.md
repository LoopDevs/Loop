# Audit Tracker

This is the execution document for the comprehensive audit. Update it as the audit proceeds.

## Baseline Snapshot

- Audit start date: 2026-04-17
- Commit SHA: `884b1a94562145e155b60bb3c3f673261357449a`
- Auditor: Codex
- Node version: `v24.6.0`
- npm version: `11.5.1`
- Packages in scope: `apps/backend`, `apps/web`, `apps/mobile`, `packages/shared`
- Supporting scope: `docs/`, `.github/`, `scripts/`, root config files
- Repo footprint:
  - `131` tracked source/workflow/support files in primary audit scope (`apps/backend/src`, `apps/web/app`, `packages/shared/src`, `tests`, `.github`, `scripts`)
  - `1369` files total in repo excluding `.git` and `node_modules`
- Known environment limitations:
  - No guaranteed access to production infrastructure, secrets, or real upstream CTX credentials
  - Mobile device/runtime verification is limited to repo contents unless later supplemented by actual device/build outputs
- Commands planned:
  - `npm run verify`
  - `npm run build`
  - `npm run test:e2e`
  - targeted `rg`, `sed`, `find`, and config inspection commands

## Workstream Status

| Workstream                                                    | Status   | Evidence                                                                              | Findings                                 | Notes                                                               |
| ------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------- |
| Repository hygiene and governance                             | complete | repo inventory, ignored-artifact scan, root/workflow/doc review                       | A-001, A-006, A-010, A-035, A-037        | governance, stale residue, and misleading artifacts captured        |
| Architecture and boundary compliance                          | complete | boundary scans, `AGENTS.md`, web/native import review                                 | A-005                                    | architecture-rule drift recorded                                    |
| Backend correctness and resilience                            | complete | backend source review, verify/build pass, handler/middleware inspection               | A-016, A-022, A-023, A-025               | core backend lifecycle and resilience issues captured               |
| Web correctness and UX integrity                              | complete | routes/hooks/stores review, local e2e results, SSR runtime inspection                 | A-002, A-015, A-019, A-029               | user-facing correctness and recovery issues captured                |
| Mobile shell and native integration                           | complete | `apps/mobile`, ignored native project inspection, wrapper review                      | A-012, A-033, A-034                      | native-source ambiguity and platform config risks captured          |
| Shared types, contracts, and serialization                    | complete | shared type review, backend/web contract comparison                                   | A-011                                    | contract drift recorded                                             |
| Auth, session, identity, and token handling                   | complete | auth handlers, auth store, session restore review                                     | A-008, A-018, A-020, A-024, A-036        | auth lifecycle and trust-boundary issues captured                   |
| Orders, payment, redemption, and business flows               | complete | order handlers, purchase flow, redeem flow, e2e review                                | A-009, A-019, A-030                      | order/payment/redeem path reviewed end to end                       |
| Security, privacy, abuse, and trust boundaries                | complete | secret scan, logging/image/webview review, SSR header/runtime checks                  | A-001, A-009, A-023, A-024, A-025, A-027 | security and abuse surfaces audited across backend, web, and mobile |
| Performance, caching, and runtime efficiency                  | complete | SSR/runtime response inspection, third-party asset review, payment polling review     | A-032                                    | runtime dependency and retry-efficiency risks captured              |
| Accessibility, responsiveness, and degraded-network behaviour | complete | search/map/offline UX review, responsive e2e matrix review                            | A-013, A-026                             | meaningful a11y/responsive gaps captured                            |
| Tests, test quality, and regression confidence                | complete | `npm run verify`, `npm run test:e2e`, mocked suite run, coverage run                  | A-003, A-004, A-010, A-014, A-026        | local and CI confidence gaps captured                               |
| CI/CD, release safety, and deployment correctness             | complete | workflow review, local/CI e2e behaviour, deploy asset review, GitHub governance check | A-003, A-004, A-031, A-037               | release-safety and automation risks captured                        |
| Observability, alerting, and incident readiness               | complete | `/metrics`, Discord hooks, Sentry wiring review, runtime header check                 | A-016, A-021, A-022                      | observability blind spots captured                                  |
| Dependencies, supply chain, licenses, and update posture      | complete | package review, `npm audit` run, workflow/tooling dependency review                   | A-017, A-031                             | dependency and automation supply-chain risks captured               |
| Documentation accuracy and operational usability              | complete | docs vs code/workflow review, support-artifact review                                 | A-006, A-007, A-035                      | docs and operational reference drift captured                       |
| Backlog, roadmap, and delivery-risk alignment                 | complete | roadmap/mobile/testing docs vs code reality, product copy review                      | A-007, A-028, A-030, A-037               | roadmap/product-truth gaps captured                                 |

Status values:

- `not started`
- `in progress`
- `blocked`
- `complete`

## Depth Coverage

Track whether each major workstream has been reviewed from both altitude levels.

| Workstream                                                    | High-level review | Low-level review | End-to-end review | Notes                                                         |
| ------------------------------------------------------------- | ----------------- | ---------------- | ----------------- | ------------------------------------------------------------- |
| Repository hygiene and governance                             | yes               | yes              | no                | repo inventory and ignored-artifact pass complete             |
| Architecture and boundary compliance                          | yes               | yes              | no                | rules compared against concrete imports/boundaries            |
| Backend correctness and resilience                            | yes               | yes              | no                | backend code review underway                                  |
| Web correctness and UX integrity                              | yes               | yes              | yes               | code review plus local e2e executed                           |
| Mobile shell and native integration                           | yes               | yes              | no                | wrapper/config pass underway                                  |
| Shared types, contracts, and serialization                    | yes               | yes              | no                | shared package reviewed against backend/web use               |
| Auth, session, identity, and token handling                   | yes               | yes              | yes               | auth and restore flows reviewed end to end                    |
| Orders, payment, redemption, and business flows               | yes               | yes              | yes               | purchase flow and redeem flow reviewed                        |
| Security, privacy, abuse, and trust boundaries                | yes               | yes              | no                | initial threat-model pass complete                            |
| Performance, caching, and runtime efficiency                  | yes               | yes              | yes               | SSR/runtime behaviour and polling paths reviewed              |
| Accessibility, responsiveness, and degraded-network behaviour | yes               | yes              | yes               | search semantics, offline UI, and mobile test matrix reviewed |
| Tests, test quality, and regression confidence                | yes               | yes              | yes               | verify and e2e commands executed                              |
| CI/CD, release safety, and deployment correctness             | yes               | yes              | no                | workflow pass in progress                                     |
| Observability, alerting, and incident readiness               | yes               | yes              | no                | initial pass only                                             |
| Dependencies, supply chain, licenses, and update posture      | yes               | yes              | no                | pending live audit result                                     |
| Documentation accuracy and operational usability              | yes               | yes              | no                | docs drift pass in progress                                   |
| Backlog, roadmap, and delivery-risk alignment                 | yes               | yes              | no                | roadmap/docs compared to current state                        |

## Findings Register

Record each finding as a separate item.

| ID    | Severity | Workstream                                                    | File / Surface                                                                                                                                                  | Summary                                                                                                                                                                                                                                                  | Evidence                                                                                                                                                                                                                                                                          | Proposed action                                                                                                                                                                                             | Status                                                                                                                                                                                                                                                                |
| ----- | -------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ---- |
| A-001 | HIGH     | Security, privacy, abuse, and trust boundaries                | `scripts/pay-order.mjs`                                                                                                                                         | Local helper contains a hardcoded Stellar secret key in plain text. The file is ignored, but it exists in the shared workspace and violates the project’s “no secrets in source/tooling” standard.                                                       | Secret literal present in `scripts/pay-order.mjs`; ignored artifact visible in `git status --ignored --short`.                                                                                                                                                                    | Remove the file from the shared workspace or convert it to env-only input, and add guardrails so secret-bearing helpers are not kept alongside repo code.                                                   | open                                                                                                                                                                                                                                                                  |
| A-002 | HIGH     | Web correctness and UX integrity                              | `apps/web/app/routes/home.tsx`, `apps/web/app/components/features/Navbar.tsx`, `apps/web/app/routes/map.tsx`, `apps/web/app/components/features/ClusterMap.tsx` | Merchant directory, search, and map name lookup only load the first 100 merchants even though the catalog is larger, so some merchants never appear or resolve correctly.                                                                                | These surfaces call `useMerchants({ limit: 100 })`; project docs reference ~117 merchants and comments acknowledge backend clamping.                                                                                                                                              | Add full-catalog pagination or a dedicated full-catalog/search endpoint for UI surfaces that require complete merchant coverage.                                                                            | open                                                                                                                                                                                                                                                                  |
| A-003 | HIGH     | Tests, test quality, and regression confidence                | `playwright.config.ts`, root `test:e2e` command                                                                                                                 | `npm run test:e2e` is not self-contained locally: it boots only the web server, not the backend the suite depends on, so purchase-flow tests fail with missing merchant data.                                                                            | `playwright.config.ts` starts only `npm run dev:web`; local `npm run test:e2e` failed in chromium purchase-flow because no merchant links were present.                                                                                                                           | Make the local e2e command self-contained or split it into clearly named partial vs full-stack commands.                                                                                                    | open                                                                                                                                                                                                                                                                  |
| A-004 | MEDIUM   | Tests, test quality, and regression confidence                | `playwright.config.ts`, local `test:e2e`                                                                                                                        | Local default e2e includes `mobile-safari`, but the suite hard-fails unless WebKit is manually installed. This makes the default command fail in a fresh checkout even when Chromium coverage is healthy.                                                | Escalated local `npm run test:e2e` passed 11 tests and failed 8 `mobile-safari` launches with missing WebKit executable.                                                                                                                                                          | Gate `mobile-safari` behind an opt-in flag or make bootstrap/install steps explicit enough that the default command succeeds.                                                                               | open                                                                                                                                                                                                                                                                  |
| A-005 | MEDIUM   | Architecture and boundary compliance                          | `apps/web/app/root.tsx:112`                                                                                                                                     | A direct dynamic import of `@capacitor/keyboard` exists outside `app/native/`, violating the repo’s stated Capacitor boundary and bypassing the lint restriction.                                                                                        | Import-boundary scan found `await import('@capacitor/keyboard')` in `root.tsx`; AGENTS rule requires all Capacitor plugin calls to live in `apps/web/app/native/`.                                                                                                                | Move keyboard access into a native wrapper and tighten linting to catch dynamic imports outside `app/native/`.                                                                                              | open                                                                                                                                                                                                                                                                  |
| A-006 | MEDIUM   | Documentation accuracy and operational usability              | `docs/adr/`                                                                                                                                                     | ADR numbering is duplicated (`001-*` and `002-*` each appear twice), making references ambiguous and weakening the decision log as a source of truth.                                                                                                    | `find docs/adr -maxdepth 1 -type f                                                                                                                                                                                                                                                | sort`shows duplicate`001`and`002` prefixes.                                                                                                                                                                 | Renumber ADRs into a single ordered sequence and fix inbound references.                                                                                                                                                                                              | open                                                                                                         |
| A-007 | MEDIUM   | Documentation accuracy and operational usability              | `docs/testing.md`, `docs/mobile-native-ux.md`, `docs/roadmap.md`, `docs/architecture.md`                                                                        | Several docs materially drift from current repo reality: outdated test counts/behaviour, stale mobile plugin notes, roadmap assumptions, and conflicting auth wording.                                                                                   | `docs/testing.md` lists outdated counts and command behaviour; `docs/mobile-native-ux.md` references an old biometric package and stale install state; `docs/architecture.md` overview still says “JWT”; roadmap/docs require reconciliation.                                     | Perform a documentation truth pass and update operational docs against current code and workflow behaviour.                                                                                                 | open                                                                                                                                                                                                                                                                  |
| A-008 | MEDIUM   | Auth, session, identity, and token handling                   | `apps/web/app/hooks/use-session-restore.ts`                                                                                                                     | If no refresh token is stored, session restore returns early and skips pending purchase restoration, weakening recovery after interruption.                                                                                                              | Early return on `refreshToken === null` happens before the pending-order restore block.                                                                                                                                                                                           | Decouple purchase recovery from auth restoration so pending payment state restores even when auth state does not.                                                                                           | open                                                                                                                                                                                                                                                                  |
| A-009 | MEDIUM   | Security, privacy, abuse, and trust boundaries                | `apps/web/app/native/webview.ts`                                                                                                                                | `openWebView` accepts plain `http:` URLs, so an insecure redeem URL could be opened in production.                                                                                                                                                       | `assertSafeUrl()` allows both `https:` and `http:` without an environment guard.                                                                                                                                                                                                  | Restrict production redeem URLs to `https:` or an explicit allowlist; only permit `http:` in controlled dev/test contexts.                                                                                  | open                                                                                                                                                                                                                                                                  |
| A-010 | LOW      | Repository hygiene and governance                             | `tests/e2e-mocked/fixtures/mock-ctx.mjs`, verify flow                                                                                                           | `eslint` emits a warning during `npm run verify`, but the workflow still ends with `ALL CHECKS PASSED`, weakening the “clean means clean” standard.                                                                                                      | `npm run verify` reported an unused `eslint-disable` warning in `tests/e2e-mocked/fixtures/mock-ctx.mjs` and still printed success.                                                                                                                                               | Remove the warning and decide whether warnings should fail local verification for this repo.                                                                                                                | done — removed the unused directive and `lint` now runs `--max-warnings=0` so any future warning fails verify.                                                                                                                                                        |
| A-011 | MEDIUM   | Shared types, contracts, and serialization                    | `apps/backend/src/orders/handler.ts`, `packages/shared/src/orders.ts`, `apps/backend/src/openapi.ts`                                                            | The documented/order-contract minimum amount is inconsistent across backend code, shared types, and OpenAPI.                                                                                                                                             | Backend Zod schema uses `.min(0.01)`; shared type comment says `1 ≤ n ≤ 10_000`; OpenAPI schema also documents `.min(1)`.                                                                                                                                                         | Reconcile the contract in code, shared docs, and OpenAPI so clients and generated docs cannot drift on order validation.                                                                                    | open                                                                                                                                                                                                                                                                  |
| A-012 | MEDIUM   | Mobile shell and native integration                           | `.gitignore`, `apps/mobile`, ignored native projects                                                                                                            | iOS and Android native projects exist only as ignored local artifacts, so the mobile release surface is not versioned or auditable from the repo itself.                                                                                                 | `.gitignore` ignores `apps/mobile/ios/` and `apps/mobile/android/`; `git status --ignored --short` shows both directories present only as ignored artifacts.                                                                                                                      | Decide whether native projects are part of the product source of truth; if yes, version them, and if no, document a reproducible generation/configuration process and audit the generated state separately. | open                                                                                                                                                                                                                                                                  |
| A-013 | LOW      | Accessibility, responsiveness, and degraded-network behaviour | `apps/web/app/components/features/Navbar.tsx`                                                                                                                   | The search combobox sets `aria-activedescendant`, but the corresponding option IDs are never rendered, so keyboard/screen-reader focus semantics are broken.                                                                                             | `aria-activedescendant` points to `search-option-${selectedIndex}`; `SearchDropdown` renders `role="option"` buttons without matching `id` attributes.                                                                                                                            | Render stable option IDs and verify keyboard/screen-reader behaviour for the search combobox.                                                                                                               | open                                                                                                                                                                                                                                                                  |
| A-014 | HIGH     | Tests, test quality, and regression confidence                | `apps/web/vitest.config.ts`, `docs/testing.md`, coverage reports                                                                                                | Web test coverage is far below the documentation’s stated confidence picture, with major UI surfaces and routes excluded from coverage and overall web line coverage only ~40%.                                                                          | `npm run test:coverage` reported web lines at `39.84%`; `apps/web/vitest.config.ts` excludes routes and `root.tsx` and sets thresholds as low as 14–25%; `docs/testing.md` presents a much stronger coverage posture.                                                             | Re-baseline coverage expectations honestly, then raise real coverage on routes, auth/purchase UI, and critical components instead of relying on optimistic docs and low thresholds.                         | open                                                                                                                                                                                                                                                                  |
| A-015 | MEDIUM   | Web correctness and UX integrity                              | `apps/web/app/components/features/ClusterMap.tsx`                                                                                                               | Real cluster-fetch failures are swallowed silently, so the map can fail empty with no user-visible error or telemetry path.                                                                                                                              | `updateMarkers()` catches any `fetchClusters()` error and returns early; the comment claims real errors “fall through to the error boundary,” but the catch prevents that.                                                                                                        | Differentiate aborts from genuine failures, surface user feedback, and capture/log real map data failures.                                                                                                  | open                                                                                                                                                                                                                                                                  |
| A-016 | MEDIUM   | Observability, alerting, and incident readiness               | `apps/backend/src/app.ts` `/metrics` output                                                                                                                     | The Prometheus exposition for `loop_circuit_state` emits two `# HELP` lines for the same metric, which is not valid exposition format and can break or degrade scraping/parsing.                                                                         | `/metrics` builder pushes two `# HELP loop_circuit_state ...` lines before the `# TYPE` line.                                                                                                                                                                                     | Emit one valid `HELP` line per metric and move state semantics into the description or separate comments/docs.                                                                                              | open                                                                                                                                                                                                                                                                  |
| A-017 | LOW      | Dependencies, supply chain, licenses, and update posture      | `apps/backend/package.json` / `@hono/node-server`                                                                                                               | Dependency audit reports a known moderate advisory on `@hono/node-server <1.19.13`. The currently confirmed exploit path is limited, but the package is behind a patched version.                                                                        | Escalated `npm audit --audit-level=high` reported GHSA-92pp-h63x-v22m affecting `@hono/node-server 1.19.12`.                                                                                                                                                                      | Upgrade to a patched `@hono/node-server` version after checking compatibility, and document whether the advisory affects this app’s actual usage surface.                                                   | open                                                                                                                                                                                                                                                                  |
| A-018 | MEDIUM   | Auth, session, identity, and token handling                   | `apps/web/app/services/api-client.ts`, backend env contract                                                                                                     | The web client hardcodes `loopweb/loopios/loopandroid` for `X-Client-Id`, while the backend treats those values as configurable env vars. If backend client IDs change, authenticated requests can break after login.                                    | `api-client.ts` defines a hardcoded `clientIdMap`; backend env/schema/docs expose `CTX_CLIENT_ID_WEB                                                                                                                                                                              | IOS                                                                                                                                                                                                         | ANDROID` as configurable values.                                                                                                                                                                                                                                      | Centralize client-id configuration so auth initialization and authenticated follow-up requests cannot drift. | open |
| A-019 | MEDIUM   | Orders, payment, redemption, and business flows               | `apps/web/app/components/features/purchase/RedeemFlow.tsx`, `apps/web/app/native/webview.ts`                                                                    | Popup-blocked or failed webview opens are surfaced as thrown errors by `openWebView`, but `RedeemFlow` never catches them, so the user gets no recovery UI.                                                                                              | `openWebView()` throws on popup-blocked fallback; `handleOpenWebView()` awaits it without a try/catch and click handlers call it via `void handleOpenWebView()`.                                                                                                                  | Catch webview-open failures in `RedeemFlow`, show actionable UI, and prevent unhandled promise rejections during redemption.                                                                                | open                                                                                                                                                                                                                                                                  |
| A-020 | MEDIUM   | Auth, session, identity, and token handling                   | `apps/web/app/hooks/use-session-restore.ts`, `apps/web/app/services/api-client.ts`                                                                              | Failed launch-time refresh attempts do not clear invalid stored refresh tokens, so the app can retry a permanently dead token on every startup.                                                                                                          | `tryRefresh()` returns `null` on refresh failure without clearing storage; `useSessionRestore()` swallows that result and does not call `clearSession()`.                                                                                                                         | On definitive refresh-token failure, clear persisted auth state during session restore instead of leaving stale credentials behind.                                                                         | open                                                                                                                                                                                                                                                                  |
| A-021 | MEDIUM   | Observability, alerting, and incident readiness               | `apps/backend/src/app.ts`, `apps/backend/src/logger.ts`                                                                                                         | Access logging uses Hono’s default logger instead of the repo’s Pino logger, so request logs are not guaranteed to share the same structure, redaction rules, or request-id correlation strategy as the rest of the backend logs.                        | `app.ts` wires `hono/logger` for access logs, while application logs use `logger.ts`/Pino.                                                                                                                                                                                        | Replace the default Hono logger with a Pino-backed request logger so structured logs and redaction rules are consistent end to end.                                                                         | open                                                                                                                                                                                                                                                                  |
| A-022 | MEDIUM   | Observability, alerting, and incident readiness               | `apps/backend/src/app.ts` request metrics                                                                                                                       | Request metrics still have an unbounded-cardinality path for unmatched routes, despite the implementation comment claiming route labels stay bounded.                                                                                                    | Metrics middleware falls back to `c.req.path` when `c.req.routePath` is null; random 404 paths can create arbitrary new metric keys.                                                                                                                                              | Normalize unmatched routes to a fixed label like `NOT_FOUND` instead of raw path strings.                                                                                                                   | open                                                                                                                                                                                                                                                                  |
| A-023 | HIGH     | Security, privacy, abuse, and trust boundaries                | `apps/backend/src/app.ts` rate limiter                                                                                                                          | Per-IP rate limiting keys off the first `X-Forwarded-For` value supplied on the request, so auth and abuse limits depend on external header sanitization instead of a repo-controlled trusted client-IP source.                                          | `rateLimit()` uses `c.req.header('x-forwarded-for')?.split(',')[0]`; tests inject arbitrary `x-forwarded-for` values directly.                                                                                                                                                    | Derive client IP from a trusted proxy boundary or normalized server metadata, and document/reject untrusted forwarded header chains.                                                                        | open                                                                                                                                                                                                                                                                  |
| A-024 | MEDIUM   | Security, privacy, abuse, and trust boundaries                | `apps/web/app/native/secure-storage.ts`, `apps/web/app/stores/auth.store.ts`, `docs/standards.md`                                                               | Native refresh tokens and stored email are persisted in Capacitor Preferences even though the repo standard says mobile refresh tokens must use secure storage only.                                                                                     | `secure-storage.ts` uses `@capacitor/preferences` for refresh token and email; `docs/standards.md` says “Refresh tokens on mobile: Capacitor secure storage only.”                                                                                                                | Move auth persistence to a real secure-storage implementation (Keychain/Keystore-backed) or relax the written standard explicitly and document the resulting risk.                                          | open                                                                                                                                                                                                                                                                  |
| A-025 | MEDIUM   | Security, privacy, abuse, and trust boundaries                | `apps/backend/src/images/proxy.ts`, `apps/backend/fly.toml`, `docs/roadmap.md`                                                                                  | The image proxy’s strongest SSRF mitigation (`IMAGE_PROXY_ALLOWED_HOSTS`) is optional and not enforced by checked-in production config, even though the source documents a known DNS-rebinding TOCTOU limitation without it.                             | `validateImageUrl()` documents the TOCTOU limitation; `env.ts` makes `IMAGE_PROXY_ALLOWED_HOSTS` optional; `apps/backend/fly.toml` does not set it; `docs/roadmap.md` still lists it as a production to-do.                                                                       | Make an allowlist mandatory for production deployment and fail startup if the image proxy is enabled without it.                                                                                            | open                                                                                                                                                                                                                                                                  |
| A-026 | MEDIUM   | Tests, test quality, and regression confidence                | `playwright.config.ts`, `tests/e2e/purchase-flow.test.ts`, `apps/web/app/components/features/Navbar.tsx`                                                        | The local `mobile-safari` Playwright project is configured to run the full purchase-flow suite even though that suite expects desktop-only UI, so the browser matrix is internally inconsistent even after WebKit is installed.                          | `mobile-safari` has no `testMatch` restriction; purchase-flow test expects the navbar search input to be visible; `Navbar.tsx` renders that search input behind `hidden md:block`.                                                                                                | Restrict mobile Safari to mobile-appropriate suites or make the purchase-flow tests layout-agnostic.                                                                                                        | open                                                                                                                                                                                                                                                                  |
| A-027 | MEDIUM   | Security, privacy, abuse, and trust boundaries                | `apps/web` SSR surface                                                                                                                                          | The user-facing SSR web app ships without explicit hardening headers, unlike the backend.                                                                                                                                                                | Local SSR runtime `curl -I http://localhost:3000/` returned only `Date`, `Connection`, and `Keep-Alive`; no `Content-Security-Policy`, `X-Frame-Options`, `Referrer-Policy`, `Strict-Transport-Security`, or similar headers are configured in `apps/web` or `apps/web/fly.toml`. | Add explicit security headers at the SSR layer or deployment edge and verify them in automated tests.                                                                                                       | open                                                                                                                                                                                                                                                                  |
| A-028 | LOW      | Backlog, roadmap, and delivery-risk alignment                 | `apps/web/app/root.tsx`, `apps/web/app/routes/home.tsx`, `README.md`                                                                                            | Current product copy promises cashback even though cashback is explicitly a Phase 2 feature and not present in the shipped flow.                                                                                                                         | Root/home meta descriptions and `README.md` say users “earn cashback”; `docs/roadmap.md` and ADR-002 place cashback in Phase 2.                                                                                                                                                   | Align marketing/meta/docs with current Phase 1 reality and reserve cashback language for roadmap or future-state messaging.                                                                                 | open                                                                                                                                                                                                                                                                  |
| A-029 | LOW      | Web correctness and UX integrity                              | `apps/web/app/routes/orders.tsx`                                                                                                                                | Order history hardcodes a dollar sign before the amount regardless of the order’s fiat currency.                                                                                                                                                         | `OrderRow` renders `${order.amount.toFixed(2)} {order.currency}` while backend/shared contracts allow non-USD currencies.                                                                                                                                                         | Reuse a currency-aware formatter so non-USD orders do not render as values like `$25.00 EUR`.                                                                                                               | open                                                                                                                                                                                                                                                                  |
| A-030 | MEDIUM   | Orders, payment, redemption, and business flows               | `apps/web/app/components/features/purchase/PaymentStep.tsx`, `docs/roadmap.md`                                                                                  | Payment polling has no terminal retry budget despite roadmap/docs claiming transient failures are retried only a limited number of times, so users can sit in retry-until-expiry limbo on persistent failures.                                           | `PaymentStep.tsx` increments `consecutiveErrors` but never uses it to stop polling; `docs/roadmap.md` says payment polling “retries transient up to 5 times, then gives up.”                                                                                                      | Add an explicit retry budget/backoff with user-visible failure handling, or update docs to match the actual indefinite-until-expiry behaviour.                                                              | open                                                                                                                                                                                                                                                                  |
| A-031 | MEDIUM   | Dependencies, supply chain, licenses, and update posture      | `.github/workflows/pr-review.yml`                                                                                                                               | The AI review workflow installs `@anthropic-ai/claude-code` globally at runtime without pinning a version, making review behaviour non-reproducible and broadening CI supply-chain risk.                                                                 | `pr-review.yml` runs `npm install -g @anthropic-ai/claude-code` instead of using a pinned version in repo-managed dependencies.                                                                                                                                                   | Pin the CLI version (or vendor it through the repo/container image) so automated review runs are reproducible and auditable.                                                                                | open                                                                                                                                                                                                                                                                  |
| A-032 | LOW      | Performance, caching, and runtime efficiency                  | `apps/web/app/root.tsx`, `apps/web/app/components/features/ClusterMap.tsx`                                                                                      | The app depends on third-party Google Fonts and CARTO/OpenStreetMap tile infrastructure at runtime, adding privacy leakage and extra failure/performance surface with no self-hosted fallback.                                                           | `root.tsx` links to `fonts.googleapis.com` / `fonts.gstatic.com`; `ClusterMap.tsx` pulls tiles from `basemaps.cartocdn.com` and attribution links to OpenStreetMap/CARTO.                                                                                                         | Decide whether these third-party calls are acceptable; if not, self-host fonts/tiles or document the dependency and its privacy implications explicitly.                                                    | open                                                                                                                                                                                                                                                                  |
| A-033 | MEDIUM   | Mobile shell and native integration                           | `apps/mobile/android/app/src/main/AndroidManifest.xml`, `apps/web/app/native/secure-storage.ts`, `apps/web/app/native/purchase-storage.ts`                      | Android backup is enabled with no exclusion rules, so persisted auth/purchase state can be swept into device/cloud backup channels.                                                                                                                      | `AndroidManifest.xml` sets `android:allowBackup="true"` and defines no `fullBackupContent` or `dataExtractionRules`; native auth/pending-order state is stored via Capacitor Preferences.                                                                                         | Disable backups for sensitive app data or define explicit backup-exclusion rules for auth and purchase state.                                                                                               | open                                                                                                                                                                                                                                                                  |
| A-034 | MEDIUM   | Mobile shell and native integration                           | `apps/mobile/ios/App/App/Info.plist`, `apps/web/app/native/biometrics.ts`                                                                                       | The iOS native project has no `NSFaceIDUsageDescription` despite shipping a biometric-auth plugin and app-lock flow.                                                                                                                                     | `biometrics.ts` uses `@aparajita/capacitor-biometric-auth`; `Info.plist` contains no `NSFaceIDUsageDescription` key.                                                                                                                                                              | Add the required Face ID usage description and verify biometric flows on real iOS hardware before treating the feature as shippable.                                                                        | open                                                                                                                                                                                                                                                                  |
| A-035 | LOW      | Documentation accuracy and operational usability              | `claude-audit.md`, `ctx.postman_collection.json`, `RESEARCH.md`                                                                                                 | Several non-`docs/` support artifacts are stale enough to mislead contributors: the legacy audit file claims everything is complete, and the checked-in Postman collection is for an older DASH-based surface rather than the current Loop/XLM contract. | `claude-audit.md` starts with “All items complete”; `ctx.postman_collection.json` is named `dashdirect` and uses DASH examples; `RESEARCH.md` still outlines a different framework/product plan.                                                                                  | Archive, remove, or clearly mark historical support artifacts so only current operational references remain active.                                                                                         | done — removed `claude-audit.md`, archived `RESEARCH.md` to `docs/archive/2026-pre-implementation-research.md` with a historical-notice header, and updated `docs/audit-checklist.md`. `ctx.postman_collection.json` is gitignored so no repo-side action was needed. |
| A-036 | MEDIUM   | Auth, session, identity, and token handling                   | `apps/backend/src/auth/handler.ts`, `apps/backend/src/orders/handler.ts`                                                                                        | Authenticated backend routes forward any client-supplied `X-Client-Id` upstream without validation, even though client IDs are treated elsewhere as controlled backend configuration.                                                                    | `requireAuth()` copies `X-Client-Id` straight from the request into context; order handlers forward it upstream; auth handlers map platform to configured client IDs instead of trusting raw client input.                                                                        | Validate `X-Client-Id` against an explicit allowlist or derive it server-side so entity-context policy is not left to untrusted clients.                                                                    | open                                                                                                                                                                                                                                                                  |
| A-037 | HIGH     | CI/CD, release safety, and deployment correctness             | GitHub repository governance                                                                                                                                    | The repo’s documented “protected main branch + PR-only workflow” policy is not currently enforceable through GitHub branch protection on the present hosting plan.                                                                                       | `gh repo view LoopDevs/Loop --json ...` shows `LoopDevs/Loop` is private and the viewer has `ADMIN`; `gh api repos/LoopDevs/Loop/branches/main/protection` returns 403 “Upgrade to GitHub Pro or make this repository public to enable this feature.”                             | Upgrade the GitHub plan or change hosting posture so branch protection/rulesets can actually enforce PR-only merges and required checks.                                                                    | open                                                                                                                                                                                                                                                                  |

## Evidence Log

Use short entries. Link files, commands, tests, workflow names, or screenshots/logs where relevant.

| Date       | Workstream          | Evidence                                                                                                                                                                      |
| ---------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-17 | Baseline            | Captured commit SHA, runtime versions, repo footprint, and audit command plan.                                                                                                |
| 2026-04-17 | Verification        | `npm run verify` passed typecheck/build/tests/docs but emitted an ESLint warning in `tests/e2e-mocked/fixtures/mock-ctx.mjs`.                                                 |
| 2026-04-17 | Build               | `npm run build` succeeded for backend and web SSR builds.                                                                                                                     |
| 2026-04-17 | E2E                 | Escalated local `npm run test:e2e` ran 21 tests: 11 passed, chromium purchase-flow failed without backend support, and local `mobile-safari` failed without WebKit installed. |
| 2026-04-17 | Security            | Secret scan and ignored-artifact scan surfaced `scripts/pay-order.mjs` with a hardcoded Stellar secret key and several ignored local artifacts.                               |
| 2026-04-17 | Architecture        | Import-boundary scan found direct `@capacitor/keyboard` dynamic import in `apps/web/app/root.tsx`.                                                                            |
| 2026-04-17 | Web                 | Route/component review found multiple UI surfaces capped at `useMerchants({ limit: 100 })`, truncating catalog coverage.                                                      |
| 2026-04-17 | E2E mocked          | Escalated `npm run test:e2e:mocked` passed both mocked purchase-flow tests.                                                                                                   |
| 2026-04-17 | Contracts           | Order amount contract drift found between backend validation, shared comments, and OpenAPI documentation.                                                                     |
| 2026-04-17 | Mobile              | `git status --ignored --short` confirmed local `apps/mobile/ios` and `apps/mobile/android` directories are present but not versioned.                                         |
| 2026-04-17 | Dependencies        | Escalated `npm audit --audit-level=high` reported one moderate advisory on `@hono/node-server <1.19.13`.                                                                      |
| 2026-04-17 | Coverage            | `npm run test:coverage` reported backend lines `84.94%`, web lines `39.84%`; many web components/routes remain at `0%` and web thresholds are intentionally set very low.     |
| 2026-04-17 | Web map             | `ClusterMap.tsx` review found all `fetchClusters()` failures are swallowed, including genuine backend/network errors.                                                         |
| 2026-04-17 | Observability       | `/metrics` builder in `apps/backend/src/app.ts` emits duplicate `HELP` lines for `loop_circuit_state`.                                                                        |
| 2026-04-17 | Auth config         | `api-client.ts` hardcodes client IDs while backend env/config treats them as configurable.                                                                                    |
| 2026-04-17 | Redeem flow         | `openWebView()` failure path throws, but `RedeemFlow` does not catch or surface it to users.                                                                                  |
| 2026-04-17 | Session restore     | Launch-time refresh failure path leaves stale persisted refresh token in storage.                                                                                             |
| 2026-04-17 | Observability       | Backend uses Hono’s default access logger alongside Pino, and request metrics still fall back to raw paths when no route pattern exists.                                      |
| 2026-04-17 | Security            | Rate limiting is keyed from the first `X-Forwarded-For` value rather than a repo-controlled trusted client-IP source.                                                         |
| 2026-04-17 | Security            | Mobile auth persistence stores refresh tokens in Capacitor Preferences even though the written standard calls for secure storage only.                                        |
| 2026-04-17 | Security            | Image proxy source documents a DNS-rebinding limitation unless `IMAGE_PROXY_ALLOWED_HOSTS` is set; checked-in production config does not enforce it.                          |
| 2026-04-17 | Web runtime         | Local SSR `curl -I` returned only `Date`, `Connection`, and `Keep-Alive`, confirming no explicit HTTP hardening headers on the web surface.                                   |
| 2026-04-17 | Product truth       | SSR HTML/meta and `README.md` still market cashback in present tense even though cashback remains a Phase 2 feature.                                                          |
| 2026-04-17 | Tests               | Playwright config runs `mobile-safari` across the full purchase-flow suite even though the suite expects desktop-only navbar search UI.                                       |
| 2026-04-17 | UX                  | Orders page formatting still prefixes `$` regardless of the backend-provided fiat currency.                                                                                   |
| 2026-04-17 | Payment flow        | `PaymentStep.tsx` tracks `consecutiveErrors` but never enforces a retry ceiling despite roadmap/docs claiming one exists.                                                     |
| 2026-04-17 | Supply chain        | `pr-review.yml` installs `@anthropic-ai/claude-code` globally without a pinned version.                                                                                       |
| 2026-04-17 | External deps       | Web runtime depends on Google Fonts and CARTO/OpenStreetMap tile hosts with no self-hosted fallback or disclosure.                                                            |
| 2026-04-17 | Mobile              | Android native config leaves backups enabled while auth and pending-order state are persisted via Capacitor Preferences.                                                      |
| 2026-04-17 | Mobile              | iOS native project lacks `NSFaceIDUsageDescription` even though biometric auth is wired in the web/native layer.                                                              |
| 2026-04-17 | Support artifacts   | `claude-audit.md`, `ctx.postman_collection.json`, and `RESEARCH.md` contain historical state that conflicts with the current repo and audit posture.                          |
| 2026-04-17 | Auth trust boundary | `requireAuth()` forwards arbitrary client-provided `X-Client-Id` upstream on authenticated order routes.                                                                      |
| 2026-04-17 | Release safety      | GitHub API confirmed branch protection is unavailable for the current private repo/plan despite admin access.                                                                 |

## Open Questions

- Are ignored native projects (`apps/mobile/ios`, `apps/mobile/android`) intentionally excluded from version control, or is the repo missing part of the intended mobile source of truth?

## Residual Risks

- Production-only behaviour still cannot be fully verified from this audit alone: real CTX account/entity behaviour, production secrets, production deployment edge configuration, and App Store / device-only native behaviour may surface additional issues.
- The mobile source of truth remains ambiguous because `apps/mobile/ios` and `apps/mobile/android` are present locally but intentionally ignored from git; that limits confidence in the repo as the sole auditable release artifact.

## Final Audit Gate

Do not mark the audit complete until all are true:

- every workstream has evidence attached
- every critical and high finding has an agreed remediation path
- every blocked item has a concrete reason
- no material gaps remain after a final reconciliation pass
