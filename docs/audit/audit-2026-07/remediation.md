# Loop-app Remediation Ledger — audit 2026-07

**Branch:** `fix/audit-2026-07-remediation` (off audited SHA `83010533`) · **Pushed → [PR #1665](https://github.com/LoopDevs/Loop/pull/1665) against `main` for your review** · **Status: COMPLETE — every finding dispositioned (committed / held / deferred / no-op)**

> **~127 findings committed** (authoritative list = git `Audit-Finding:` trailers). Every remaining finding has an explicit disposition in `remediation-state.json`. Breakdown:
>
> - **Committed & DB-panel-VERIFIED money-logic** (flagged for your co-sign): the payout double-pay+liveness-wedge fix M5a (**3 adversarial-panel rounds**), the procure refund+band trio M5b, and the money mediums MNY-13 (deposit-refund ADR-017 fence — atomic backend+web+openapi), MNY-04-opfloat (cursor attribution), MNY-11-emissioncap (cap bound to minted stroops), MNY-10 (emission destination pinned to registered wallet), MNY-04-hotfloat (CHECK-abort clamp), MNY-15-vaultpending & NS-13 (stranded-money watchdogs), MNY-20 (unmatched-deposit skip, never auto-refunds an unattributable deposit), DOM-03 (self-serve home-currency live-balance guard), FT-04 (excludes redemption burns from the crypto-payouts tax total), CONV-WATCH-01 (watchdog advisory-lock fence).
> - **Committed security**: P2-03 (redeem-URL `javascript:` XSS scheme-gate), P2-04 (native-webview outbound-injection origin-scope — cross-origin redeem-code leak), FE-08 (CSP nonce in SSR meta), P2-07 (admin step-up modal echoes the amount/destination it authorizes), OBS-04 (Sentry scrubber drift), the earlier SSRF/auth cluster.
> - **Committed reliability / i18n / UX / tests**: backend wave-4 tail (REL-05, BK-fleetsize, BK-bodylimit), AGT-06-replay (id-token replay retention sweep), COR-14-enum (db-CHECK↔TS enum parity guard), TST-04/TST-07 (ledger-writer + truncate-harness coverage), and the web wave (P2-08 chrome i18n, FE-12/16(no-op)/17/18/20/23/36/39/40/41/43/57, P2-09, AUD-10, AUD-13).
> - **Held for you — the irreducible classes (per your policy)**: DB migrations (FT-13, DAT-01, MNY-01-INV3, DAT-03, BK-pctcheck, NS-16, NS-03, MNY-06-hotfloat, INT-07), auth-token logic (SEC-02-stepup, NS-09, BK-otpatomic, COR-11, P2-05, P2-06, FE-06, FE-07, FE-09), crypto/signing (MNY-19, NS-10), infra/CI/IaC (NS-07, P2-13, FE-37, FE-38), and documented-money-invariant/business-decision items (MNY-06-vaultredemption's 0.5% buffer, MNY-11-autorefund's new budget value, NS-04/NS-05/NS-08 policy-and-schema features, MNY-22 floor, CONV-AUTH-01, AGT-06-fly, FT-10). Each has a handoff note (and, where produced, a reviewable patch) in `remediation-state.json` → `wave6ProtectedHeld` / `held` / `needsDecision`.
> - **Deferred — need a real device / browser / crawler / e2e harness to verify** (a fixer can write the code but can't prove it on the platform where the bug lives): the mobile/native + SEO/crawler + e2e web items (FE-01/02/03/04/05/10/19/22/24/34/35/55, P2-10/11/12/14, AGT-11, FE-33) — listed in `webTailDispositions`.
> - **No-op (verified not a defect)**: FE-16 (ADR-037 makes support==admin reads by design), DOM-velocity, DAT-11-apy.
>
> **🛡️ Headline validation — the checks-and-balances worked, repeatedly.** The DB-backed default-reject money panel caught **real money defects that green test suites missed**: FT-06 phantom-delivery; the M5a compensate-path double-pay AND the liveness wedge the round-1 fix _introduced_ (a fix that traded one money bug for another — caught round 2); and the MNY-11-onchainrail refund-cap **concurrency window** (two concurrent on-chain refunds both passing a near-full cap because the counted state was established outside the advisory lock — caught and sent back). No money-logic fix committed on a fixer's self-report; several money mediums correctly **held as irreducible** rather than auto-committed. This is exactly the audit→fix rigor the campaign set out to prove, and it is reusable as models improve.

Executes the findings from the [2026-07-11 audit](../../full-taxonomy-audit-2026-07-11-SUMMARY.md) via the audit-suite `/remediate` flow: each fix re-derives the finding, is independently verified (diverse-lens panel for high-stakes), and only commits at a gate-green tip. This ledger is the live progress artifact and the crash-safe record — it is updated as each fix lands.

## Policy for this run (your choices)

- **Autonomy:** full-autonomous, stop-the-line. Fix + commit to the branch; halt on a red gate/suite.
- **Money:** commit money-**logic** fixes (through the strong panel + the CI money-invariant gate), flagged below for your PR review; **HOLD** the irreducible classes — DB migrations, crypto/signing, auth-token logic — as reviewable patches (a unit test can't validate a data backfill or a constant-time compare).
- **Order:** safest-first (hygiene → a11y/i18n/UX → detectability/config/tests → security → concurrency → money-correctness last), with **wave 1 a deliberate cross-section** to prove every path this session.

## ✅ Money gate (now runnable)

A disposable Postgres test DB was stood up (docker `loop-remediation-db`, port **5434**; extra DBs `loop_test2/3/4` for parallel money verification), so the money-invariant gate (`LOOP_E2E_DB=1 … test:integration`) **runs** — every money-logic fix was verified against the live DB by an adversarial default-reject panel before commit (not deferred). To reproduce a money gate locally: `LOOP_E2E_DB=1 DATABASE_URL='postgres://loop:loop@localhost:5434/loop_test' npm run test:integration` from `apps/backend`. Full integration suite was green on the branch tip after each money commit (last observed 279/279).

⚠️ **Housekeeping:** the working tree has stray **untracked** `apps/backend/src/__tests__/sharp-*.ts` + `final-verdict-test.ts` (image-attack test junk from a prior session) that fail a _local_ `apps/backend` typecheck but are NOT on the branch and won't reach the PR. Safe to `rm`.

---

## 🔴 HUMAN MUST REVIEW (ranked by stakes)

**Money commits (landed via the DB-backed default-reject panel — review in the PR):**

| Finding                            | Sev/Dim         | Disposition          | Why review                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Commit     |
| ---------------------------------- | --------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| CONV-MNY-01                        | low→med/**MNY** | ✅ COMMITTED (money) | Emission-conservation pre-check re-scoped to mirror currency to match the 0061 trigger. Both money verifiers cleared it against the **live DB** (83 integration tests through the real `applyAdminEmission` 409 path; byte-level query-vs-trigger match; NULL-mirror edge proven fail-closed; direction-safe — can only turn a 500 into the intended 409). Flagged because it's money-logic, per your policy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `b678a583` |
| FT-01, FT-05, BK-retryexh          | high/**MNY**    | ✅ COMMITTED (money) | **On-chain payout double-pay + liveness wedge — the campaign's highest-stakes fix.** At retry-exhaustion a `transient_horizon` failure with an in-flight 404 (tx submitted, not yet Horizon-indexed) fell through to `markPayoutFailed` + `autoCompensateFailedWithdrawal`; the in-flight tx then landed → **user paid twice**. Fix: an ambiguous in-flight 404 always defers; an exhausted `submitted` row with a persisted hash is resolved off that hash _before any submit_ (landed→confirm; provably-expired-and-404→fail+compensate), never re-submitting; a new `listClaimablePayouts` clause re-picks the exhausted row so it resolves instead of wedging (funds were otherwise stranded). Compensation stays exactly-once (row `FOR UPDATE` + `compensatedAt` re-check) and scoped to legacy-debited withdrawals (no unbacked emission mint). **Took THREE rounds through the DB-backed adversarial panel** — round 1 caught a surviving compensate-path double-pay, round 2 caught a liveness wedge the round-1 fix introduced, round 3 VERIFIED both closed with two independent proven-red experiments (non-mocked: real `listClaimablePayouts` + real compensation primitive). 20 integration + 292 unit + full suite (240 integration on tip) green. `touchesProtectedClass` → **human money co-sign required.** Residuals (both pre-existing / infra-bounded, not surviving defects): a crash-only `txHash IS NULL` exhaustion strand (recoverable via admin reset), and a documented 30s expiry margin. | `5c49a664` |
| FT-03, MNY-15-procure, MNY-22-band | high/**MNY**    | ✅ COMMITTED (money) | **Terminal CTX-payment failures stranded a debited user with no card.** `procureOne` only runs from `state='paid'`, so a config / reconcile / submit failure marked the order terminally `failed` and never refunded — the user paid Loop and got nothing. Now all three arms `autoRefundFailedOrder` (idempotent via the 0013 partial unique index — cannot double-refund) and page ops with a per-kind `ctxPaid` flag (config/submit = false → CTX unpaid; reconcile = true → a prior on-chain memo may exist, ops must reconcile). **MNY-22**: the payment-amount sanity band priced the _expected_ wholesale via the XLM oracle while CTX settles in **USDC**, making the 125% ceiling ~6× too loose — a grossly over-priced/tampered USDC URL sailed under it; now denominated in the settlement asset (XLM path byte-identical). DB-backed default-reject panel **VERIFIED**: refund idempotent under the 0013 index + INV-8 lock, `ctxPaid` mis-classification is paging-only (refund fires unconditionally), band correctly re-denominated, replaced test was a legitimate correction (the old one asserted the stranding bug) proven RED on pre-fix code; 9 live-DB integration + 53 unit + typecheck all green. Full integration suite 234/234 on the branch tip. `touchesProtectedClass` → **needs human money co-sign**.                                                                                                                                                                                    | `2db94fe2` |

| OBS-04 | high/**SEC** | ✅ COMMITTED (security) | Sentry scrubber had drifted from `logger.ts` REDACT*PATHS, leaking 13 secrets (appSecret, PRIVY_APP_SECRET, OTP code, issuer/channel/operator Stellar secrets, API keys) to Sentry. Fix derives the key set from REDACT_PATHS (drift structurally impossible). 2-verifier panel cleared it: strict-superset verified (0 dropped, +13 recovered), leak-closure traced at every nesting level, no import cycle. One verifier flagged it protected-for-awareness (technical read clean) → committed + flagged here rather than held, as it only \_strengthens* a control. | `d766bd17` |

_Coverage follow-ups (verifier-flagged, worth adding as new findings):_

- _CONV-MNY-01: no end-to-end integration test for the shared-mirror 409-vs-500 path._
- _OBS-04: the scrubber only redacts **string** secret values (`typeof==='string'`), so a numeric secret slips; and `exception.values[].value` / `event.user` regions aren't key-walked. Pre-existing, orthogonal to the drift — new SEC findings._

⚠️ **Environment note:** your loop-app working tree has stray **untracked** `apps/backend/src/__tests__/sharp-*.ts` + `final-verdict-test.ts` files (image-attack test junk from a prior session) with lint errors. They're not on the branch and won't reach the PR, but they make a _local_ `apps/backend` typecheck fail spuriously. The tracked tree typechecks clean (0 errors). Consider deleting them.

---

## Ledger (every finding → a disposition)

| Finding       | Sev/Dim      | Outcome          | Commit     | Notes                                                                                                                                                                                                                                                                         |
| ------------- | ------------ | ---------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUD-16        | low/HLT      | ✅ FIXED         | `0b303bd7` | Extracted duplicated `formatCashbackPct` → `~/utils/format-cashback` + characterization test. Verified behavior-equivalent, gate green.                                                                                                                                       |
| AUD-07        | med/UXP      | ✅ FIXED         | `d77eaa64` | Dead link: shop CTA passed merchant **id** where `/gift-card/:name` resolves by **slug**. Fixed to slug + renamed prop for type-honesty. Independently verified (test proven red `m-1`→`argos`); gate green (159 tests).                                                      |
| AUD-12        | med/I18N     | ✅ FIXED         | `c435f9e2` | `settings.cashback#formatAmount` duplicated the canonical bigint formatter and used the **host** locale. Delegated to `formatMinorCurrency` + `useLocaleTag`, preserving the `+`/`-` sign convention. Verified (en-IN lakh-grouping test proven red); gate green (161 tests). |
| CONV-MNY-01   | low→med/MNY  | ✅ FIXED (money) | `b678a583` | First money commit. Landed via DB-backed default-reject panel once the test DB was stood up. See HUMAN-MUST-REVIEW.                                                                                                                                                           |
| P2-A/B/C      | low/ACC      | ✅ FIXED         | `47f508c8` | Wave 2 a11y batch: Avatar `alt` falls back on empty/whitespace; Skeleton exposes a polite SR status (matches Spinner); Toast dismiss meets WCAG 2.5.8 24×24. All 6 tests proven red; independently verified; gate green (47 tests).                                           |
| AUD-11, ONB-4 | low/UXP+I18N | ✅ FIXED         | `66518e31` | Customer-voiced `INSUFFICIENT_BALANCE`; distinct lockout copy for `TOO_MANY_ATTEMPTS` (was indistinguishable from a transient throttle). Both proven red; verified; `RATE_LIMITED`/429 provably untouched.                                                                    |
| P2-DATE       | med/I18N     | ✅ FIXED         | `5984f92a` | `settings.cashback` `formatDate` used the host locale → threaded the route locale (like AUD-12). Proven red (en-CA); verified; gate green (14 tests).                                                                                                                         |

| P2-DATE-SWEEP | med/I18N | ✅ FIXED | `788db667` | Extracted canonical `formatDateTime` into `i18n/format.ts` (closing the file's own comment/code drift) and routed the sibling `formatDate` copies through it. Re-derivation caught the finding was _partly wrong_: only OrderPayoutCard + PendingPayoutsCard were host-locale (fixed to route locale); `admin.orders` already used a deliberate `ADMIN_LOCALE` ops-pin → preserved byte-identical, not regressed. Verified; gate green (57 tests). |

| P2-DATE-SWEEP2 | med/I18N | ✅ FIXED | `7d30eae2` | Routed the remaining host-locale date sites through `formatDateTime` (MobileHome, auth → route locale; admin.cashback → ADMIN_LOCALE house convention; settings.cashback consolidated; +1 sibling found in-file). Verifier scrutinized the admin output-shape change → matches the sibling audit table. Proven red; gate green (136 tests). **Zero host-locale date sites remain — the class is fully closed.** |

| FT-06 | high/OBS | ✅ COMMITTED (detectability) | `96483727` | `sendWebhook` returned `true` when the monitoring webhook was unset → watchdogs latched a breach as "delivered" and never re-fired (phantom delivery). Fix: return `false` + one-time warn. **Rejected on the first attempt** and retried — see below. Retry completed the fix (dependent watchdog test + stale docs) and the full 210-test integration gate is green (verified by re-running it). |

**Running total: 14 findings committed (11 commits) · 0 held · 0 final-rejected (1 rejected→retried→committed) · every finding accounted for.** All three risk tiers proven end-to-end (web / money / security+detectability).

### 🛡️ Validation highlight — the default-reject panel caught a bad fix (FT-06)

FT-06's first attempt was contract-correct but **incomplete**: it left a dependent integration test asserting the old phantom-`true` behavior, turning the integration gate red. The fixer _and_ the unit-only verifier both wrongly believed that test was skipped. **Only the verifier that ran the actual integration gate against the live DB caught it**, and the default-reject panel (any REJECT kills) blocked the commit. It went back for a bounded retry, which completed the fix and genuinely re-greened the gate (210/210, re-verified). This is the exact "tests pass because nobody ran the real gate" failure mode the hardening (test-proven-red + run-the-real-gate + diverse-lens panel + default-reject) exists to stop — working as designed on money-adjacent code.

### Discovered during remediation — the host-locale date defect is SYSTEMIC (audit undercounted it)

The audit found ~2 date-locale issues; fixing them surfaced a whole class. Confirmed host-locale date sites still to sweep (flagged by fixers/verifiers, outside each unit's scope fence):

- `components/features/home/MobileHome.tsx:825` & `:834` (user-facing)
- `routes/auth.tsx:342` (user-facing)
- `routes/admin.cashback.tsx:495` (admin — likely should use `ADMIN_LOCALE`)
- `routes/settings.cashback.tsx:94` — the P2-DATE-fixed local copy; trivial consolidation onto the new shared `formatDateTime`

_(This is a headline property: **remediation is a discovery mechanism.** A recurring-class fix reveals the class's true size — feed these back into the audit findings. Also queued: the ONB-4 `Retry-After` cross-file plumbing follow-up.)_

## Waves (plan)

1. **Cross-section (in progress)** — AUD-07, AUD-12, CONV-MNY-01 (+ AUD-16 done). Proves bug/hygiene/money/hold paths.
2. Hygiene / code-health / docs-drift breadth (HLT/DOC).
3. Accessibility / i18n / UX (ACC/I18N/UXP).
4. Detectability / observability / config / tests (OBS/CFG/TST/CID).
5. Security / privacy non-crypto (SEC/PRV).
6. Concurrency / reliability (CON/REL).
7. Money-correctness (MNY/DOM) — last, needs the DB gate.

_Resumable: a killed run continues by reading committed finding-ids from `git log --format=%s remediation/audit-2026-07` and skipping them._
