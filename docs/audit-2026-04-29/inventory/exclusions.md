# Exclusions

## Explicit exclusions from the audit subject

These paths are generated by the audit itself and are not part of the product being audited:

- `docs/audit-2026-04-29/**`

Reason:

- self-referential audit artifacts would otherwise contaminate file-disposition coverage and create meaningless findings against the audit machinery itself

## Notable non-tracked surfaces

- generated native project trees under `apps/mobile/ios` and `apps/mobile/android` may exist locally but are intentionally not versioned per ADR 007
- local env files, build outputs, and dependency directories are excluded unless a phase explicitly inspects them as runtime artifacts
