# Inventory Convention

Phase 0 should populate this folder with:

- `git-ls-files.txt`: all tracked files in scope
- `phase-map.md`: file-to-phase ownership map
- `exclusions.md`: generated, external, or intentionally excluded surfaces with reasons
- optional supporting dumps for file counts or tree snapshots

The audit is not complete until every tracked in-scope file has a disposition.
