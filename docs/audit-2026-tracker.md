# Loop — Cold Adversarial Audit (2026-04) — Tracker

> Source of truth for audit status. See [`audit-2026-adversarial-plan.md`](./audit-2026-adversarial-plan.md) for the plan.
>
> **This tracker is live-updated during execution.** The plan is frozen; this tracker is the state.
>
> **Operator-side action required:** rows that need the user (not the agent) to flip a GitHub repo / org / personal-account setting are gathered in [`audit-2026-admin-handoff.md`](./audit-2026-admin-handoff.md) — workflow is "operator flips → ping agent → agent verifies via `gh api` → tracker row flips."

## Plain-English summary

A cold, adversarial audit of the Loop codebase was executed across 20 phases (0–19 plus a dedicated Phase 6.5 for financial correctness), using 15 parallel subagents orchestrated by a lead auditor, against commit `450011d` on 2026-04-23. The audit was independent of the prior `codebase-audit.md` / `audit-tracker.md` program and re-derived every conclusion from primary evidence (file reads, command outputs, empirical probes).

**The headline finding is that the codebase is not ready to take live traffic.** 467 findings were filed across every severity band: 10 Critical, 79 High, 171 Medium, 164 Low, 43 Info. The 10 Criticals concentrate in two areas — **authentication / authorization** (A2-119 org-level 2FA disabled; A2-550 + A2-551 unverified CTX JWT lets any authenticated user act as any other user) and **money flow** (A2-601 credit-funded orders stuck in `pending_payment` forever; A2-602 payout retry path unreachable; A2-610/611/700 `accrue-interest` corrupts balances under multi-currency and loses-updates under concurrent admin adjustments — all three empirically reproduced; A2-619 cross-currency order validation silently mismatches; A2-720 migration 0011 missing from drizzle's `_journal.json` so the admin idempotency table is never created on fresh deploys, bricking every ADR-017 admin write at boot). Beyond the Criticals, the 79 Highs span security gaps (Loop JWT missing `iss`/`aud` claims, Pino redaction misses signing keys, web serves zero security headers, refresh-token reuse detected but family not revoked), operational-readiness gaps (no runbooks, no on-call, no DSR flow, no DR plan, no signing-cert calendar, no staging env, no runtime kill-switches), cross-app contract drift (~30 admin shapes + 13 `/me*` shapes web-only, no CI drift detector, no client-version header, enum-exhaustiveness not enforced), and CI/CD posture (no GitHub Environments, no rollback rehearsal, no SAST, no preview deploys).

**Most of the audit's value came from empirical confirmation.** Phase 6.5 spun up an ephemeral Postgres, replayed all 12 migrations, seeded synthetic data, and ran the ledger invariant `balance = SUM(transactions)` against it — several prior phases' static-read findings (accrue-interest race, cross-currency mismatch, lost-update under adjustments) were independently reproduced with runtime evidence. Phase 18's 29-attack red-team playbook found 10 defenses that held cleanly (SQL injection, bigint overflow, trust-proxy discipline, procurement race, OTP timing), re-confirmed 13 prior findings with empirical attempts, and filed 9 net-new findings dominated by admin-accountability gaps (A2-2006 no tamper-evident audit trail; A2-2008 admin reads unaudited — a compromised admin can bulk-exfil PII with zero trace). The legacy audit program's closed findings were reconciled: nothing it marked "done" regressed silently, but three items aged poorly (A-025 image SSRF now exposed to DNS-rebinding, A-035 stale support artifacts still linked, A-037 branch protection now reopened at higher severity). Per the pre-launch operating model, every finding — Critical through Info — enters a single remediation queue ordered by severity; sign-off is blocked until all 467 are resolved or accepted-with-rationale-and-second-reviewer. The audit's data-gathering phase is complete; the remediation phase is where the next chapter of work lives.

---

---

## Index

### Finding counts (by severity)

| Severity  | Count   |
| --------- | ------- |
| Critical  | 10      |
| High      | 79      |
| Medium    | 171     |
| Low       | 164     |
| Info      | 43      |
| **Total** | **467** |

### Finding counts (by status)

| Status      | Count |
| ----------- | ----- |
| open        | 467   |
| in-progress | 0     |
| resolved    | 0     |
| accepted    | 0     |
| wontfix     | 0     |
| deferred    | 0     |

### Phase progress

Plan has 20 phases (0–19 plus 6.5). Progress updated as each phase exits.

| Phase | Title                           | Status      | Audited-by | Reviewed-by | Evidence                                                                     | Findings (C/H/M/L/I)                   |
| ----- | ------------------------------- | ----------- | ---------- | ----------- | ---------------------------------------------------------------------------- | -------------------------------------- |
| 0     | Inventory                       | ✅ complete | claude     | pending     | [phase-0-inventory.md](./audit-2026-evidence/phase-0-inventory.md)           | 0/0/1/3/0                              |
| 1     | Governance & Repo Hygiene       | ✅ complete | agent      | pending     | [phase-1-governance.md](./audit-2026-evidence/phase-1-governance.md)         | 1/4/9/10/3                             |
| 2     | Architecture compliance         | ✅ complete | agent      | pending     | [phase-2-architecture.md](./audit-2026-evidence/phase-2-architecture.md)     | 0/0/1/3/3                              |
| 3     | Dependencies & Supply chain     | ✅ complete | agent      | pending     | [phase-3-dependencies.md](./audit-2026-evidence/phase-3-dependencies.md)     | 0/0/3/5/2                              |
| 4     | Build & release reproducibility | ✅ complete | agent      | pending     | [phase-4-build-release.md](./audit-2026-evidence/phase-4-build-release.md)   | 0/1/6/4/1                              |
| 5     | Backend per-module audit        | ✅ complete | agent (×4) | pending     | 5a + 5b + 5c + 5d                                                            | 7/15/38/43/7                           |
| 6     | Database & Data Layer           | ✅ complete | agent      | pending     | [phase-6-database.md](./audit-2026-evidence/phase-6-database.md)             | 2/6/10/7/0 (some corroborate P4/P5c)   |
| 6.5   | Financial Correctness           | ✅ complete | agent      | pending     | [phase-6.5-financial.md](./audit-2026-evidence/phase-6.5-financial.md)       | 0/5/3/1/0 (empirical runtime evidence) |
| 7     | API surface                     | ✅ complete | agent      | pending     | [phase-7-api.md](./audit-2026-evidence/phase-7-api.md)                       | 0/0/4/8/3                              |
| 8     | Web per-module audit            | ✅ complete | agent (×2) | pending     | 8a + 8b                                                                      | 0/4/6/17/10                            |
| 9     | Mobile shell                    | ✅ complete | agent      | pending     | [phase-9-mobile.md](./audit-2026-evidence/phase-9-mobile.md)                 | 0/2/6/6/0                              |
| 10    | Shared package                  | ✅ complete | agent      | pending     | [phase-10-shared.md](./audit-2026-evidence/phase-10-shared.md)               | 0/0/7/9/0                              |
| 11    | Cross-app integration           | ✅ complete | agent      | pending     | [phase-11-cross-app.md](./audit-2026-evidence/phase-11-cross-app.md)         | 0/10/17/7/0                            |
| 12    | Security deep-dive              | ✅ complete | agent      | pending     | [phase-12-security.md](./audit-2026-evidence/phase-12-security.md)           | 0/4/7/4/0 (new only)                   |
| 13    | Observability                   | ✅ complete | agent      | pending     | [phase-13-observability.md](./audit-2026-evidence/phase-13-observability.md) | 0/4/15/5/4                             |
| 14    | Testing                         | ✅ complete | agent      | pending     | [phase-14-testing.md](./audit-2026-evidence/phase-14-testing.md)             | 0/4/6/5/0                              |
| 15    | Documentation                   | ✅ complete | agent      | pending     | [phase-15-docs.md](./audit-2026-evidence/phase-15-docs.md)                   | 0/1/9/15/5                             |
| 16    | CI/CD & Automation              | ✅ complete | agent      | pending     | [phase-16-cicd.md](./audit-2026-evidence/phase-16-cicd.md)                   | 0/4/6/6/1                              |
| 17    | Operational readiness           | ✅ complete | agent      | pending     | [phase-17-operational.md](./audit-2026-evidence/phase-17-operational.md)     | 0/14/12/4/1                            |
| 18    | Adversarial / Red-team          | ✅ complete | agent      | pending     | [phase-18-redteam.md](./audit-2026-evidence/phase-18-redteam.md)             | 0/1/5/2/1 (new)                        |
| 19    | Synthesis & sign-off            | ✅ complete | claude     | pending     | this document                                                                | — (clerical)                           |

---

## Findings

Findings are recorded below their owning phase. Each finding follows the shape from plan §3.3.

### Phase 0 — Inventory

Complete. Evidence: [phase-0-inventory.md](./audit-2026-evidence/phase-0-inventory.md). Commit SHA at capture: `84fc581`.

#### A2-001 — Favicon files are 0 bytes

| Field       | Value                                                                                                                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Severity    | **Medium**                                                                                                                                                                                              |
| Status      | open                                                                                                                                                                                                    |
| Files       | `apps/web/public/loop-favicon.ico` (0B), `apps/web/public/loop-favicon.png` (0B), referenced from `apps/web/app/root.tsx:135-136`                                                                       |
| Evidence    | `wc -c` on the two files returns `0`. `root.tsx` emits `<link rel="icon" href="/loop-favicon.ico">` and `<link rel="icon" type="image/png" href="/loop-favicon.png">`.                                  |
| Impact      | Browsers fetching the favicon receive an empty response. Tab icon is blank / generic. User-facing polish defect; not a security issue.                                                                  |
| Remediation | Replace both files with actual favicon binaries exported from `loop-favicon.svg` (which is non-empty), or remove the `<link>` entries and keep only the SVG favicon (supported by all modern browsers). |
| Owner       | _unassigned_                                                                                                                                                                                            |

#### A2-002 — Unreferenced root-level `looplogo.svg` is a duplicate

| Field       | Value                                                                                                                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Severity    | **Low**                                                                                                                                                                                                 |
| Status      | open                                                                                                                                                                                                    |
| Files       | `looplogo.svg` (repo root, 1572 bytes)                                                                                                                                                                  |
| Evidence    | `sha256sum looplogo.svg apps/web/public/loop-logo.svg` → identical hash `a38062762e6d...`. `grep -rn looplogo.svg` across `apps/`, `docs/`, `scripts/`, top-level `.md`/`.ts` files returns no matches. |
| Impact      | Dead file. Confusing during editing (which logo is canonical?).                                                                                                                                         |
| Remediation | Delete `looplogo.svg`.                                                                                                                                                                                  |
| Owner       | _unassigned_                                                                                                                                                                                            |

#### A2-003 — Duplicate PNG logos not referenced from web source

| Field       | Value                                                                                                                                                                                                                     |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Severity    | **Low**                                                                                                                                                                                                                   |
| Status      | open                                                                                                                                                                                                                      |
| Files       | `apps/web/public/loop-logo.png` (19259B), `apps/web/public/loop-logo-square.png` (19259B)                                                                                                                                 |
| Evidence    | `shasum -a 256` on both files → identical hash `2e3aaa10d321...`. Scoped grep through `apps/web/app/**/*.{ts,tsx,css}` returns zero references (only matches are Android build artifacts that are themselves gitignored). |
| Impact      | Two files' worth of dead static-asset payload shipped with the web build.                                                                                                                                                 |
| Remediation | Delete both unless intentionally kept as a public URL consumed outside the app (e.g. og:image for a specific path — confirm with owner before deleting).                                                                  |
| Owner       | _unassigned_                                                                                                                                                                                                              |

#### A2-004 — `loop-favicon.svg` has content but no reference

| Field       | Value                                                                                                                                                                                                                                         |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Severity    | **Low**                                                                                                                                                                                                                                       |
| Status      | open                                                                                                                                                                                                                                          |
| Files       | `apps/web/public/loop-favicon.svg` (234 bytes)                                                                                                                                                                                                |
| Evidence    | `grep -rn loop-favicon.svg apps/` returns no reference from application code (only build artifacts). `root.tsx:135-136` links `.ico` and `.png` but not the `.svg`.                                                                           |
| Impact      | An intended SVG favicon that was never wired up. Either the file is stale or `root.tsx` is incomplete. Ties in with A2-001 — the SVG may be the fix for A2-001.                                                                               |
| Remediation | Add `<link rel="icon" type="image/svg+xml" href="/loop-favicon.svg">` to `root.tsx` alongside the raster fallbacks (preferred modern pattern), **or** delete the file if it's no longer intended. Decision depends on how A2-001 is resolved. |
| Owner       | _unassigned_                                                                                                                                                                                                                                  |

### Phase 1 — Governance

Complete. Evidence: [phase-1-governance.md](./audit-2026-evidence/phase-1-governance.md). Commit SHA at capture: `450011d`. 27 findings (1 Critical / 4 High / 9 Medium / 10 Low / 3 Info).

| ID     | Severity     | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-101 | **High**     | ~~Branch protection permits admin bypass; zero required reviews~~ **resolved-pending-review**: branch protection on `main` now enforces `required_approving_review_count: 1` with `dismiss_stale_reviews: true`. Verified via `gh api repos/LoopDevs/Loop/branches/main/protection`. Bypass allowance is scoped to `AshFrancis` (the operator) — agent-driven merges now route through the normal review gate. Confirmed by PR #981 admin-merge attempt failing with `At least 1 approving review is required by reviewers with write access`.                                |
| A2-102 | Medium       | No signed-commit policy on `main`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-103 | **High**     | CODEOWNERS references non-existent `@LoopDevs/engineering` team                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-104 | Medium       | ~~CODEOWNERS coverage stale vs admin / credits / stellar / migration surfaces~~ **resolved-pending-review**: `.github/CODEOWNERS` now covers `apps/backend/src/auth/`, `credits/`, `admin/`, `orders/`, `payments/`, plus `db/migrations/`, `db/schema.ts`, `packages/shared/src/`, and `docs/adr/`. Each section carries an `A2-104:` comment block explaining the rationale (ledger writers, ADR-017 admin primitives, ADR-016 payout submit, etc.). Phantom `@LoopDevs/engineering` team is a separate finding (A2-103).                                                   |
| A2-105 | **High**     | ~~Secret-scanning + dependabot-alerts + push-protection disabled at repo & org level~~ **resolved-pending-review**: verified at HEAD via `gh api repos/LoopDevs/Loop --jq '.security_and_analysis'` — all three audit-named features are now `enabled`: `secret_scanning`, `secret_scanning_push_protection`, and `dependabot_security_updates`. Non-provider patterns + validity checks remain off (Phase-2 hardening; they generate heavy noise without first establishing a triage rota). Repo went public earlier this session, which unlocked these GitHub-side toggles. |
| A2-106 | Low          | ~~Dependabot `reviewers` points at missing team~~ **resolved-pending-review**: `reviewers` directive removed from `.github/dependabot.yml`. CODEOWNERS now handles review assignment for paths that need it (A2-104 closed by #823); dependabot PRs land in the normal approval flow without a phantom `LoopDevs/engineering` team ping.                                                                                                                                                                                                                                      |
| A2-107 | Medium       | ~~Commitlint is client-side only; no server-side enforcement~~ **resolved-pending-review**: `.github/workflows/ci.yml` Quality job runs `npx commitlint --from <base.sha> --to <head.sha> --verbose` on every PR. Husky's local commit-msg hook still runs first; this is the bypass guard — `--no-verify`, broken local install, or GitHub-web edits all funnel through the same `commitlint.config.js` server-side. A2-107 marker inline.                                                                                                                                   |
| A2-108 | Low          | ~~Branch-prefix hook / CONTRIBUTING.md type-list drift~~ **resolved-pending-review** by A2-108/110 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| A2-109 | Medium       | ~~CONTRIBUTING.md overstates review gate vs actual~~ **resolved-pending-review**: `CONTRIBUTING.md:35-37` now states verbatim that the `@LoopDevs/engineering` team referenced in CODEOWNERS hasn't been created (audit A2-103), so GitHub silently skips the required-reviews rule and branch protection allows admin squash-merge without approval today — with the explicit guidance "Treat a reviewer as mandatory on those paths anyway; enforcement flips on once the team lands". The doc is now honest about the gap rather than overstating.                         |
| A2-110 | Low          | ~~CONTRIBUTING.md CI job-count drift (6 vs 7)~~ **resolved-pending-review** by A2-108/110 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-111 | Low          | ~~No `.gitattributes`~~ **resolved-pending-review**: `.gitattributes` exists at repo root with the `A2-111` marker — forces LF on text files (shell, SQL, ESLint configs, drizzle migration SQL) so a Windows checkout can't silently CRLF-munge them, and marks the lockfiles + generated proto + drizzle migration-meta as `linguist-generated`.                                                                                                                                                                                                                            |
| A2-112 | Info         | `ctx.postman_collection.json` at repo root (see lint-docs sentinel)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-113 | Low          | ~~PR template is decorative; authors don't use it~~ **resolved-pending-review**: `.github/pull_request_template.md` is no longer decorative — it's a structured form with a Type-of-change selector, a mandatory Checklist section, and explicit Documentation + Security subsections that authors complete via checkboxes. Recent merged PRs (#993/#995/#997, this session) all populate the template.                                                                                                                                                                       |
| A2-114 | **High**     | ~~`superfly/flyctl-actions/setup-flyctl@master` pins a branch~~ **resolved-pending-review**: `.github/workflows/ci.yml:63` now reads `superfly/flyctl-actions/setup-flyctl@fc53c09e1bc3be6f54706524e3b82c4f462f77be # v1.5` — SHA-pinned to release 1.5 with the human-readable tag in a comment.                                                                                                                                                                                                                                                                             |
| A2-115 | Medium       | ~~All first-party actions tag-pinned, not SHA-pinned~~ **resolved-pending-review**: every `uses:` directive across `.github/workflows/*.yml` now SHA-pins (verified at HEAD via `grep -E "uses: \w+/\w+@" .github/workflows/*.yml \| grep -v "@[0-9a-f]\{40\}"` — empty result). Same posture A2-114 closed for setup-flyctl, applied across all third-party actions; first-party (`actions/*`) included.                                                                                                                                                                     |
| A2-116 | Low          | ~~`pr-automation.yml`, `pr-review.yml` lack top-level `permissions`~~ **resolved-pending-review**: both workflows carry `permissions: {}` at the workflow level with per-job narrow grants (`contents: read`, `pull-requests: write`); `A2-116:` marker present in both files.                                                                                                                                                                                                                                                                                                |
| A2-117 | Info         | Repo `allowed_actions: "all"` — unbounded Marketplace access                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-118 | Info         | Org webhook inventory not verifiable from audit session (auth scope)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-119 | **Critical** | `LoopDevs` org does not require 2FA; both members are admins                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-120 | Medium       | Org on free plan — no audit log retention                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-121 | Medium       | Stale "stellarspendtest server" SSH key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-122 | Low          | No GPG keys on file for push-capable members                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-123 | Low          | Three merge modes enabled despite squash-only convention                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A2-124 | Medium       | ~~Public repo has no LICENSE~~ **resolved-pending-review**: `LICENSE` exists at repo root (stated-proprietary; matches the README "License" section). Same closure as A2-1800.                                                                                                                                                                                                                                                                                                                                                                                                |
| A2-125 | Medium       | ~~No SECURITY.md on a public pre-launch repo~~ **resolved-pending-review**: `SECURITY.md` exists at repo root — supported versions, reporting channels (GitHub Security Advisories + `security@loopfinance.io` PGP), Critical→Low severity examples (incl. LOOP-asset unbacked minting), out-of-scope cases, response-target SLAs, safe-harbour. Same closure as A2-1802.                                                                                                                                                                                                     |
| A2-126 | Low          | ~~No issue templates~~ **resolved-pending-review**: `.github/ISSUE_TEMPLATE/` carries `bug.yml`, `feature.yml`, and `config.yml` (blank issues disabled, security reports routed to GitHub Security Advisories per `SECURITY.md`). Audit captured at an earlier commit; templates landed subsequently.                                                                                                                                                                                                                                                                        |
| A2-127 | Low          | ~~No CHANGELOG.md~~ **resolved-pending-review**: `CHANGELOG.md` exists at repo root — Keep-a-Changelog format with policy preamble, audit-id-cited entries grouped by domain. Same closure as A2-1803 in Phase 15.                                                                                                                                                                                                                                                                                                                                                            |

**Blockers / deferred probes:**

- A2-118 org-webhook inventory needs `admin:org_hook` scope — `gh auth refresh -h github.com -s admin:org_hook` before re-running.
- Git-history secret scan: clean across 979 commits / 2 author emails.
- Signed-commits: 979/979 unsigned; local `gpg` missing, but `required_signatures.enabled: false` on branch protection is corroborating primary evidence.

### Phase 2 — Architecture compliance

Complete. Evidence: [phase-2-architecture.md](./audit-2026-evidence/phase-2-architecture.md). Commit SHA at capture: `450011d`. 7 findings (0 Critical / 0 High / 1 Medium / 3 Low / 3 Info). ADR reconciliation: 21 `in-sync`, 1 `drifted-minor`, 0 withdrawn, 0 never-implemented.

| ID     | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-201 | Low      | ~~AGENTS.md rule #1 claims "no server-side data fetching in loaders" but `sitemap.tsx` has a documented loader fetch~~ **resolved-pending-review**: AGENTS.md rule #1 now carries the documented exception ("The only loader that fetches server-side is `routes/sitemap.tsx`: crawlers need an XML response, not a React shell. Any new loader-side fetch beyond that needs a comment explaining why TanStack Query doesn't fit.")       |
| A2-202 | Medium   | ~~AGENTS.md rule #2 says backend doesn't mint JWTs / generate OTPs / send emails — contradicted by ADR-013 implementation (`auth/tokens.ts` HS256 mint, `auth/otps.ts`, `auth/native.ts`)~~ **resolved-pending-review**: AGENTS.md rule #2 + the "Auth has two paths" section (lines 57 + 101) correctly describe Loop-native (HS256 mint, OTP generation, email send) and legacy CTX-proxy coexisting behind `LOOP_AUTH_NATIVE_ENABLED`. |
| A2-203 | Low      | ~~ADR-011 promises `DEFAULT_USER_CASHBACK_PCT_OF_CTX` + `DEFAULT_LOOP_MARGIN_PCT_OF_CTX` env vars — not present in backend source~~ **resolved-pending-review**: both present at `apps/backend/src/env.ts:142/146` with Zod validation + sum-check at `env.ts:407-412`, and in `apps/backend/.env.example:79-80`.                                                                                                                         |
| A2-204 | Low      | ~~`packages/shared/src/api.ts` `ApiErrorCode` enum lags behind backend-emitted codes (e.g. `IDEMPOTENCY_KEY_REQUIRED`)~~ **resolved-pending-review**: every backend `{ code: '...' }` emission (18 total) has a matching entry in the shared `ApiErrorCode` const at `packages/shared/src/api.ts:63` — verified via `grep -rhoE "code: '[A-Z_]+'" apps/backend/src` against the enum. The file already carries an `A2-204:` marker.       |
| A2-205 | Info     | ~~ADRs 009, 010, 011, 013, 014, 017 still say "Proposed" despite shipped implementation~~ **resolved-pending-review**: all six ADRs now show `Status: Accepted` (verified by `grep "^Status:" docs/adr/{009,010,011,013,014,017}*.md`). Same closure as A2-1813 in Phase 15.                                                                                                                                                              |
| A2-206 | Info     | No automated secret scan (gitleaks / trufflehog) in pre-commit or CI                                                                                                                                                                                                                                                                                                                                                                      |
| A2-207 | Info     | ~~AGENTS.md "quick commands" don't mention `LOOP_WORKERS_ENABLED`~~ **resolved-pending-review**: env summary now lists `LOOP_WORKERS_ENABLED` with a comment pointing at ADR-016 + the `LOOP_STELLAR_OPERATOR_SECRET` wiring prerequisite. Full docs remain in `docs/development.md`.                                                                                                                                                     |

### Phase 3 — Dependencies & Supply chain

Complete. Evidence: [phase-3-dependencies.md](./audit-2026-evidence/phase-3-dependencies.md) plus sibling `phase-3-npm-ls-all.txt`, `phase-3-npm-audit.json`, `phase-3-npm-outdated.json`, `phase-3-install-hooks.txt`. Commit SHA at capture: `450011d`. 10 findings (0 Critical / 0 High / 3 Medium / 5 Low / 2 Info).

| ID     | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-301 | Medium   | ~~`vitest` pinned `4.1.0` in backend+web but lockfile actually resolves `4.1.4` (peer-forced by `@vitest/coverage-v8`); exact-pin misleading~~ **resolved-pending-review**: `apps/backend/package.json:43` and `apps/web/package.json:60` updated to `"vitest": "4.1.4"` — matches the resolved version `npm install` produces. `package-lock.json` regenerated; `npm install` is now stable on a fresh checkout (no peer-forced bump-on-install behaviour). The pinned and resolved versions agree.                                                                                                                                                                                                                                                                                                          |
| A2-302 | Low      | ~~5 extraneous packages in `node_modules/` not in lockfile (`@emnapi/*`, `@napi-rs/wasm-runtime`, `@tybys/wasm-util`); dev env not `npm ci`-reproducible~~ **resolved-pending-review**: re-verified `package-lock.json` at HEAD — `@emnapi/core`, `@emnapi/runtime`, `@emnapi/wasi-threads`, `@napi-rs/wasm-runtime`, and `@tybys/wasm-util` are all listed as **optional transitive dependencies** of platform-specific WASM-binding packages (resolved entries with `"optional": true`). `npm ci` produces a deterministic install of these on every fresh checkout; the audit captured a state where `npm install` had bumped them past what `npm ci` would lock to, but lockfile-driven `npm ci` is reproducible. Verified via `npm ls --all` returning zero `extraneous` after a clean `npm ci` rebuild. |
| A2-303 | Medium   | ~~`esbuild@0.18.20` dev-server CVE (GHSA-67mh-4wv8-2f99) reachable via `drizzle-kit → @esbuild-kit/esm-loader` deprecated chain; 4 moderate advisories, same root cause~~ **resolved-pending-review**: `apps/backend/package.json` pins `drizzle-kit` at `0.31.10` — modern release that no longer depends on the `@esbuild-kit/*` deprecated chain or the vulnerable esbuild range. `npm audit --omit=dev` runs clean in CI's Security audit job.                                                                                                                                                                                                                                                                                                                                                            |
| A2-304 | Medium   | `@bufbuild/buf@1.68.2` postinstall shells to `npm install` as fallback binary fetcher — supply-chain surface on `--omit=optional` fresh clones                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| A2-305 | Low      | ~~libvips (`@img/sharp-libvips-*@1.2.4`) is LGPL-3.0-or-later, only non-permissive licence in tree; requires OSS attribution page before public launch~~ **resolved-pending-review** by `docs/third-party-licenses.md` — libvips entry covers LGPL §6 attribution via repo doc + planned `/licenses` site page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-306 | Low      | ~~`apps/web/public/leaflet/marker-*.png` are BSD-2 copies shipped without upstream copyright notice~~ **resolved-pending-review** by `docs/third-party-licenses.md` — Leaflet marker images entry preserves the BSD-2 copyright notice from the upstream `LICENSE`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-307 | Low      | ~~`@hono/node-server 1.19.14 → 2.0.0` major bump Dependabot's minor-and-patch group will never file; 19 outdated total~~ **resolved-pending-review**: `apps/backend/package.json:22` now pins `@hono/node-server` at `2.0.0`; lockfile updated alongside. The major-version bump that Dependabot's grouped config wouldn't file landed via a deliberate manual upgrade.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-308 | Low      | ~~52 transitive packages have multiple installed versions (3 `esbuild` majors, 3 `ansi-styles`, etc.); no CVE-reachable but dedup recommended~~ **resolved-pending-review**: `npm ls --all` at HEAD reports ~748 deduped entries vs the audit's baseline of 52 multi-version conflicts. Significant convergence happened naturally as transitive deps caught up to a single major (esbuild via drizzle-kit's 0.31.10 bump from A2-303, ansi-styles via the Vite + vitest 4 alignment). No CVE-reachable findings remain.                                                                                                                                                                                                                                                                                      |
| A2-309 | Info     | ~~`postgres@3.4.9` licensed Unlicense (public-domain dedication); record on OSS attribution page~~ **resolved-pending-review** by `docs/third-party-licenses.md` — postgres driver listed explicitly; the rationale for listing a no-attribution-required package is spelled out so legal's due-diligence surface stays complete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-310 | Info     | ~~Only root `package.json` declares `engines.node`; workspace-level declarations absent~~ **resolved-pending-review**: each of `apps/backend/package.json`, `apps/web/package.json`, `apps/mobile/package.json`, and `packages/shared/package.json` now declares `"engines": { "node": ">=22.0.0" }`, matching the root pin. A workspace-only consumer that ran `npm install` from a sub-package would previously skip the engine check; now it fails the same way the root install does.                                                                                                                                                                                                                                                                                                                     |

### Phase 4 — Build & release

Complete. Evidence: [phase-4-build-release.md](./audit-2026-evidence/phase-4-build-release.md). Commit SHA at capture: `450011d`. 12 findings (1 High / 6 Medium / 4 Low / 1 Info).

| ID     | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-401 | **High** | ~~Drizzle `_journal.json` missing migration 0011; `runMigrations` silently skips `admin_idempotency_keys`~~ **resolved-pending-review** by Batch 1 PR 2 (corroborated by Phase 6 as A2-720 at Critical)                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-402 | Medium   | ~~No `HEALTHCHECK` directive in either Dockerfile~~ **resolved-pending-review**: `apps/backend/Dockerfile:60` and `apps/web/Dockerfile:54` both declare `HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3` against the in-container probe. Fly + any Kubernetes-style orchestrator now reads container health from the directive.                                                                                                                                                                                                                                                                                                     |
| A2-403 | Medium   | ~~Base image `node:22-alpine` pinned by floating tag, not SHA256 digest~~ **resolved-pending-review**: both Dockerfiles' `FROM` lines now read `node:22-alpine@sha256:8ea2348b068a9544dae7317b4f3aafcdc032df1647bb7d768a05a5cad1a7683f` (verified by `grep "FROM " apps/{backend,web}/Dockerfile`). The same SHA appears in both the builder + runtime stages.                                                                                                                                                                                                                                                                                              |
| A2-404 | Medium   | ~~`npm run proto:generate` produces a diff vs checked-in `clustering_pb.ts` (quote + whitespace drift; no prettier step)~~ **resolved-pending-review**: script now chains `prettier --write 'packages/shared/src/proto/**/*.ts'` after `buf generate`, so the generated output lands in the same shape the repo's prettier config enforces.                                                                                                                                                                                                                                                                                                                 |
| A2-405 | Low      | ~~`NSFaceIDUsageDescription` copy in overlay script drifted from live Info.plist; overlay only writes when absent~~ **resolved-pending-review**: `apps/mobile/scripts/apply-native-overlays.sh` factored Face-ID + Location usage-description writes into a `plist_set_or_add_string` helper. Reads the current value via `PlistBuddy Print`; if it matches canonical, no-op; if drifted, rewrites with `PlistBuddy Set`; if absent, `Add`. Drifted live values now reconcile instead of being silently kept. Idempotent — a clean run produces zero writes.                                                                                                |
| A2-406 | Low      | ~~Overlay script `cp`s unconditionally even when source == dest; mtime churn on every pass~~ **resolved-pending-review**: `apps/mobile/scripts/apply-native-overlays.sh` introduces a `cp_if_changed` helper using `cmp -s` to skip writes when bytes already match. Applied to every overlay `cp` of an XML / Java / PNG / properties file (~10 sites — backup rules, MainActivity, launcher icons across 5 densities × 3 files, splash drawable across 11 densities, splash_icon, ic_launcher_background, styles.xml). Eliminates the mtime churn that bumped Xcode's incremental-build cache and made `git status` on the gitignored native trees noisy. |
| A2-407 | Medium   | ~~No `[deploy] release_command` in fly.toml; migration-vs-deploy ordering documented only in an inline code comment~~ **resolved-pending-review**: `apps/backend/fly.toml:14` declares `release_command = "node apps/backend/dist/migrate-cli.js"` — Fly runs the drizzle migrator BEFORE swapping traffic to the new release, so a forward-incompatible migration aborts the deploy rather than corrupting an in-flight request. Closes ADR-012's deploy ordering concern.                                                                                                                                                                                 |
| A2-408 | Medium   | SBOM, provenance attestation, container CVE scanning, image signing all absent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| A2-409 | Medium   | ~~GitHub Actions pinned by tag, not commit SHA (`superfly/flyctl-actions/setup-flyctl@master` worst)~~ **resolved-pending-review**: same closure as A2-114 + A2-115 — every `uses:` directive across `.github/workflows/*.yml` is now SHA-pinned (verified by `grep -E "uses: \w+/\w+@" .github/workflows/*.yml \| grep -v "@[0-9a-f]\{40\}"` returning empty). The flyctl-actions case A2-114 called out is now `setup-flyctl@fc53c09e1bc3be6f54706524e3b82c4f462f77be # v1.5`.                                                                                                                                                                            |
| A2-410 | Low      | `.dockerignore` only at repo root, not per-app; docs tell operators to `cd apps/backend && fly deploy`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A2-411 | Low      | ~~`.husky/pre-push` doesn't call `scripts/verify.sh`; skips typecheck/lint/format:check~~ **resolved-pending-review**: `.husky/pre-push` now invokes `npm run verify` which runs `scripts/verify.sh` (typecheck + lint + format:check + lint:docs + test). A2-411 marker in the hook.                                                                                                                                                                                                                                                                                                                                                                       |
| A2-412 | Info     | ~~Drizzle snapshot chain broken (`0001_snapshot.json`..`0011_snapshot.json` absent); `db:generate` would diff against initial schema~~ **resolved-by-policy**: hand-written SQL migrations are the intentional workflow (triggers / partial indexes / cross-column CHECKs aren't representable in Drizzle's schema diff — see A2-703). `drizzle.config.ts` + `apps/backend/AGENTS.md` §"Recipe: Add a DB migration" now both document the policy; `db:generate` retained as an emergency baselining escape hatch only.                                                                                                                                      |

Full per-finding evidence + remediation pointers in the evidence file.

**Deferred probes** (tools not installed on the audit host): `trivy`, `grype`, `syft`, `cosign`. Their absence in CI is independently confirmed and rolled into A2-408.

### Phase 5 — Backend per-module

Phase split across four sub-agents (5a–5d) per surface. Completion marked when all four return.

#### Phase 5a — `apps/backend/src/admin/`

Complete. Evidence: [phase-5a-admin.md](./audit-2026-evidence/phase-5a-admin.md). Commit SHA at capture: `450011d`. 14 findings (0 Critical / 1 High / 5 Medium / 8 Low / 0 Info).

| ID     | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-500 | Medium   | ~~Admin idempotency snapshots never expire; no handler honors TTL; no sweeper~~ **resolved-pending-review**: `apps/backend/src/admin/idempotency.ts:31` documents the 24h TTL closure (`ADMIN_IDEMPOTENCY_TTL_HOURS`, default 24h). `sweepStaleIdempotencyKeys()` at `apps/backend/src/admin/idempotency.ts:185` deletes stale rows; the hourly sweep is wired at `apps/backend/src/app.ts:1748`. ADR-017 #6 TTL promise structurally enforced.                                                                                                                                                                                                                                                               |
| A2-501 | Medium   | ~~`GET /api/admin/discord/config` omits `DISCORD_WEBHOOK_ADMIN_AUDIT`~~ **resolved-pending-review**: already surfaced at `admin/discord-config.ts:39` via `adminAudit: statusOf(env.DISCORD_WEBHOOK_ADMIN_AUDIT)` with paired test at `__tests__/discord-config.test.ts:18`.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-502 | **High** | ~~`PUT /merchant-cashback-configs/:merchantId` not ADR-017-compliant: no Idempotency-Key, no reason, no `{result, audit}` envelope~~ **resolved-pending-review**: `admin/handler.ts:82` carries the `A2-502` marker and uses the full ADR-017 chain (validateIdempotencyKey, withIdempotencyGuard, buildAuditEnvelope, notifyAdminAudit + notifyCashbackConfigChanged fanout).                                                                                                                                                                                                                                                                                                                                |
| A2-503 | Low      | ~~`merchants-catalog-csv` row-cap applied to in-memory count, not the unbounded SQL SELECT~~ **resolved-pending-review** by A2-503/504 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-504 | Low      | ~~`merchants-catalog-csv` contains a no-op self-comparison `where(eq(merchantId, merchantId))`~~ **resolved-pending-review** by A2-503/504 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A2-505 | Medium   | ~~3 CSV admin endpoints missing from `openapi.ts`~~ **resolved-pending-review**: 19 admin CSV routes registered in `apps/backend/src/openapi.ts` match the 19 `'/api/admin/.*\.csv'` literals in `apps/backend/src/app.ts` (verified by counting matches in both files). `lint-docs.sh` §9 enforces ongoing parity bidirectionally.                                                                                                                                                                                                                                                                                                                                                                           |
| A2-506 | Medium   | ~~9 non-CSV admin endpoints missing from `openapi.ts` (incl. base `GET /api/admin/orders`)~~ **resolved-pending-review**: every non-CSV admin route in `apps/backend/src/app.ts` now has a matching `registry.registerPath(...)` in `apps/backend/src/openapi.ts` after this PR — verified by normalising `:param` ↔ `{param}` and `diff`-ing the two route sets (zero residual). The last residual was `/api/admin/users/recycling-activity` (the JSON sibling of the already-registered CSV); registered in this PR alongside the other Phase-5d closures. `scripts/lint-docs.sh` §9 hard-fails on any future drift.                                                                                        |
| A2-507 | Low      | ~~4 admin handlers lack try/catch; errors lose handler-scoped logger bindings~~ **resolved-pending-review** by A2-507 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A2-508 | Medium   | ~~13 admin handlers have no paired test file (mostly PII-exposing user drill-down)~~ **resolved-pending-review**: `apps/backend/src/admin/__tests__/` now contains 82 test files. The 13 user-drill-down + adjacent handlers all have paired coverage: `user-search`, `user-by-email`, `user-detail`, `user-credits` (+ `-csv`), `user-credit-transactions` (+ `-csv`), `user-cashback-summary`, `user-cashback-by-merchant`, `user-cashback-monthly`, `user-flywheel-stats`, `user-payment-method-share`, `merchant-flows`, `reconciliation`. Verified by `ls apps/backend/src/admin/__tests__/ \| grep -E "user-\|merchant-flows\|reconciliation"`. PII-exposing handlers now have shape + redaction tests. |
| A2-509 | Low      | ~~`POST /api/admin/merchants/resync` has no Idempotency-Key (idempotent by mutex only)~~ **resolved-pending-review**: `admin/merchants-resync.ts:87` wraps the write in `withIdempotencyGuard` with the standard `IDEMPOTENCY_KEY_REQUIRED` 400 branch.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-510 | Low      | ~~`user-credits-csv` truncation sentinel diverges from canonical shape~~ **resolved-pending-review** by A2-510 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-511 | Low      | ~~`notifyAdminAudit` posts full admin email to Discord (inconsistent with tail-only user-id convention)~~ **resolved-pending-review** by A2-511 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| A2-512 | Low      | ~~UUID regex duplicated across ~14 files; drift-prone~~ **resolved-pending-review** by A2-512 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| A2-513 | Low      | ~~Several merchant-scoped handlers skip the `/^[A-Za-z0-9._-]+$/` + 128-char check used by siblings~~ **resolved-pending-review** by A2-513 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

#### Phase 5b — `auth/`, `ctx/`, `users/`, `config/`

Complete. Evidence: [phase-5b-auth-users.md](./audit-2026-evidence/phase-5b-auth-users.md). Commit SHA at capture: `450011d`. 20 findings (2 Critical / 3 High / 4 Medium / 9 Low / 2 Info). IDs non-contiguous in the A2-55x / A2-56x / A2-57x ranges — full population in the evidence file.

| ID     | Severity     | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| A2-550 | **Critical** | ~~`requireAuth` CTX pass-through accepts unverified JWTs; every `/me` handler trusts `decodeJwtPayload(bearerToken).sub` → any authenticated user can read/write any other user's row~~ **resolved-pending-review** by Batch 1 PR 1                                                                                                                                                                                                                                                                                                                                                                                                   |
| A2-551 | **Critical** | ~~`PUT /api/users/me/stellar-address` rewrites arbitrary user's payout destination (downstream of A2-550)~~ **resolved-pending-review** by Batch 1 PR 1 (closed automatically by A2-550 fix)                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-552 | Low          | ~~`setHomeCurrencyHandler` count→update race~~ **resolved-pending-review** by A2-552 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-555 | Info         | ~~Upstream 500-char body logs could leak JWT-shaped strings~~ **resolved-pending-review**: `upstream-body-scrub.ts` + `__tests__/upstream-body-scrub.test.ts` — JWT-shape redaction, A2-555 marker.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| A2-556 | Medium       | ~~`nativeRefreshHandler` does not revoke-all on reuse despite docstring~~ **resolved-pending-review**: `apps/backend/src/auth/native.ts:239-251` reuse-detection branch now calls `revokeAllRefreshTokensForUser(record.userId)` on every reuse hit, with a `Refresh-token reuse detected — revoking all refresh tokens for user` log line. Pairs with A2-562 (the wiring) and A2-1608 (the broader stolen-family guarantee).                                                                                                                                                                                                         |
| A2-557 | Low          | ~~Redundant re-verify in `issueTokenPair` → `replacedByJti` link~~ **resolved-pending-review**: `apps/backend/src/auth/native.ts:108`+`:122`+`:191`+`:260` carry `A2-557` markers. `issueTokenPair` now returns the fresh refresh `jti` directly; the rotation handler links the new row's `replacedByJti` without re-verifying the just-signed token, and the wire response strips the internal `refreshJti` field. One fewer `verifyLoopToken` call on the rotation hot path.                                                                                                                                                       |
| A2-558 | Low          | ~~Circuit-open on `request-otp` leaks a 503 fingerprint vs 200 enumeration envelope~~ **resolved-pending-review**: `auth/handler.ts:99-116` catches `CircuitOpenError` and collapses to the generic `{ message: 'Verification code sent' }` 200 envelope; paired test at `__tests__/handler.test.ts:155`.                                                                                                                                                                                                                                                                                                                             |
| A2-560 | Low          | ~~OTP attempts ceiling off-by-one (`lte` vs `lt`)~~ **resolved-pending-review**: `auth/otps.ts:103` uses strict `lt` with `A2-560:` marker; OTP_MAX_ATTEMPTS is the true ceiling.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-561 | Low          | ~~`incrementOtpAttempts` bumps every live row for the email~~ **resolved-pending-review**: `auth/otps.ts:128` bumps only the single-newest-row target with `A2-561:` marker.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-562 | Low          | ~~`revokeAllRefreshTokensForUser` exported/tested but not wired to any route~~ **resolved-pending-review**: `auth/native.ts:235` wires `revokeAllRefreshTokensForUser` in the reuse-detection branch — family-wide revoke on replay.                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-565 | **High**     | ~~`DELETE /api/auth/session` never revokes Loop-native refresh rows; logout leaves 30-day refresh live~~ **resolved-pending-review** by Batch 2A auth PR (#769)                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-566 | **High**     | ~~Social ID-tokens accepted without nonce binding → replayable within provider TTL~~ **resolved-pending-review**: one-shot-consume via `social_id_token_uses` (migration 0019). `auth/id-token-replay.ts::consumeIdToken` records `sha256(token)` after verify; replay hits the PK conflict and the handler returns the same generic 401 as a verify failure. DB errors fail closed as 503 so a postgres blip can't open a replay window.                                                                                                                                                                                             |
| A2-567 | Medium       | ~~Google `iss` exact-match rejects valid `accounts.google.com` (no `https://`) variants~~ **resolved-pending-review**: `auth/social.ts:198` + `auth/id-token.ts:97` accept both `https://accounts.google.com` and `accounts.google.com` variants; test pins both.                                                                                                                                                                                                                                                                                                                                                                     |
| A2-568 | Low          | ~~`/api/auth/social/{google,apple}` missing from `openapi.ts`~~ **resolved-pending-review**: both routes registered at `apps/backend/src/openapi.ts:1885` (`/api/auth/social/google`) and `:1931` (`/api/auth/social/apple`). Closure pairs with A2-662 (the Loop-native order surface) — both surfaces' openapi gaps closed in the same wave.                                                                                                                                                                                                                                                                                        |
| A2-569 | Medium       | ~~`id-token.ts` skips `nbf`, doesn't bound `iat` or `exp-iat`~~ **resolved-pending-review**: `verifyIdToken` enforces `nbf > now + leeway → not_yet_valid`, `iat > now + leeway → iat_future`, `exp - iat > maxLifetimeSeconds → lifetime_exceeded`, and `exp + leeway < now → expired` (leeway now applied to exp too). Defaults: 60s leeway, 3600s max lifetime. 5 new test cases pin each bound including the "within leeway" positive branches.                                                                                                                                                                                   |
| A2-570 | Low          | ~~Social `identities.ts` race can violate `users.email` unique index → 500~~ **resolved-pending-review**: `apps/backend/src/auth/identities.ts:80-130` step-3 INSERT now uses `onConflictDoNothing()` + in-transaction re-SELECT — same race-safe pattern as `findOrCreateUserByEmail` (A2-706). Two parallel social signups for the same brand-new email both reach step 3; the losing INSERT no-ops, the re-SELECT inside the same transaction picks up the winning row, and the identity link uses that. `created` reports honestly (false on the losing side). New unit test pins the race scenario; existing 7 tests still pass. |     |
| A2-571 | **High**     | ~~`EMAIL_PROVIDER=console` bypasses production guard; ships plaintext-OTP logger~~ **resolved-pending-review** by Batch 2A (#770)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-572 | Medium       | ~~`operatorFetch` doesn't retry against next operator on upstream 5xx, contrary to docstring~~ **resolved-pending-review**: `apps/backend/src/ctx/operator-pool.ts:215-247` — 5xx / network error from operator N retries against N+1 (up to `min(2, operators.length)`); 4xx propagates straight through. `A2-572:` markers at lines 215 + 239.                                                                                                                                                                                                                                                                                      |
| A2-573 | Low          | ~~`initialised=true` set before parse; config fix requires restart~~ **resolved-pending-review** by A2-573 PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| A2-574 | Info         | ~~`/api/config` exposes feature-surface unauth, deliberate per ADR 020~~ **resolved-pending-review (working as designed)**: ADR-020 explicitly lists `/api/config` as a Tier-1 public-by-nature path — feature-surface flags drive the unauthenticated homepage render and must be readable without a session. The audit's text already concedes this is "deliberate per ADR 020"; the line is a documentation-confirmation finding rather than a security gap. Closing as "no change required."                                                                                                                                      |

#### Phase 5c — `orders/`, `payments/`, `credits/` (money-flow)

Complete. Evidence: [phase-5c-money-flow.md](./audit-2026-evidence/phase-5c-money-flow.md). Commit SHA at capture: `450011d`. **50 findings** (5 Critical / 7 High / 22 Medium / 14 Low / 2 Info). IDs A2-600 through A2-649.

**Critical (5):**

| ID     | Title                                                                                                                                                                                                                                                 |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-601 | ~~Credit-funded orders have no debit path; never transition to `paid`; stay `pending_payment` until 24h expiry~~ **resolved-pending-review** by Batch 1 PR 4 (#765)                                                                                   |
| A2-602 | ~~Payout worker's "leave in `submitted` for retry" path unreachable — `listPendingPayouts` filters `state='pending'` only; transient failures strand rows forever~~ **resolved-pending-review** by Batch 1 PR 5 (#766). Amplifies A2-603 and A2-1512. |
| A2-610 | ~~`accrueOnePeriod` UPDATE omits the `currency` clause — any multi-currency user has every balance row overwritten on every accrual~~ **resolved-pending-review** by Batch 1 PR 3                                                                     |
| A2-611 | ~~`accrueOnePeriod` writes `row.balanceMinor + accrual` from a pre-txn read — lost-update race against concurrent `applyAdminCreditAdjustment`~~ **resolved-pending-review** by Batch 1 PR 3                                                          |
| A2-619 | ~~Loop-handler returns `chargeMinor` in home currency; watcher validates against `faceValueMinor` in catalog currency — cross-currency orders silently mis-validate~~ **resolved-pending-review** by Batch 1 PR 6 (#767)                              |

**High (7):**

| ID     | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-603 | ~~No watchdog timer on `submitted`-state payouts (amplifies A2-602)~~ **resolved-pending-review**: `LOOP_PAYOUT_WATCHDOG_STALE_SECONDS` (default 300s) drives a rolling watchdog in `apps/backend/src/payments/payout-worker.ts` that re-claims stuck `submitted` rows via a CAS on `attempts` (markPayoutSubmitted / reclaimSubmittedPayout branches at lines 156-181). Same watchdog also closes A2-602.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-605 | ~~Outbound payout memo is `orderId.slice(0,28)` — ~2^-40 birthday collision; separate generator recommended~~ **resolved-pending-review**: `generatePayoutMemo()` in `apps/backend/src/credits/payout-builder.ts:80` produces a fresh 20-char base32 memo with 100 bits of entropy per call (node:crypto CSPRNG). Independent of `orderId` — future order-id shape changes can't narrow collision resistance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A2-613 | ~~Ledger invariant has no DB-level enforcement~~ **resolved-pending-review**: the DB-level invariants that _can_ be expressed as CHECKs were added in migrations 0013/0014/0015 — `credit_transactions_amount_sign` (sign-by-type), `credit_transactions_type_known` / `credit_transactions_currency_known` (enum-like), `credit_transactions_period_cursor_interest_only`, `credit_transactions_reference_unique` (partial unique for at-most-once writers), `user_credits_currency_known`. The cross-table sum invariant `user_credits.balanceMinor == sum(credit_transactions.amountMinor)` per (user_id, currency) is intrinsically not expressible as a single CHECK; it is enforced in the app-layer transactional writers (`applyCashback` / `applyAdminCreditAdjustment` / `accrueOnePeriod` all update both tables in one drizzle txn) and monitored by the `computeLedgerDriftSql` reconciliation surface at `GET /api/admin/reconciliation` (ADR 009), with drift above threshold posting to Discord. |
| A2-614 | ~~No unique constraint on `(type, reference_type, reference_id)` in `credit_transactions` — duplicate-writer risk~~ **resolved-pending-review**: migration 0013 adds partial unique index `credit_transactions_reference_unique ON credit_transactions (type, reference_type, reference_id) WHERE type IN ('cashback', 'refund', 'spend') AND reference_type IS NOT NULL AND reference_id IS NOT NULL`. Scoped to the at-most-once writer types; `adjustment` + `interest` + `withdrawal` deliberately excluded for the reasons documented inline in the migration.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| A2-621 | ~~`sweepStuckProcurement` can flip CTX-fulfilled orders to `failed` with no reconciliation~~ **resolved-pending-review**: `apps/backend/src/orders/transitions.ts:302-319` fires a per-row `notifyStuckProcurementSwept` Discord alert (orderId + userId + merchantId + chargeMinor + chargeCurrency + ctxOperatorId + procuredAtMs) so ops can reconcile against CTX manually before any refund. Channel description at `apps/backend/src/discord.ts:961` calls out the double-spend risk explicitly. Per-row (not aggregated) on purpose — a non-zero sweep is rare and each row needs its own drill-down.                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-622 | ~~Procurement txn gap — CTX charge and Loop ledger write can't be atomic; crash between them leaves CTX billed, user uncredited, no refund writer~~ **resolved-pending-review**: the "can't be atomic" part is intrinsic (external API call cannot sit inside a DB txn). Mitigation chain: (a) `markOrderProcuring` persists the in-flight state before the CTX call; (b) A2-621's `sweepStuckProcurement` flips zombies to `failed` after cutoff; (c) A2-621's per-row Discord alert forces manual ops reconciliation before any user-facing refund; (d) admin refund writer `POST /api/admin/users/:userId/refunds` exists for ops-initiated refunds under ADR-017 + A2-901.                                                                                                                                                                                                                                                                                                                                   |
| A2-626 | ~~Cursor write after transitions loop; no cursor-age watchdog~~ **resolved-pending-review**: 1-minute cadence cursor-age probe at `apps/backend/src/payments/watcher.ts:461` fires a Discord alert (`apps/backend/src/discord.ts:679` / 967) when the payment watcher's Horizon cursor hasn't advanced in >10 min — catches crashed / hung tickers that would otherwise silently stop processing deposits. One-shot per stuck period.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

Remaining 38 findings (A2-604, A2-606–A2-609, A2-612, A2-615–A2-618, A2-620, A2-623–A2-625, A2-627–A2-649) are in the evidence file — 22 Medium / 14 Low / 2 Info.

**Invariants the agent could NOT confirm at this commit:**

- Ledger sum invariant against prod-shaped dataset (needs Phase 6.5 SQL method)
- Interest accrual idempotency across reruns (scheduler not wired end-to-end)
- `accrue-interest.ts` under real Postgres (tests mock `db`, so A2-610 slid past the test suite)
- Memo collision bounds empirically

#### Phase 5d — `clustering/`, `merchants/`, `images/`, `public/` + top-level files

Complete. Evidence: [phase-5d-rest.md](./audit-2026-evidence/phase-5d-rest.md). Commit SHA at capture: `450011d`. 28 findings (0 Critical / 4 High / 7 Medium / 12 Low / 5 Info). IDs A2-650 through A2-677.

**High (4):**

| ID     | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-652 | ~~Operator-secret rotation unimplemented despite ADR 016~~ **resolved-pending-review**: rotation primitive shipped — `apps/backend/src/env.ts:286-289` declares `LOOP_STELLAR_OPERATOR_SECRET_PREVIOUS` as an optional zod field (Stellar-secret regex), `apps/backend/src/logger.ts:75-80` redacts both bare and `*.`-globbed forms of the current and previous secret, and `docs/deployment.md:120` documents the slot. ADR-016's rotation procedure is operator-side (deploy with new active key + previous as fallback signer until in-flight `submitted` payouts confirm, then drop previous) — Stellar-signed transactions are sequence-pinned so a live in-flight tx survives a key rotation regardless. Audit captured the pre-`_PREVIOUS` state. |
| A2-655 | ~~Pino redaction config misses 6 secret-bearing env keys~~ **resolved-pending-review**: `apps/backend/src/logger.ts:77-110` redaction paths now cover every secret-bearing env key: `LOOP_STELLAR_OPERATOR_SECRET` (+ `_PREVIOUS`), `LOOP_JWT_SIGNING_KEY` (+ `_PREVIOUS`), `GIFT_CARD_API_KEY` / `_SECRET`, `DATABASE_URL`, `SENTRY_DSN`, and three Discord webhook URLs (`DISCORD_WEBHOOK_ORDERS` / `_MONITORING` / `_ADMIN_AUDIT`) — both bare key and `*.<key>` globs so a boot-time `log.debug({ env })` can't leak them. Comment at `logger.ts:81` credits `A2-655 + A2-1601`.                                                                                                                                                                      |
| A2-662 | ~~5 app.ts routes missing from `openapi.ts`~~ **resolved-pending-review**: all five routes the audit flagged (`/api/admin/cashback-monthly`, `/api/admin/merchant-stats.csv`, `/api/admin/orders`, `/api/auth/social/{google,apple}`, plus `/api/orders/loop` + `/api/orders/loop/{id}`) are registered in `apps/backend/src/openapi.ts`; the Loop-native surface block carries an `A2-662 / A2-1504` citation at `openapi.ts:265` / `2218`. `scripts/lint-docs.sh` §9 allowlist for these routes removed in the same PR — the in-flight-exemption band-aid is no longer needed, so CI now hard-fails on any regression.                                                                                                                                  |
| A2-672 | ~~Image-proxy DNS-rebinding TOCTOU still open~~ **deferred-by-ADR-005-§5**: documented accepted limitation for Phase 1. Practical mitigation is `IMAGE_PROXY_ALLOWED_HOSTS` (enforced in production, allowlisting only `spend.ctx.com` + the CTX S3 bucket). Full fix — custom `undici.Dispatcher.connect` that reuses the already-resolved IP with the expected `Host` header — is tracked in ADR-005 §5 as "Revisit: before the image proxy is ever allowed to accept arbitrary third-party hostnames." Source comment at `apps/backend/src/images/proxy.ts:233` carries the same `KNOWN LIMITATION` marker.                                                                                                                                            |

**Medium (7):** ~~A2-650 unrate-limited merchant reads~~ **resolved-pending-review**: `apps/backend/src/app.ts:827-861` rate-limits the entire `/api/merchants/*` family — `GET /api/merchants` (180/60s), `/all` (60/60s), `/by-slug/:slug` (120/60s), `/cashback-rates` (120/60s), `/:id` (120/60s), public sister at `:881` (60/60s). · A2-653 5 env vars bypass the zod schema · ~~A2-654 un-typed emergency SSRF override flag~~ **resolved-pending-review**: `apps/backend/src/env.ts:91` declares `DISABLE_IMAGE_PROXY_ALLOWLIST_ENFORCEMENT: z.enum(['1']).optional()` — only the literal string `'1'` is accepted; anything else fails `parseEnv()` at boot. The opt-out check at `env.ts:480` reads the parsed value rather than `process.env`. · ~~A2-664 / A2-665 / A2-670 uncaught DB throws → 500 on merchant/cashback-rate reads~~ **resolved-pending-review**: `apps/backend/src/merchants/handler.ts:211` (`A2-664 / A2-1006` marker) catches DB throws on `/cashback-rates` and returns the empty-rates shape per ADR-020 never-500. `:272` (`A2-665` marker) does the same for `/:merchantId/cashback-rate` returning `{ userCashbackPct: null }`. A2-670 closed by the same try/catch pattern in the per-merchant handler. · ~~A2-676 `PublicCashbackPreview` duplicated between backend + shared (ADR-019 breach)~~ **resolved-pending-review**: `apps/backend/src/public/cashback-preview.ts:62-63` carries the `A2-676 + ADR 019` marker and `import + export type { PublicCashbackPreview } from '@loop/shared'`. The web side imports the same canonical type via `services/public-stats`. One source of truth across the wire boundary.

**Low (12):** A2-651 misleading routing comment · A2-656 notifier catalog ordering · A2-657 uuid-slice inconsistency · A2-659 silent MAX_PAGES cap · A2-661 rate-limit map expired-entry eviction · A2-663 400/401 undocumented · A2-666 500 undocumented · A2-669 refresh-lock is boolean not promise · A2-671 untyped `c.get` cast · A2-673 image cache byte-count drift · A2-674 webhook URLs accept non-`https` · A2-677 fallback `asOf` misleading.

**Info (5):** A2-658 currency-length unchecked · A2-660 `/health` + `/metrics` unrate-limited · A2-667 probe-flap race · A2-668 openapi versioning · A2-675 openapi shape re-declaration.

---

**Phase 5 aggregate (5a + 5b + 5c + 5d):** 112 findings (7 Critical / 15 High / 38 Medium / 43 Low / 7 Info).

### Phase 6 — Database & Data Layer

Complete. Evidence: [phase-6-database.md](./audit-2026-evidence/phase-6-database.md). Commit SHA at capture: `450011d`. Migrations 0000–0011 replayed against ephemeral Postgres 16. **26 findings** (2 Critical / 6 High / 10 Medium / 7 Low / 0 Info). Two findings corroborate prior-phase findings at higher severity — noted in "Cross-phase corroboration" below.

**Critical (2):**

| ID     | Title                                                                                                                                                                                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-700 | ~~`accrue-interest.ts` UPDATE filters only by `user_id` (not `currency`), writes stale balance outside `FOR UPDATE` — ledger invariant breaks for multi-currency users, concurrent cashback lost~~ **resolved-pending-review** by Batch 1 PR 3 (corroborates A2-610/611) |
| A2-720 | ~~Migration `0011_admin_idempotency_keys.sql` absent from `_journal.json` — drizzle's migrator iterates journal only; fresh deploys never create the table; every ADR-017 admin write would fail on boot~~ **resolved-pending-review** by Batch 1 PR 2                   |

**High (6):**

| ID     | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-701 | ~~Schema declares `uniqueIndex` named `..._pk_idx`; migration installs `PRIMARY KEY ..._pk`; divergent~~ **resolved-pending-review**: `apps/backend/src/db/schema.ts` `admin_idempotency_keys` index block rewritten — `uniqueIndex('admin_idempotency_keys_pk_idx').on(...)` replaced with `primaryKey({ columns: [...], name: 'admin_idempotency_keys_pk' })`, matching the constraint shape migration 0011 installs. Previously a `drizzle-kit generate` would have emitted `DROP CONSTRAINT admin_idempotency_keys_pk` + `CREATE UNIQUE INDEX admin_idempotency_keys_pk_idx` — losing PK metadata without changing semantic uniqueness.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-702 | ~~`user_credits` has no primary key, only a unique index~~ **resolved-pending-review**: migration `0017_user_credits_primary_key.sql` drops the `user_credits_user_currency` unique index and promotes to `CONSTRAINT user_credits_pkey PRIMARY KEY (user_id, currency)`. Schema.ts matches via `primaryKey({ columns: [t.userId, t.currency], name: 'user_credits_pkey' })`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| A2-703 | ~~`merchant_cashback_configs_audit` trigger + function not represented in `schema.ts`; next `drizzle-kit generate` would drop the ADR-011 audit trail~~ **resolved-pending-review**: migration `0016_cashback_config_audit_trigger_guard.sql` re-asserts the trigger + plpgsql function idempotently (`CREATE OR REPLACE FUNCTION` + `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER`). `schema.ts:274` inline comment documents the drizzle-kit blind spot so a human reviewer knows the trigger lives outside the drizzle-modelled schema. Any accidental drizzle-push regeneration re-runs 0016 and restores the audit trail.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-704 | ~~No CHECK enum on `credit_transactions.currency` — any 3-char string accepted~~ **resolved-pending-review**: migration `0014_credit_tx_currency_check.sql` adds `credit_transactions_currency_known CHECK (currency IN ('USD', 'GBP', 'EUR'))`. Schema.ts `credit_transactions_currency_known` CHECK matches.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| A2-705 | ~~No CHECK enum on `orders.currency` (catalog side) — any 3-char string accepted~~ **resolved-pending-review**: migration `0021_orders_currency_check.sql` adds `orders_currency_known CHECK (currency IN ('USD', 'GBP', 'EUR'))`. Paired with the existing `orders_charge_currency_known` CHECK on chargeCurrency, so both currencies on an order row now share the same allowed set. Schema.ts `orders_currency_known` CHECK matches. A fourth-currency rollout (e.g. CAD) will be a deliberate migration touching every `*_currency_known` CHECK across the schema so all four tables move together.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-706 | ~~`users.email` has no unique index; Loop-native signup race acknowledged in-code but unfixed~~ **resolved-pending-review**: migration `0020_users_email_unique.sql` adds a partial unique index `users_email_loop_native_unique ON users (LOWER(email)) WHERE ctx_user_id IS NULL`. Scoped to Loop-native rows so CTX-proxied users' denormalised `email` copies can still collide with a separate Loop-native row (two identity planes). `findOrCreateUserByEmail` in `apps/backend/src/db/users.ts:89` now INSERTs with `onConflictDoNothing()` and re-SELECTs on the empty-returning path so a racing loser returns the winning caller's row instead of erroring — the old in-comment "signup throughput is low enough that the race is not worth a heavier constraint today" acknowledgement is replaced with the structural fix it asked for. Backend unit test coverage: 2 new tests (`A2-706: re-selects the raced row when ON CONFLICT DO NOTHING absorbs the insert` + the pathological "both miss" throw case) alongside the updated happy-path assertion. |

Remaining 17 findings (A2-707–A2-719, A2-721–A2-725) in the evidence file — 10 Medium / 7 Low.

**Cross-phase corroboration:**

- **A2-720** (Critical) corroborates and escalates **A2-401** (High from Phase 4). Phase 4 agent flagged the journal omission; Phase 6 agent replayed migrations and confirmed the table is never created in a fresh deploy — real severity is Critical, not High.
- **A2-700** (Critical) corroborates **A2-610** + **A2-611** (both Critical from Phase 5c). Same bug in `accrue-interest.ts`. Three independent evidences (static read, query-shape analysis, migration replay) all converge; remediation is a single fix.

**Deferred probes:** `pg_stat_user_indexes` (never-hit / bloat — plan G4-05), `pg_stat_statements`, Fly PG `max_connections` cross-check, autovacuum tuning, read-replica topology. All require prod/live-DB telemetry — deferred to Phase 17 per plan scope.

### Phase 6.5 — Financial Correctness

Complete. Evidence: [phase-6.5-financial.md](./audit-2026-evidence/phase-6.5-financial.md). Commit SHA at capture: `450011d`. Ephemeral Postgres 16 replay + seeded + drift-injected + raced. Property tests (50k iterations) on `@loop/shared/cashback-realization.ts` + `repo.ts::computeCashbackSplit` + `price-feed.ts::convertMinorUnits` — all 13 invariants hold.

**9 new findings (A2-900 through A2-908):** 0 Critical / 5 High / 3 Medium / 1 Low / 0 Info.

| ID     | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-900 | **High** | ~~Reconciliation endpoint is `user_credits`-anchored LEFT JOIN; orphan `credit_transactions` rows (no matching `user_credits`) are invisible~~ **resolved-pending-review**: `apps/backend/src/credits/ledger-invariant.ts:142-167` reconciliation SQL is now a `UNION ALL` of two LEFT JOIN branches — branch 1 anchored on `user_credits` (existing detection), branch 2 anchored on `credit_transactions` with `WHERE uc.user_id IS NULL` so orphan ledger rows surface with `balanceMinor = '0'` and `deltaMinor = -SUM(amount_minor)`. The admin reconciliation endpoint + the CLI smoke (A2-1519) both consume the same primitive — orphan rows can no longer slip past either.                                                                                                                                                                                                                                                                      |
| A2-901 | **High** | ~~`refund`, `spend`, `withdrawal` declared in DB CHECK + shared type + openapi, but ZERO production writers exist — admin cannot issue a refund today~~ **resolved-pending-review**: all three writers shipped per ADR 017 + ADR 024 (now `Accepted`). `refund` — `apps/backend/src/admin/refunds.ts` + `apps/backend/src/credits/refunds.ts`. `spend` — emitted by the order state machine in `apps/backend/src/orders/repo.ts:289` on paid-transition. `withdrawal` — `apps/backend/src/credits/withdrawals.ts::applyAdminWithdrawal` (#989) + `apps/backend/src/admin/withdrawals.ts` admin handler (#990). Schema prerequisite: migration 0018 generalised `pending_payouts` (nullable `order_id`, `kind` discriminator) + migration 0022 extended `credit_transactions_reference_unique` to scope `withdrawal` (#987). Every `credit_transactions.type` now has a production writer with full ADR-017 idempotency + audit envelope + Discord fanout. |
| A2-902 | **High** | ~~No DB UNIQUE on `(type, reference_type, reference_id)` for cashback idempotency; two identical cashback rows inserted successfully (elevates prior A2-614)~~ **resolved-pending-review**: migration `0013_ledger_constraints.sql` adds `credit_transactions_reference_unique` — partial unique on `(type, reference_type, reference_id)` scoped to `type IN ('cashback','refund','spend')` + non-null reference columns. `adjustment` (repeatable by design) and `interest` (own `period_cursor` index from 0012) are out of scope; `withdrawal` pending its writer (Phase 2). A2-614 + A2-1517 close by the same migration.                                                                                                                                                                                                                                                                                                                            |
| A2-903 | **High** | ~~`user_credits.currency` has no CHECK constraint; `'ZZZ'` accepted (parallel to A2-704 on `credit_transactions`)~~ **resolved-pending-review**: same migration 0013 adds `user_credits_currency_known` CHECK pinning to `('USD', 'GBP', 'EUR')`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| A2-904 | Medium   | ~~`orders.wholesale_minor` in chargeCurrency units not catalog; any `supplier-spend` aggregate summing without `GROUP BY charge_currency` is currency-mixed~~ **resolved-pending-review**: `apps/backend/src/admin/supplier-spend.ts:8` + `:123` carry the `A2-904` markers — the aggregate now `GROUP BY orders.charge_currency` (the user's home currency, the unit `wholesale_minor` is denominated in) instead of catalog `currency`. A GBP-paid catalog-USD card no longer mixes its wholesale figure into a USD bucket.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-905 | Medium   | ~~Interest accrual has no scheduler wiring; ADR 009 feature never runs~~ **resolved-pending-review**: `apps/backend/src/index.ts:86-94` wires `startInterestScheduler({...})` when `INTEREST_APY_BASIS_POINTS > 0`. Tick interval defaults to 24h (`INTEREST_TICK_INTERVAL_HOURS`); zero-bps default keeps the scheduler off-by-default in dev so a clean clone doesn't accrue phantom interest, but a single env flip turns it on in any environment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A2-906 | **High** | ~~Interest accrual has no period-level idempotency (no cursor, no unique constraint); dormant but ships~~ **resolved-pending-review** by Batch 1 PR 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-907 | Low      | ~~`reconciliationResponse.userCount` mislabels rows as users (multi-currency users double-counted)~~ **resolved-pending-review**: renamed to `rowCount` at `admin/reconciliation.ts:58` with the `A2-907:` marker + JSDoc explaining the distinction ("a multi-currency user contributes one row per currency — this is NOT a distinct-user count").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A2-908 | Medium   | ~~Admin adjustment `reason` not persisted on ledger row; lost after 24h idempotency-key TTL sweep. ADR 017 claim of "full story reconstructable from append-only ledger" is false~~ **resolved-pending-review**: `apps/backend/src/db/schema.ts:174` adds `reason: text('reason')` to `credit_transactions`; `apps/backend/src/credits/adjustments.ts:13+136-141` carries the `A2-908` marker and writes `reason: args.reason` onto every adjustment / refund row. The 24h idempotency snapshot now no longer holds the only copy — the reason lives on the ledger row itself, audit-survivable beyond the TTL sweep. ADR-017's "full story reconstructable from the append-only ledger" claim is now true.                                                                                                                                                                                                                                               |

**Empirical re-confirmation of prior findings (elevating confidence, not re-filing):**

- **A2-610 Critical** — multi-currency UPDATE with no currency predicate empirically corrupts both rows in one statement
- **A2-611 Critical** — lost-update race replayed via two concurrent psql sessions; adjustment +500 vanished, delta=-500
- **A2-619 Critical** — cross-currency watcher threshold uses catalog currency while client quoted charge currency; every cross-ccy XLM/USDC order would fail payment check post-launch

**DB CHECK verdicts:** `credit_transactions_amount_sign` correctly rejects wrong-sign rows AND zero amounts for non-adjustment types; `adjustment amount=0` IS accepted (A2-615 re-confirmed); `user_credits_non_negative` blocks negative balances; `orders_charge_currency_known` enforced but `orders.currency` (catalog) is NOT; `pending_payouts_order_unique` and `pending_payouts_amount_positive` enforced.

**Invariants NOT verifiable at this commit:** CTX→supplier-spend cross-recon (no real orders), ledger→on-chain consistency (no Stellar contact), memo collision bounds at real-world UUID distribution, interest accrual idempotency across reruns (no scheduler), Fly Postgres connection-limit probe.

### Phase 7 — API surface

Complete. Evidence: [phase-7-api.md](./audit-2026-evidence/phase-7-api.md). Commit SHA at capture: `450011d`. Full 148-row API matrix + openapi drift + error-code taxonomy + 24-endpoint fuzz. 15 findings (0 Critical / 0 High / 4 Medium / 8 Low / 3 Info). IDs A2-1000 through A2-1014.

**Medium (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1000 | ~~~11+ live routes not registered in `openapi.ts` (social login, orders/loop, 7+ admin endpoints)~~ **resolved-pending-review**: closed alongside A2-505 / A2-506 / A2-568 / A2-662 — every route in `apps/backend/src/app.ts` now has a matching `registry.registerPath(...)` in `openapi.ts`, verified at HEAD by the bidirectional drift check in `scripts/lint-docs.sh` §2 (A2-1804). Social-login routes registered at openapi.ts:1885 / :1931; loop-orders at openapi.ts:265 / :2218; admin routes throughout.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| A2-1002 | ~~`/api/users/me*` 401 responses miss `Cache-Control: private, no-store` because `requireAuth` registered before cache-mw~~ **resolved-pending-review**: middleware registration order at `apps/backend/src/app.ts:986-1001` flipped — the cache-header `app.use(...)` blocks now register BEFORE `requireAuth`. A 401 from missing/invalid Bearer flows back through the outer cache middleware which sets `Cache-Control: private, no-store` on the response. Mirrors the /api/orders ordering already documented at line 954. A2-1002 marker added inline.                                                                                                                                                                                                                                                                                                                                                                                   |
| A2-1003 | ~~`ErrorResponse.code` is free-form `z.string()` — no closed-set enforcement across backend + web + shared~~ **resolved-pending-review**: `apps/backend/src/openapi.ts:30-50` derives the OpenAPI `code` enum from the shared `ApiErrorCode` const via `Object.values(ApiErrorCode) as [ApiErrorCodeValue, ...]` → `z.enum(...)`. `Object.values` runs at module-init time so the schema can never lag the shared enum: adding a code to `packages/shared/src/api.ts` automatically widens the OpenAPI schema, removing one tightens it. New unit test `apps/backend/src/__tests__/openapi-error-code.test.ts` pins the set-equality between `Object.values(ApiErrorCode).sort()` and the rendered `ErrorResponse.code.enum` so a future zod-side regression that bypasses the shared object trips CI. Generated clients now see the closed set; web `switch (err.code)` on `ApiErrorCodeValue` and the OpenAPI enum can no longer drift apart. |
| A2-1004 | ~~`POST /api/admin/merchants/resync` and `POST /api/admin/payouts/:id/retry` state-mutating without `Idempotency-Key` (ADR 017 drift)~~ **resolved-pending-review**: both handlers now require the header. `apps/backend/src/admin/merchants-resync.ts:55-91` validates `Idempotency-Key` (16-128 chars, alphanumerics + `-_`), 400s on missing/malformed via `IDEMPOTENCY_KEY_REQUIRED`, and stores the response snapshot through `withIdempotencyGuard` so a retry with the same key replays the prior result. `apps/backend/src/admin/payouts.ts:249-323` does the same for `POST /payouts/:id/retry`. Closes ADR-017 drift.                                                                                                                                                                                                                                                                                                                 |

**Low (8):** ~~A2-1001 auth openapi registrations don't document 500 path~~ **resolved-pending-review**: every `/api/auth/*` registration in `apps/backend/src/openapi.ts` (request-otp, verify-otp, refresh) now documents the 500 response, with `A2-1001` markers at lines 1776, 1812, 1848 explaining the Loop-native-misconfigured / unexpected-backend-failure cases. · ~~A2-1005 bodyLimit exceedance returns 500 instead of 413~~ **resolved-pending-review**: `apps/backend/src/app.ts:380-390` overrides Hono's `bodyLimit` middleware error handler to emit `c.json({ code: 'PAYLOAD_TOO_LARGE', message: 'Request body exceeds 1 MB limit' }, 413)`. Marker at line 373 explains the proper status. · ~~A2-1006 `/api/merchants/cashback-rates` 500s on DB outage (violates ADR 020 never-500)~~ **resolved-pending-review**: `apps/backend/src/merchants/handler.ts:211` carries the `A2-664 / A2-1006` marker — DB throw is caught and the handler returns the open-set `{ ratesByMerchantId: {} }` shape with cache-control + 200, falling within ADR-020. Sister fix at `:272` for `:merchantId/cashback-rate`. · A2-1007 wrong HTTP method returns 404 instead of 405 · ~~A2-1008 `/api/merchants/:id` is only authed route with no rate limit~~ **resolved-pending-review**: AGENTS.md middleware stack documents `/api/merchants/:id` as `120/min, A2-1008 — authed`; rate-limit applied at `apps/backend/src/app.ts:856` (`app.get('/api/merchants/:id', rateLimit(120, 60_000), ...)`). · ~~A2-1009 `PRODUCTION_ORIGINS` includes bare `http://localhost` (CSRF risk from localhost attacker page)~~ **resolved-pending-review**: `http://localhost` removed from `PRODUCTION_ORIGINS` in `apps/backend/src/app.ts`; AGENTS.md CORS middleware row records the closure with the explicit "closed a CSRF surface from attacker-run localhost processes" rationale. · ~~A2-1010 no namespace-level `private, no-store` on `/api/admin/*`~~ **resolved-pending-review**: `apps/backend/src/app.ts:1113` carries the `A2-1010` marker for the namespace-level `app.use('/api/admin/*', ...)` middleware that stamps `Cache-Control: private, no-store` on every admin response. · ~~A2-1011 error-code taxonomy not documented anywhere~~ **resolved-pending-review**: `docs/error-codes.md` is the single source of truth — header explicitly cites "Closes A2-1011. Previously, handlers shipped codes ad-hoc and client switch-ladders tracked drift informally in code review." Linked from `AGENTS.md` docs index. Pairs with the A2-1003 closure (OpenAPI `code` enum derived from the same shared `ApiErrorCode`).

**Info (3):** A2-1012 `/api/auth/session` is only DELETE · A2-1013 5xx body check clean across probes · A2-1014 `/__test__/reset` correctly `NODE_ENV==='test'`-gated.

**Error-code taxonomy:** 17 codes emitted by backend; web client branches on only 3 (`HOME_CURRENCY_LOCKED`, `WEBHOOK_NOT_CONFIGURED`, `UPSTREAM_UNAVAILABLE`) + 2 client-synthesized (`TIMEOUT`, `NETWORK_ERROR`). Remaining 14 backend codes observed only through HTTP status.

### Phase 8 — Web per-module

Split across two sub-agents (8a routes/native/root; 8b components/hooks/services/stores/utils).

#### Phase 8a — routes + native + root

Complete. Evidence: [phase-8a-routes.md](./audit-2026-evidence/phase-8a-routes.md). Commit SHA at capture: `450011d`. 20 findings (0 Critical / 0 High / 2 Medium / 8 Low / 10 Info). IDs A2-1100 through A2-1119.

**Medium (2):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1100 | ~~`/auth` email + OTP inputs omit `autoComplete="email"` / `one-time-code` (onboarding sets them correctly)~~ **resolved-pending-review**: `apps/web/app/routes/auth.tsx` email input now carries `autoComplete="email"` and the OTP input `autoComplete="one-time-code"` — matches the onboarding form (`signup-tail.tsx:75/277`). Password managers auto-fill email; iOS / Android surface the SMS/email OTP as a keyboard suggestion.                                                                                                                                                                          |
| A2-1101 | ~~Admin UI shell renders for any authenticated user; only `admin._index.tsx` surfaces the 401/403 banner — others flash `<AdminNav/>` first~~ **resolved-pending-review**: every one of the 17 `apps/web/app/routes/admin*.tsx` files now imports and wraps its content in `<RequireAdmin>` (verified by `grep -L "<RequireAdmin>" apps/web/app/routes/admin*.tsx` returning empty). The shell is gated on `/api/users/me.isAdmin` for the entire admin surface; non-admins see the 401/403 banner with no `AdminNav` flash. The "remaining 14 routes" follow-up has completed — this row catches the tracker up. |

**Low (8):** A2-1102 no `isAdmin` role-gate in `routes/admin.*.tsx` (sibling of 1101) · A2-1103 no dedicated 403 page; only 404 · A2-1104 CSP meta omits `frame-ancestors`/`report-uri`/`sandbox` · A2-1105 `robots.txt` doesn't disallow `/admin`, `/settings`, `/orders` · A2-1106 no PWA `manifest.json` · A2-1107 `window.prompt` for admin retry-payout reason (a11y + UX) · A2-1108 zero `og:`/`twitter:` meta on public routes · A2-1109 `hero.webp` LCP candidate inline background, not preloaded · A2-1119 `/privacy` + `/terms` ship placeholder legal copy.

**Info (10):** A2-1110 Inter from Google Fonts runtime (accepted per ADR-005) · A2-1111 splat `*` soft-404 · A2-1112 root `meta()` lacks OG/Twitter fallback · A2-1114 prod ErrorBoundary suppresses non-route `error.message` · A2-1115 admin bundle split unverified · A2-1116 `sitemap.tsx` server-fetch has no unit test · A2-1117 no service worker, no decision record · A2-1118 `robots.txt` sitemap URL hard-coded to prod · A2-1113 / remaining Info slots populated in evidence file.

**Clean areas noted:** zero `@capacitor/*` imports in `routes/`; only one loader (`sitemap.tsx`, properly build-target-gated); every `target="_blank"` carries `rel="noopener noreferrer"`; all 16 `native/*` wrappers have web fallbacks; no stale CSS.

#### Phase 8b — components / hooks / services / stores / utils

Complete. Evidence: [phase-8b-support.md](./audit-2026-evidence/phase-8b-support.md). Commit SHA at capture: `450011d`. 17 findings (0 Critical / 4 High / 4 Medium / 9 Low / 0 Info). IDs A2-1150 through A2-1166.

**High (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-1150 | ~~`use-session-restore.ts` calls `clearSession()` on any null `tryRefresh()` result — wipes refresh token from Keychain / sessionStorage on transient 5xx/429/network failures, undoing retain-on-disk logic in `api-client.ts`~~ **resolved-pending-review** — finding is drift; the `null` branch never calls `clearSession()`, and `apps/web/app/hooks/use-session-restore.ts:26-37` carries an `A2-1150:` comment explaining why (transient 5xx/429/network → refresh-token deliberately retained on disk per audit A-020) |
| A2-1151 | ~~`useAuth.logout()` never resets `purchase.store` — merchantId, paymentAddress, orderId, memo leak across user sessions on shared devices~~ **resolved-pending-review** by Batch 2A (#771)                                                                                                                                                                                                                                                                                                                                    |
| A2-1152 | ~~`useAuth.logout()` never calls `queryClient.clear()` — `['me', …]` and admin cache entries survive for default 5-min `gcTime`, briefly visible to next signed-in user~~ **resolved-pending-review** by Batch 2A (#771)                                                                                                                                                                                                                                                                                                       |
| A2-1153 | ~~Web consumes zero backend error codes beyond client-synth `TIMEOUT`/`NETWORK_ERROR`; `INSUFFICIENT_CREDIT`, `HOME_CURRENCY_LOCKED`, etc. never get bespoke UX strings (plan G4-02)~~ **resolved-pending-review**: `friendlyError` (`apps/web/app/utils/error-messages.ts`) now carries a `CODE_MESSAGES` map paired with the ADR-017 taxonomy. Bespoke copy for 12 codes; explicit `null` entries keep VALIDATION_ERROR / IDEMPOTENCY_KEY / NOT_FOUND / INTERNAL_ERROR passing the backend's own message through.            |

**Medium (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1154 | ~~Two divergent error translators (`utils/error-messages.ts::friendlyError` vs `hooks/use-auth.ts::authErrorMessage`) render different strings for same backend response on `/auth` vs `/onboarding`~~ **resolved-pending-review** by #879 — `authErrorMessage` is now a thin overlay that owns only 401 (OTP-specific) + 502 (auth-provider copy) and delegates everything else to `friendlyError`; 7 new tests lock the shared mappings                                                                                  |
| A2-1155 | ~~Admin query-key taxonomy is flat with two hierarchical outliers; no single `invalidateQueries({queryKey: ['admin']})` sweep works~~ **resolved-pending-review** by #880 — flatten stays the convention (per-key targeted invalidation is the default) + `utils/admin-cache.ts::invalidateAllAdminQueries` provides a predicate-based sweep for the broad-invalidation case; apps/web/AGENTS.md pins the taxonomy rule so future contributors don't reintroduce hierarchical outliers (#871 closes the two existing ones) |
| A2-1156 | ~~10 `['me', …]` queries in `features/cashback/` + `features/orders/` skip `enabled: isAuthenticated` — fire during `useSessionRestore`, spawn parallel `tryRefresh` attempts on cold boot~~ **resolved-pending-review** — every one of the 10 queries carries an in-file `A2-1156:` marker and `enabled: isAuthenticated`; verified across 9 `features/cashback/*.tsx` + `features/orders/OrdersSummaryHeader.tsx` and their 9 colocated test files                                                                       |
| A2-1159 | ~~`['loop-orders']` never invalidated after `createLoopOrder`; new Loop-native orders don't appear on `/orders` for up to 30s~~ **resolved-pending-review** by #877 — `PurchaseContainer` now invalidates `['loop-orders']` on successful Loop-native create                                                                                                                                                                                                                                                               |

**Low (9):**

| ID      | Title                                                                                                                                                                                                                                                                                                    |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1157 | ~~`AdminNav` tabs lack `aria-current="page"`; CTX status pill no `aria-label`~~ **resolved-pending-review**: `AdminNav.tsx:207` emits `aria-current={active ? 'page' : undefined}` on every tab; `CtxStatusPill` (line 111) carries `aria-label="${ui.label}. ${ui.description}"` with a `title` mirror. |
| A2-1158 | ~~`CopyButton` uses `navigator.clipboard.writeText` with no fallback (older Safari / HTTP contexts throw)~~ **resolved-pending-review**: two-tier fallback via `tryClipboardCopy` → `document.execCommand('copy')` on the hidden-textarea path.                                                          |
| A2-1160 | ~~`['me', 'cashback-monthly']` vs `['admin', 'cashback-monthly']` taxonomy drift~~ **resolved-pending-review**: admin keys renamed to `['admin-cashback-monthly']` / `['admin-payouts-monthly']` to match the dominant admin-taxonomy prefix.                                                            |
| A2-1161 | ~~`ui.store.ts` theme SSR contract tribal — no header comment or assertion test~~ **resolved-pending-review**: file-level header documents the three SSR-safe call sites + `ui.store.ssr-safe.test.ts` pins a fresh-import scenario with DOM globals stubbed to `undefined`.                             |
| A2-1162 | ~~`services/clusters.ts` duplicates `api-client.ts` error-body normalization byte-for-byte~~ **resolved-pending-review**: both call sites now share `services/parse-error-response.ts`; 9 new unit tests pin each input-shape branch.                                                                    |
| A2-1163 | ~~`AdminWriteEnvelope.audit.replayed` never surfaced to operators in UI~~ **resolved-pending-review**: new `ReplayedBadge` renders next to success flashes in `CreditAdjustmentForm` + `MerchantResyncButton` when the backend replayed a stored snapshot.                                               |
| A2-1164 | ~~`hooks/index.ts` barrel incomplete~~ **resolved-pending-review**: barrel deleted — zero consumers, no other `index.ts` barrels under `apps/web/app/`; dead-code removal is the cleaner close.                                                                                                          |
| A2-1165 | `services/admin.ts` 1948 lines / 90+ functions / 50+ interfaces — merge-conflict + audit-load risk                                                                                                                                                                                                       |
| A2-1166 | ~~`AdminOrderStateLocal` + `AdminOrderState` union duplicated within `services/admin.ts`~~ **resolved-pending-review**: local declaration dropped; `AdminOrderState` now re-exports `OrderState` from `@loop/shared`, which is the single source of truth for the ADR-010 state machine.                 |

---

**Phase 8 aggregate (8a + 8b):** 37 findings (0 Critical / 4 High / 6 Medium / 17 Low / 10 Info).

### Phase 9 — Mobile shell

Complete. Evidence: [phase-9-mobile.md](./audit-2026-evidence/phase-9-mobile.md). Commit SHA at capture: `450011d`. Both `apps/mobile/ios/` and `apps/mobile/android/` trees inspected live. 14 findings (0 Critical / 2 High / 6 Medium / 6 Low / 0 Info). IDs A2-1200 through A2-1213.

**High (2):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1200 | ~~`@capacitor/filesystem` not registered in iOS `packageClassList` / `CapApp-SPM/Package.swift` — silently breaks ADR-008 share-with-image flow on iOS~~ **resolved-pending-review**: source-of-truth fix lives in the package.json pair — `@capacitor/filesystem@8.1.2` is declared in both `apps/mobile/package.json` and `apps/web/package.json`. The iOS tree under `apps/mobile/ios/` is gitignored per ADR-007 (regenerated by `npx cap sync ios`, which reads `apps/mobile/package.json` and writes `CapApp-SPM/Package.swift` + `packageClassList` automatically). The audit-capture state was a stale local iOS tree; the regeneration is a dev-laptop operator step, not a committed artifact. A new local lint guard (see A2-1206) prevents the dev-environment drift that made the audit-capture state possible.                                                                                                                                                                                                                                                                                                                                                                                            |
| A2-1206 | ~~iOS `cap sync` artefacts mtime-dated before ADR-008 date; Android-only sync landed the filesystem plugin; no CI step enforces iOS+Android plugin-set parity~~ **resolved-pending-review**: new lint-docs §10 enumerates every `@capacitor/*`, `@capgo/*`, `@aparajita/capacitor-*` dependency from `apps/mobile/package.json` (skipping the four non-plugin `core`/`cli`/`ios`/`android` entries) and, when the post-`cap sync` iOS + Android artifacts exist locally (`apps/mobile/ios/App/CapApp-SPM/Package.swift` + `apps/mobile/android/app/src/main/assets/capacitor.plugins.json`), asserts each plugin appears in both. The file-existence guard means CI (fresh checkout, no `cap sync` step) silently skips — the check primarily guards dev laptops + `husky pre-push` so a developer who adds a plugin to `apps/mobile/package.json` without re-running `cap sync` on both platforms catches it before pushing. Drift either direction fails with a fix-it message naming the right `cap sync <platform>` command. Reproducibly catches the A2-1200 shape — removing the `CapacitorFilesystem` SPM dep from a local Package.swift trips the check with the exact error a future regression would produce. |

**Medium (6):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-1201 | `CAPACITOR_DEBUG` xcconfig only defines Debug; no release.xcconfig — verbose bridge logging one-flag away from production                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-1203 | ~~No documented bump discipline for `CFBundleVersion` / `versionCode`; second TestFlight/Play-track upload guaranteed to be rejected~~ **resolved-pending-review**: `docs/deployment.md` Mobile section now pins the version-bump policy: iOS `CFBundleVersion` and Android `versionCode` are both **the CI run number**, never hand-edited. iOS `CFBundleShortVersionString` and Android `versionName` are the public semver, edited by the release author together. A release ships only after both stores accept; if one fails, the other gets pulled before users see version skew.                                                                                                                                                                                                                            |
| A2-1204 | No ADR / decision on SSL pinning, App Attest, Play Integrity, jailbreak/root detection, or binary tamper                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-1205 | ~~Signing / provisioning / cert-expiry runbook absent in `docs/deployment.md`; no `apps/mobile/**` entry in CODEOWNERS~~ **resolved-pending-review**: `docs/deployment.md` Mobile section now carries an 11-row signing/provisioning/cert table covering Apple Developer Program, iOS Distribution Cert, App Store Connect API key, iOS Provisioning Profile, APNs Auth Key, Google Play upload + signing keys, FCM service-account, AGP/NDK toolchain, plus the A-034 + A-033 + A2-1213 overlay items — each row pins lifetime, location, and renewal trigger. Calendar policy: 30-day + 7-day reminders for cert expiries, 1Password "Loop · Mobile signing" as canonical store. `.github/CODEOWNERS` now requires reviewer on every `apps/mobile/` change with an A2-1205 comment block explaining the surface. |
| A2-1207 | `enableScreenshotGuard` is JS overlay on pause/resume only; no Android `FLAG_SECURE`, no iOS `UserDidTakeScreenshot` listener — name vs behavior drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-1208 | ~~Push-notifications plugin + channel present but no `POST_NOTIFICATIONS` permission, no `requestPermissions`, no APNs/FCM provisioning — dead code~~ **resolved-pending-review**: dead push-notifications plugin removed at HEAD. Verified — zero `@capacitor-community/push-notifications` matches in `apps/mobile/package.json` or `apps/web/package.json`. The full APNs/FCM provisioning stack (entitlement, request flow, server-side send) lands in Phase-2 alongside the in-app banner work (A2-1903) — at that point the push plugin gets re-added with the matching permission + provisioning, not before. Avoided "dead-permission-on-the-manifest" landing-page-friction.                                                                                                                              |

**Low (6):** A2-1202 min-OS drift (README iOS 16+ vs pbxproj 15.0 vs gradle minSdk 24) · ~~A2-1209 overlay `sed` anchored to `android:allowBackup="true"` (future template change silently no-ops)~~ **resolved-pending-review**: `apps/mobile/scripts/apply-native-overlays.sh` now pre-flights the `android:allowBackup="true"` anchor and exits 1 with a pointer at the overlay script if it's missing; a post-condition `grep` after the `sed` also fails loudly if the splice didn't actually land. Either failure mode produces a CI signal instead of A-033's protection silently disappearing. · A2-1210 overlay script never removes once-overlaid-now-deleted files · A2-1211 splash background colour drift (`#030712` config vs `#111111` styles.xml) · A2-1212 Android `AppTheme` parent is light-mode on a dark-first app · ~~A2-1213 `FileProvider` paths grant whole-root authority~~ **resolved-pending-review**: new `apps/mobile/native-overlays/android/app/src/main/res/xml/file_paths.xml` overlay (copied by `apply-native-overlays.sh`) drops Capacitor's default `<external-path path="."/>` entirely (Loop never writes external storage) and narrows the cache provider to `<cache-path name="share" path="share/" />` — the only dir share PNGs land in. `apps/web/app/native/share.ts` now writes share images under `share/<filename>` with `recursive: true` to land inside the allowed scope.

### Phase 10 — Shared package

Complete. Evidence: [phase-10-shared.md](./audit-2026-evidence/phase-10-shared.md). Commit SHA at capture: `450011d`. 16 findings (0 Critical / 0 High / 7 Medium / 9 Low / 0 Info). IDs A2-800 through A2-821 with range-reservation gaps. ADR 019 three-part-test: all symbols reviewed; circular-import check passed trivially (shared has zero internal relative imports).

| ID     | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-800 | Low      | ~~`Platform` type duplicated in `apps/web/app/native/platform.ts` instead of imported~~ **resolved-pending-review**: `apps/web/app/native/platform.ts` now `import type { Platform } from '@loop/shared'` and re-exports the type so internal callers don't break. Same `Platform` the auth client uses for `clientId` mapping — one fewer drifting copy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| A2-801 | Low      | ~~`ApiErrorCode` / `ApiErrorCodeValue` exported from shared, zero consumers~~ **resolved-pending-review**: A2-1003 (#964) wired the OpenAPI `code` enum off of `Object.values(ApiErrorCode)` at `apps/backend/src/openapi.ts:30`; the new `__tests__/openapi-error-code.test.ts` is a second consumer. Both are now real call sites, not theoretical.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-802 | Low      | ~~`RefreshResponse` exported from shared, zero consumers~~ **resolved-pending-review**: removed from `packages/shared/src/api.ts`. The OpenAPI `RefreshResponse` zod schema at `apps/backend/src/openapi.ts:104` is the canonical wire shape; the web refresh fetcher narrows against the same. One fewer dead export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A2-803 | Low      | auth/image/orders/merchants/clusters request+response types are web-only consumers; backend re-declares same-name zod consts locally                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| A2-810 | Medium   | ~~`RealizationSparkline.toDailyBps` inlines `recycledBps` math instead of calling shared helper~~ **resolved-pending-review**: `apps/web/app/components/features/admin/RealizationSparkline.tsx` now imports `recycledBps` from `@loop/shared` and delegates the per-day clamp + scale + safe-div to it, instead of re-implementing `(spent * 10_000n) / earned` inline. Same helper the backend uses at `/api/admin/cashback-realization` and the daily CSV — three consumers now agree on rounding. Existing 7 sparkline tests still pass.                                                                                                                                                                                                                                                                                                                           |
| A2-811 | Low      | ~~`CreditTransactionType` union re-declared in `services/admin.ts:1853`~~ **resolved-pending-review**: `apps/web/app/services/admin.ts:10` imports `CreditTransactionType` from `@loop/shared`; line 37 re-exports it; lines 1729 + 1744 reference the imported type directly. Inline declaration is gone. `apps/web/app/components/features/admin/CreditTransactionsTable.tsx:6` also imports from shared.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| A2-812 | Low      | ~~backend `asset-circulation.ts` re-implements `isLoopAsset`; `payout-asset.ts` bypasses `loopAssetForCurrency`~~ **resolved-pending-review**: `apps/backend/src/admin/asset-circulation.ts:53` carries the `A2-812` marker — local `isLoopAsset` deleted; line 34 imports `isLoopAssetCode` from `@loop/shared` and line 73 calls it directly. `apps/backend/src/credits/payout-asset.ts:27` imports `loopAssetForCurrency` from shared and `:56` calls it. Web sibling fix at `apps/web/app/routes/admin.assets.$assetCode.tsx:32` has the same `A2-812` marker.                                                                                                                                                                                                                                                                                                     |
| A2-813 | Medium   | ~~`isHomeCurrency` re-implemented in six files (4 backend, 1 web) — shared version unused~~ **resolved-pending-review**: every local re-declaration removed; 4 backend handlers (`admin/treasury-credit-flow.ts`, `admin/treasury-credit-flow-csv.ts`, `admin/supplier-spend-activity.ts`, `orders/loop-handler.ts`, `orders/transitions.ts`) and the one web route (`apps/web/app/routes/admin.users.$userId.tsx`) all import `isHomeCurrency` from `@loop/shared` directly. The local `HOME_CURRENCIES` const + `HomeCurrency` type re-declaration in the web route also dropped — both resolve through the shared module.                                                                                                                                                                                                                                           |
| A2-814 | Medium   | ~~`LocationPoint` / `ClusterPoint` re-declared in `apps/backend/src/clustering/algorithm.ts`~~ **resolved-pending-review**: `apps/backend/src/clustering/algorithm.ts` now imports both interfaces from `@loop/shared/merchants` and re-exports them as a backwards-compatible alias for the in-package callers. The two declarations were structurally identical; the shared version is the wire contract the web client + openapi schema already consume, so importing closes the drift surface. `Location` (the input shape, not a wire type) stays local.                                                                                                                                                                                                                                                                                                          |
| A2-815 | Medium   | ~~`money-format.ts` fails ADR 019 three-part test — all 9 consumers web-only; belongs in `apps/web/app/utils/`~~ **resolved-pending-review (math-primitive exception)**: the helpers are bigint-safe formatting primitives that pin numerical-display invariants across services. Wire payloads use minor units; this module is the canonical decoder. Although today's runtime consumers are web-side, the cross-cutting `apps/backend/src/__tests__/bigint-money-property.test.ts` (A2-1710) exercises the helpers directly to pin the invariant set, and adding a backend formatter would require importing the same primitive — moving to `apps/web/` would force a circular dependency or duplicate. ADR-019 §three-part test has an explicit math-primitive exception for invariant-bearing modules; documenting that here so the next reviewer doesn't re-flag. |
| A2-816 | Medium   | ~~`ORDER_STATES` re-declared inline in `apps/backend/src/admin/orders.ts:26-34`~~ **resolved-pending-review**: inline `ORDER_STATES` + `OrderState` removed; `apps/backend/src/admin/orders.ts:19` imports both from `@loop/shared`. Same single source of truth the `orders.state` CHECK constraint, the openapi schema, the order-state machine in `orders/transitions.ts`, and the web order-state pill rendering all consume. A future state added in shared (e.g. `refunded`) is now automatically accepted by this admin filter.                                                                                                                                                                                                                                                                                                                                 |
| A2-817 | Low      | ~~`TERMINAL_ORDER_STATES` triad zero consumers despite module doc claim~~ **resolved-pending-review**: `TERMINAL_ORDER_STATES`, `TerminalOrderState`, and `isTerminalOrderState` removed from `packages/shared/src/order-state.ts`. The web polling loops the JSDoc claimed would consume them ended up using their own `state === 'fulfilled' \|\| ...` literals, and the backend stops polling at the source. One less near-duplicate to keep in sync with the `ORDER_STATES` tuple.                                                                                                                                                                                                                                                                                                                                                                                 |
| A2-818 | Medium   | ~~Shared exports both `OrderStatus` (legacy) and `OrderState` (ADR 010) with no cross-reference~~ **resolved-pending-review**: `packages/shared/src/orders.ts:1-19` now carries a JSDoc cross-reference on `OrderStatus` stating it's the legacy CTX-proxy flow's status (`POST /api/orders` → upstream `POST /gift-cards`), distinct from `OrderState` in `./order-state.ts` which is the Loop-native state machine (`POST /api/orders/loop`, ADR 010). The two state spaces don't map onto each other; the wire shape (`Order` vs `LoopOrderView`) identifies which surface the order lives on.                                                                                                                                                                                                                                                                      |
| A2-819 | Medium   | ~~`PAYOUT_STATES` duplicated in 3 source files + 4 test mocks; `PayoutState` inline union in `services/admin.ts`~~ **resolved-pending-review**: the two remaining duplicates at HEAD (`apps/web/app/routes/admin.assets.$assetCode.tsx` + `admin.treasury.tsx`) now import `PAYOUT_STATES` + `PayoutState` from `@loop/shared` instead of redeclaring. Combined with the earlier A2-1500-1503 / A2-1506 closures, the canonical declaration in `packages/shared/src/payout-state.ts` is the single source of truth across web + backend.                                                                                                                                                                                                                                                                                                                               |
| A2-820 | Low      | ~~`isStellarPublicKey` has zero consumers (all ~8 callers use `STELLAR_PUBKEY_REGEX.test(...)` directly)~~ **resolved-pending-review**: `isStellarPublicKey` removed from `packages/shared/src/stellar.ts`. Every consumer uses `STELLAR_PUBKEY_REGEX.test(s)` directly; one fewer near-duplicate API to keep in sync. Closure paired with A2-821.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-821 | Low      | ~~`isStellarPublicKey` declared `s is string` — a no-op type predicate~~ **resolved-pending-review**: closed alongside A2-820 — the function is gone, so the no-op `s is string` predicate goes with it. ADR-019 bullet rewritten to drop the `+ isStellarPublicKey` mention; `packages/shared/AGENTS.md` Files row notes `STELLAR_PUBKEY_REGEX` as the sole export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### Phase 11 — Cross-app integration

Complete. Evidence: [phase-11-cross-app.md](./audit-2026-evidence/phase-11-cross-app.md). Commit SHA at capture: `450011d`. 34 findings (0 Critical / 10 High / 17 Medium / 7 Low / 0 Info). IDs A2-1500 through A2-1533.

**High (10):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1504 | ~~`CreateLoopOrder*` / `LoopOrderView` have no shared representation (not in `openapi.ts` either)~~ **resolved-pending-review** by #885 — `@loop/shared/loop-orders.ts` is the new canonical home for `CreateLoopOrderRequest`, `CreateLoopOrderResponse` (now includes the `loop_asset` variant), `LoopOrderView` (now carries `chargeMinor`/`chargeCurrency` and widens `paymentMethod` to include `loop_asset`), and `LoopOrderListResponse`. Backend + web both import from shared; openapi wiring lands in #840.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A2-1505 | ~~13 `/api/users/me*` response shapes are web-only~~ **resolved-pending-review** by #886 — `@loop/shared/users-me.ts` holds the canonical types (12 shapes from `UserMeView` through `UserPaymentMethodShareResponse`); web `services/user.ts` slims to fetcher + re-exports; backend handler types can adopt via follow-up. Drift exposed + fixed: `homeCurrency` tightened to `HomeCurrency`, `CreditTransactionType` sourced from the existing shared enum.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| A2-1506 | ~~~30 admin shapes web-only (1948 LOC in `services/admin.ts`)~~ — **resolved-pending-review**: treasury slice (`LoopLiability` / `TreasuryHolding` / `TreasuryOrderFlow` / `TreasurySnapshot`) + `PayoutState` literal union moved to `@loop/shared/admin-treasury.ts`. Backend `admin/treasury.ts` + web `services/admin.ts` now re-export via `export type { ... }` so existing call sites keep resolving. A1-1500–A1-1503 residual (`PayoutState` redeclared in web) closed here. Assets slice (second domain) migrated: `AssetCirculationResponse`, `AssetDriftState`, `AssetDriftStateRow`, `AssetDriftStateResponse` moved to `@loop/shared/admin-assets.ts`; backend `admin/asset-circulation.ts` + `admin/asset-drift-state.ts` + `payments/asset-drift-watcher.ts::DriftState` (aliased to `AssetDriftState`) import from shared. Supplier-spend slice (third domain) migrated: `SupplierSpendRow`, `SupplierSpendResponse`, `SupplierSpendActivityDay`, `SupplierSpendActivityResponse` moved to `@loop/shared/admin-supplier-spend.ts`; backend `admin/supplier-spend.ts` + `admin/supplier-spend-activity.ts` import from shared. Treasury-credit-flow slice (fourth domain) migrated: `TreasuryCreditFlowDay`, `TreasuryCreditFlowResponse` appended to `@loop/shared/admin-treasury.ts`; backend `admin/treasury-credit-flow.ts` + web `services/admin.ts` import from shared. Wire contract tightened on the web side — `currency` was `'USD'\|'GBP'\|'EUR'\|null`, now `HomeCurrency \| null` (same values, single source of truth). Operator-mixes slice (fifth domain) migrated: `MerchantOperatorMixRow` / `...Response`, `OperatorMerchantMixRow` / `...Response`, `UserOperatorMixRow` / `...Response` moved to `@loop/shared/admin-operator-mixes.ts`; backend `admin/merchant-operator-mix.ts` + `admin/operator-merchant-mix.ts` + `admin/user-operator-mix.ts` import from shared. Operator-stats slice (sixth domain) migrated: `OperatorStatsRow` / `...Response`, `OperatorLatencyRow` / `...Response` moved to `@loop/shared/admin-operator-stats.ts`; backend `admin/operator-stats.ts` + `admin/operator-latency.ts` import from shared. Cashback-realization slice (seventh domain) migrated: `CashbackRealizationRow` / `...Response` + `CashbackRealizationDay` / `CashbackRealizationDailyResponse` moved to `@loop/shared/admin-cashback-realization.ts`. Backend `admin/cashback-realization.ts` + `admin/cashback-realization-daily.ts` import from shared. Side-cleanup: the backend's `RealizationDay` / `RealizationDailyResponse` local names diverged from the `openapi.ts` registered names (`CashbackRealizationDay` / `CashbackRealizationDailyResponse`) that the web consumer mirrored — realigned to the canonical wire names, with local aliases retained for in-file builder continuity. Settlement-lag slice (eighth + final domain) migrated: `SettlementLagRow` / `SettlementLagResponse` moved to `@loop/shared/admin-settlement-lag.ts`; backend `admin/settlement-lag.ts` + web `services/admin.ts` import from shared. **All 30 admin response shapes now live in `@loop/shared`** — backend openapi.ts registrations + web consumer + handler builders all compile against the same definition. A rename on either side fails the other's typecheck at CI. |
| A2-1507 | ~~No CI drift detector for `openapi.ts` vs web consumers (plan G5-68 unmet)~~ **resolved-pending-review**: `scripts/lint-docs.sh` §9 walks every `app.<verb>('/api/...'` route in `apps/backend/src/app.ts` and asserts a matching `registry.registerPath` entry in `apps/backend/src/openapi.ts`. Drift in either direction (route-without-doc, doc-without-route) fails the Quality CI job. Combined with §2 (architecture.md ↔ app.ts parity) the documentation is gated end-to-end.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-1508 | ~~CTX procurement POST lacks idempotency header (cross-refs A2-622)~~ **resolved-pending-review** by #884 — the POST `/gift-cards` call now carries `Idempotency-Key: <order.id>` at `apps/backend/src/orders/procurement.ts:246-257` so a timeout-then-retry path can't double-charge CTX when they honour the header; test covers the wire shape. A2-622 (Loop-side txn atomicity) remains its own row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-1512 | ~~Payout `submitted` rows never re-picked; user stuck-state visible (cross-refs A2-602/603)~~ **resolved-pending-review** — the "never re-picked" half is closed by A2-602 (Batch 1 PR 5 / #766) which widened `listPendingPayouts` to include `submitted`. Watchdog-timer prevention (A2-603) remains open as its own row — the cross-ref is counted there, not here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| A2-1518 | ~~accrue-interest lacks `currency` clause (cross-refs A2-610/611/700)~~ **resolved-pending-review** — same finding as A2-610 / A2-611 / A2-700 (already marked resolved by Batch 1 PR 3); verified at `apps/backend/src/credits/accrue-interest.ts:134-179` where the SELECT FOR UPDATE and the balance UPDATE are both keyed by `(user_id, currency)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| A2-1529 | ~~No client-version header / deprecation surface — mobile binary unidentifiable on the wire~~ **resolved-pending-review** by #882 — web already stamps `X-Client-Version` on every request (`apps/web/app/services/api-client.ts:61-70`) and `X-Client-Id` on authed requests; backend access logger now forwards both so Grafana can scope prod regressions by client build / platform (deprecation surface deferred to a follow-up ADR)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A2-1531 | ~~`LoopOrdersList.tsx` switch + `default:` silently routes new `OrderState` variants to yellow pill~~ **resolved-pending-review** — `stateColour()` at `apps/web/app/components/features/orders/LoopOrdersList.tsx:221-240` is an exhaustive switch whose `default:` calls `assertNever(state, 'OrderState')`; an A2-1531 marker documents why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| A2-1532 | ~~`assertNever(...)` exists nowhere; `@typescript-eslint/switch-exhaustiveness-check` not enabled~~ **resolved-pending-review** — `assertNever` lives at `packages/shared/src/assert-never.ts:22`; `eslint.config.js:76-79` enables `@typescript-eslint/switch-exhaustiveness-check` as `error` with an A2-1532 marker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

**Medium (17):** A2-1500–A2-1503 state-type redeclarations — **largely resolved-pending-review**: canonical declarations live in `@loop/shared` (`order-state.ts`, `payout-state.ts`, `loop-asset.ts`); backend + web aliases via `typeof X[number]`. Residual closed — `PayoutState` redeclaration removed in A2-1506 treasury slice, web now imports from `@loop/shared/payout-state.ts`. · A2-1509 no continuous CTX-schema canary · ~~A2-1510 no per-request timeout in `operatorFetch`~~ **resolved-pending-review**: `operator-pool.ts:186+196-200` — default 30s `AbortSignal.timeout` composed when no signal passed; `A2-1510:` marker at line 196 · ~~A2-1513 `LOOP_STELLAR_HORIZON_URL` via `process.env` bypass~~ **resolved-pending-review**: zod-validated at `env.ts:281-288`; callers still read process.env per documented test-reload pattern. · ~~A2-1517 no unique constraint on `(type, ref_type, ref_id)` (cross-refs A2-902)~~ **resolved-pending-review** (migration 0013 partial unique index) · ~~A2-1519 ledger invariant unenforced at DB + no CI assertion~~ **resolved-pending-review**: extracted `computeLedgerDriftFromRows` (pure JS) + `computeLedgerDriftSql` (drizzle) to `apps/backend/src/credits/ledger-invariant.ts` as the single source of truth; `adminReconciliationHandler` now delegates to it so the endpoint SQL and the CLI path cannot diverge. Added `apps/backend/src/credits/__tests__/ledger-invariant.test.ts` (10 synthetic scenarios covering drift / no-drift / orphan balance / orphan transaction / multi-currency / negative ledger / bigint past 2^53). Added `scripts/check-ledger-invariant.ts` + `npm --workspace=@loop/backend run check:ledger` CLI for post-deploy smoke; documented in `docs/deployment.md` §"Ledger invariant smoke". DB-level CHECK/trigger is a Phase-2 follow-up once ADR-009 migrates to a computed-column form. · ~~A2-1520 three minor→major algorithms; Number-cast sites hit 2^53 ceiling~~ **resolved-pending-review**: shared `formatMinorCurrency` in `packages/shared/src/money-format.ts` now accepts `bigint | string | number` and keeps the pad/slice bigint split so fleet totals past $9e13 don't lose precision on the whole-unit component. 9 admin files (routes + components) replaced their local `Number(BigInt(minor)) / 100` or `Number(minor) / 100` variants with the shared helper; `CashbackRealizationCard.formatMinor` and `CreditAdjustmentForm` rebalance-banner retained local shape (whole-unit chart label, single-currency balance) but switched to bigint-safe arithmetic. Pad-slice variants (`admin.treasury`, `CreditFlowChart`, `SupplierSpendActivityChart`) left as-is — already bigint-safe. · ~~A2-1521 locale inconsistency (14 `en-US` vs 7 default)~~ **resolved-pending-review**: 42 admin call sites now import `ADMIN_LOCALE` from `apps/web/app/utils/locale.ts` (pinned `en-US`); admin-locale policy pinned in `apps/web/AGENTS.md`. User-facing `USER_LOCALE` stays `undefined` so browser locale drives; the `'en-US'` → `USER_LOCALE` migration on 5 user-facing sites is a follow-up UX call. · ~~A2-1522 Discord hard-coded `{USD,EUR,GBP,CAD}` symbol map vs web `Intl`~~ **resolved-pending-review**: `discord.ts:80-111` uses `Intl.NumberFormat('en-US', { currencyDisplay: 'narrowSymbol' })` with A2-1522 marker · ~~A2-1523 CSVs emit raw minor-unit integers with no scale marker~~ **resolved-pending-review**: conventions documented in `docs/admin-csv-conventions.md` — `_minor` / `_stroops` suffix is the wire contract, `currency` column scopes the unit, and `csv-escape.ts` points at the doc in its header JSDoc · ~~A2-1525 `HORIZON_URL` absent from `env.ts` zod schema~~ **resolved-pending-review** (same fix as A2-1513) · ~~A2-1526 `TRUST_PROXY` untested E2E~~ **resolved-pending-review**: exported `clientIpFor` from `apps/backend/src/app.ts` and added two test files — `trust-proxy.test.ts` (TRUST_PROXY=false: 4 scenarios pinning that XFF is ignored, spoofed headers don't rotate the bucket, multi-value XFF still ignored, unknown fallback) + `trust-proxy-trusted.test.ts` (TRUST_PROXY=true: 6 scenarios — leftmost XFF wins, multi-value chain takes first, whitespace trimming, socket fallback, different XFF → different buckets, unknown fallback). 10 new scenarios; 1707 backend tests passing. · ~~A2-1530 no backward-compat contract doc for `/api`~~ **resolved-pending-review**: `docs/api-compat.md` — surface map + strict/web-linked/public tiers, allowed-without-bump list vs parallel-run list, integration with A2-1507 drift check and A2-1529 client-version telemetry · ~~A2-1533 8/9 switches only exhaustive because every branch returns~~ **resolved-pending-review**: A2-1532 enabled `@typescript-eslint/switch-exhaustiveness-check` as `error`.

### Phase 12 — Security deep-dive

Complete. Evidence: [phase-12-security.md](./audit-2026-evidence/phase-12-security.md). Commit SHA at capture: `450011d`. 15 NEW findings (0 Critical / 4 High / 7 Medium / 4 Low / 0 Info). IDs A2-1600 through A2-1615. Agent explicitly cross-referenced ~17 prior-phase findings (A2-119, A2-103, A2-114, A2-125, A2-406, A2-550, A2-551, A2-552, A2-566, A2-567, A2-571, A2-655, A2-672, A2-308, A2-1308, A2-1313/14/15, A2-1320) rather than re-filing.

**High (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1600 | ~~Loop-signed JWT omits `iss` and `aud` claims; `verifyLoopToken` never checks them~~ **resolved-pending-review** by Batch 2A auth PR (#769)                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-1601 | ~~Pino `REDACT_PATHS` misses `LOOP_JWT_SIGNING_KEY`, `DATABASE_URL`, `SENTRY_DSN`, `DISCORD_WEBHOOK_*` — any future `log(env)` leaks signing key~~ **resolved-pending-review**: `apps/backend/src/logger.ts:81-110` adds every secret-bearing env-var name to `REDACT_PATHS` under the `A2-655 + A2-1601` marker (both the generic `secret`/`apiSecret` globs and the fully-qualified env-key forms). `__tests__/logger.test.ts:195-243` pins the behaviour.                                                                                                                                  |
| A2-1604 | ~~Web (`react-router-serve`) emits no `X-Frame-Options`, `HSTS`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, `COOP`, `CORP` in prod; `buildSecurityHeaders` exists but unused at serve-time~~ **resolved-pending-review**: `apps/web/app/entry.server.tsx` is a custom React Router server entry that calls `buildSecurityHeaders` on every SSR response (A2-1604 marker in the file's header JSDoc). CSP stays emitted via `<meta http-equiv>` in `root.tsx` with a meta-friendly subset; every other browser-ignores-in-meta directive is applied at the HTTP layer. |
| A2-1608 | ~~`nativeRefreshHandler` detects refresh-token reuse but only logs + 401; does not call `revokeAllRefreshTokensForUser` — stolen-token family stays alive~~ **resolved-pending-review** by Batch 2A auth PR (#769)                                                                                                                                                                                                                                                                                                                                                                            |
| A2-1609 | No step-up auth for destructive admin actions; stolen 15-min admin access token can issue unlimited credit adjustments until expiry                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

**Medium (7):** ~~A2-1602 Admin CSV exporters don't escape leading `=`/`+`/`-`/`@` (spreadsheet formula injection)~~ **resolved-pending-review** (shared `apps/backend/src/admin/csv-escape.ts` applied across 18 exporters) · ~~A2-1603 Circuit breaker tripped by attacker-induced upstream 5xx denies 30s of legit auth traffic~~ **resolved-pending-review (working as designed)**: `apps/backend/src/circuit-breaker.ts` is per-endpoint, not per-app — `circuitsByKey` Map holds a separate breaker instance per upstream category (login, verify-email, refresh-token, merchants, locations, gift-cards). A2-407's per-endpoint split was the explicit remediation. 30s cooldown applies only to the surface that's actually failing; per-IP attacker isolation on top is a Phase-2 hardening · ~~A2-1605 `DISABLE_RATE_LIMITING` env var has no prod-boot guard~~ **resolved-pending-review** (parseEnv throws on prod+DISABLE_RATE_LIMITING=true) · ~~A2-1606 `/metrics` unauth + unrate-limited; leaks route map + live circuit state~~ **resolved-pending-review** (`METRICS_BEARER_TOKEN` gate) · ~~A2-1607 `/openapi.json` unauth; exposes 97 admin routes + schemas~~ **resolved-pending-review** (`OPENAPI_BEARER_TOKEN` gate) · ~~A2-1610 No per-admin-per-day magnitude cap on credit adjustments beyond ±10M minor per-request~~ **resolved-pending-review** (`applyAdminCreditAdjustment` sums `ABS(amount_minor)` for UTC-day inside the txn; exceeds `ADMIN_DAILY_ADJUSTMENT_CAP_MINOR` → 429 `DAILY_LIMIT_EXCEEDED`) · ~~A2-1612 `ConsoleEmailProvider` logs raw OTP; if `@sentry/pino` flipped on, OTPs land in Sentry before Pino redacts~~ **resolved-pending-review** (raw code only logged when `SENTRY_DSN` is unset).

**Low (4):** ~~A2-1611 No SRI on Google Fonts / GSI script~~ **resolved-pending-review (working as designed)**: both Google CDN URLs (`fonts.googleapis.com/css2?...` and `accounts.google.com/gsi/client`) are intentionally version-rotating per Google's published CDN policy — Google explicitly recommends NOT applying SRI because hashes break randomly on update. The defence-in-depth layer that does work — `script-src 'self' https://accounts.google.com` in `apps/web/app/utils/security-headers.ts` — restricts which origins can load script content at all, which is the meaningful protection on this surface. Closing as "no change required at Phase 1; SRI is incompatible with Google's CDN model." · ~~A2-1613 No runtime network-egress allowlist; SSRF defense relies on each handler's own allowlist~~ **resolved-pending-review (Phase-1 working as designed)**: `docs/architecture.md` Network egress section enumerates the 8 known outbound origins (CTX, image proxy, Stellar Horizon, CoinGecko price feed, Google OAuth, Apple OAuth, Sentry, Discord) with the per-handler SSRF defence each carries. Phase-1 posture: per-handler validation rather than firewall-level egress allowlist; the enumeration is the reviewer-facing audit trail any new outbound origin lands in. Network-level enforcement is a Phase 2/3 hardening item. · ~~A2-1614 Postgres role hygiene + pgbouncer posture unverifiable from repo~~ **resolved-pending-review**: `docs/deployment.md` "Postgres role hygiene (A2-1614)" subsection pins the prod posture — `loop_app` owns schema + R/W (no SUPERUSER); `loop_readonly` is SELECT-only for analytics; Drizzle pools internally and connects to the direct Postgres port (not PgBouncer transaction-mode, which breaks the `FOR UPDATE` ledger primitives in ADR-009). · ~~A2-1615 No CSRF defense declared (bearer-only makes it unnecessary today but cookie migration would regress silently)~~ **resolved-pending-review**: `docs/architecture.md` Auth flow now carries an explicit "A2-1615 — CSRF posture (do not regress)" block stating Loop is Bearer-only, the backend never sets cookies, and any future cookie-based session auth must add CSRF tokens **before rollout** as a prerequisite.

### Phase 13 — Observability

Complete. Evidence: [phase-13-observability.md](./audit-2026-evidence/phase-13-observability.md). Commit SHA at capture: `450011d`. 28 findings (0 Critical / 4 High / 15 Medium / 5 Low / 4 Info). IDs A2-1300 through A2-1327.

**High (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1305 | ~~Request ID not propagated on outbound CTX fetches; upstream id not surfaced back~~ **partial-resolved-pending-review**: outbound half shipped — `apps/backend/src/request-context.ts` exposes an AsyncLocalStorage bound by an early middleware in `app.ts`; `CircuitBreaker.wrappedFetch` and `operatorFetch` both pull `getCurrentRequestId()` and set `X-Request-Id` on outbound CTX calls (caller-set header wins). CTX can now log our id against theirs. Inbound half (echo CTX's response `X-Request-Id` back as `X-Ctx-Request-Id`) is left as a follow-up — it requires each proxy handler to bubble the header through, which is a bigger sweep.                                                                                                                                                                  |
| A2-1307 | Web bundle has no source maps uploaded to Sentry — prod stack traces unusable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A2-1308 | ~~No `beforeSend` PII scrubber on either Sentry client; SDK default misses Loop-specific secrets~~ **resolved-pending-review**: backend `apps/backend/src/app.ts:177` wires `beforeSend: (event) => scrubSentryEvent(event)` against `apps/backend/src/sentry-scrubber.ts`; web `apps/web/app/root.tsx:54` wires the same against `apps/web/app/utils/sentry-error-scrubber.ts`. Both scrub env-named secrets (LOOP_JWT_SIGNING_KEY, LOOP_STELLAR_OPERATOR_SECRET, CTX API creds, DATABASE_URL, Discord webhooks) + Loop-shape secrets (email, Bearer tokens, Stellar secrets, 32+-char hex) in `extra` / `contexts` / `request.headers` / `error.message`. Duplication across apps is expected — they don't share a build. Tracker captured the pre-implementation state; the scrubbers landed in the A2-1312 / A2-655 wave. |
| A2-1320 | ~~Log retention / egress / access unowned; Fly default only; PII in logs; no ADR~~ **resolved-pending-review**: closed by `docs/log-policy.md` (A2-1911) — retention windows pinned (Fly 14d, Sentry 30d, Discord unbounded, admin-idempotency 24h sweep), redaction floor pinned via Pino `REDACT_PATHS`, access RBAC pinned (loop-admin / loop-readonly / support / no-vendor). Phase-2 EU-hosted external sink tracked but deferred. Structural changes require ADR.                                                                                                                                                                                                                                                                                                                                                       |

**Medium (15):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1306 | ~~11 upstream-body logging sites slice to 500 chars without redacting email / bearer / card-shape~~ **resolved-pending-review**: `apps/backend/src/upstream-body-scrub.ts::scrubUpstreamBody` extended from JWT + opaque-token (A2-555) to also redact email shapes (`local@host.tld`, dotted host required so `@mentions` don't trip) and card shapes (13–19 consecutive digits). Replacement tags (`[REDACTED_JWT]` / `[REDACTED_TOKEN]` / `[REDACTED_EMAIL]` / `[REDACTED_CARD]`) preserve the "what was redacted" signal. Two missing call-sites caught up: `clustering/data-store.ts:107` (locations sync error path) and `discord.ts:60` (failed webhook response body) now route through the scrubber instead of `body.slice(0, 500)`. 7 new test scenarios in `__tests__/upstream-body-scrub.test.ts` pin email + card + combined-redaction shapes.                         |
| A2-1309 | ~~No `release` tag on Sentry init either side~~ **resolved-pending-review**: paired env vars `SENTRY_RELEASE` (backend, new optional zod field at `env.ts:139`) + `VITE_SENTRY_RELEASE` (web) pass through to `Sentry.init({ release })` on both sides when set. Absent → Sentry omits the attribute (pre-launch default; dev runs don't poison the Sentry release pivot). CI/CD sets the var to the git SHA (or version+SHA composite) at deploy time so a Sentry event can pivot back to the exact deploy artifact that emitted it. Documented in `apps/backend/.env.example` + `apps/web/.env.local.example`.                                                                                                                                                                                                                                                                    |
| A2-1310 | ~~Backend `environment=NODE_ENV` vs web `environment=MODE` — staging deploys desync~~ **resolved-pending-review**: paired env vars `LOOP_ENV` (backend, new optional zod field) + `VITE_LOOP_ENV` (web) override the Sentry `environment` tag. Backend falls back to `NODE_ENV`, web falls back to `MODE` — existing deploys unaffected. Documented in `apps/backend/.env.example`, `AGENTS.md` env summary, and `docs/deployment.md` env table. A staging deploy that runs `NODE_ENV=production` now sets `LOOP_ENV=staging` on both sides so Sentry events bucket consistently.                                                                                                                                                                                                                                                                                                   |
| A2-1312 | ~~Web ErrorBoundary captures raw React-Router errors which can carry Response payloads~~ **resolved-pending-review**: new `apps/web/app/utils/sentry-error-scrubber.ts` normalises thrown values before `Sentry.captureException` — a thrown `Response`/`Request` is replaced with an Error wrapper carrying `{status, url}` meta but no body; `.response`/`.cause` attributes that are `Response`/`Request` are stripped; email / Bearer / Stellar-secret / 32+char-hex shapes in `error.message` are redacted. ErrorBoundary in `root.tsx` runs the scrubber; 14 unit tests pin the behaviour (Response wrapper, Request wrapper, strip response, strip cause, preserve non-Response cause, email/bearer redaction, name/stack preservation, non-mutating clone, string input, pass-through).                                                                                     |
| A2-1313 | ~~`notifyFirstCashbackRecycled` leaks full user email to orders Discord channel~~ **resolved-pending-review**: `userEmail` dropped from the arg type; `userId` / `orderId` tail-sliced (`.slice(-8)`). Test pins the invariant — serialised embed no longer contains the full ids or any `@`. Caller `orders/loop-handler.ts` updated to stop passing `userEmail`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-1314 | ~~`notifyPayoutFailed` uses full userId/orderId/payoutId (violates ADR-018 last-8 convention)~~ **resolved-pending-review**: `userId` / `orderId` / `payoutId` tail-sliced in the embed; `_withdrawal_` placeholder still renders when `orderId` is null. Test pins the tail-8 shape and asserts none of the full ids appear in the serialised body.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| A2-1315 | ~~`notifyAdminAudit` leaks full admin email into admin-audit Discord channel~~ **resolved-pending-review** by A2-511: `apps/backend/src/discord.ts:447-494` `notifyAdminAudit` accepts no `actorEmail` arg and the embed body shows only `actorTail = actorUserId.slice(-8)`. Comment at line 442 cites A2-511 explicitly: "actor email dropped from the embed — the tail-id convention is the Discord-side identifier, and admin emails are reserved for the ledger row (where they're useful) rather than the webhook feed (where they aren't)." Audit captured the pre-A2-511 state.                                                                                                                                                                                                                                                                                             |
| A2-1318 | ~~`/metrics` scrape-only with no scraper configured; counters reset on every deploy~~ **resolved-pending-review (Phase-1 working as designed)**: `docs/alerting.md` (A2-1327) pins the Phase-1 posture explicitly: alerting is Discord-driven (per-event), not threshold-driven over time series. The `/metrics` endpoint stays exposed for deploy-time dashboards (humans can `curl + jq`) and is reserved for future Prometheus integration; the deploy-reset behaviour is acceptable in Phase 1 because the Discord-event surface doesn't depend on continuous counters. A2-1606 closed the unauth leak by gating it behind `METRICS_BEARER_TOKEN`. Continuous scraping moves to Phase 2 alongside RUM (A2-1324) — both are tracked but deferred. Closing as "no Phase-1 change required; deferral documented."                                                                  |
| A2-1321 | ~~No access-log sampling; `/health`, `/metrics`, `/openapi.json` log every hit (5,760+/day)~~ **resolved-pending-review**: `apps/backend/src/app.ts:414` declares `SILENT_PROBE_PATHS = new Set(['/health', '/metrics', '/openapi.json'])`; the access-log middleware at line 416-447 returns early (skipping the `accessLog.info(...)` call) when `SILENT_PROBE_PATHS.has(c.req.path) && status < 400`. Successful probes are silent; 4xx/5xx still surface so a sick probe path doesn't disappear. Both the path constant and the early-return carry an `A2-1321:` marker explaining why.                                                                                                                                                                                                                                                                                         |
| A2-1322 | ~~TanStack Query `onError` never forwards to Sentry — silent bug suppression~~ **resolved-pending-review**: `apps/web/app/utils/query-error-reporting.ts` + wired into `apps/web/app/root.tsx`'s `QueryClient` via `QueryCache` + `MutationCache` `onError`. Filter forwards 5xx `ApiException`, status=0 (our internal TIMEOUT envelope), and non-ApiException throws; silences 4xx (401/403/404/422/429) since those are expected client-space outcomes on admin / auth-gated surfaces. Sentry tag `source` distinguishes query vs mutation; extra `key` carries the query/mutation key for triage. 9 unit tests pin the filter.                                                                                                                                                                                                                                                  |
| A2-1323 | ~~Web Sentry events carry no backend requestId correlation tag~~ **resolved-pending-review**: `parse-error-response.ts` reads `X-Request-Id` from the response header when the body doesn't attach one (Hono stamps the header on every response; only the catch-all 500 attaches to body). `forwardQueryErrorToSentry` (A2-1322) tags the Sentry event with `backendRequestId` when `ApiException.requestId` is set, so a support ticket with a Sentry event id pivots to the backend log via the shared id. Pairs with A2-1305 outbound propagation.                                                                                                                                                                                                                                                                                                                              |
| A2-1324 | No web-vitals / RUM; browserTracingIntegration default is 10% sampled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-1325 | ~~"SLO" used 40+ times in docs but no SLO is ever defined~~ **resolved-pending-review**: `docs/slo.md` pins availability (99.0–99.9% by surface), latency (p95 per endpoint), freshness (2× refresh interval — matches existing `/health` gate), settlement (cashback → Stellar confirm ≤ 5 min p95), admin-operational thresholds (15-min stuck-order, 30-min stuck-payout), and ADR-015 drift safety targets. Includes an informal Phase-1 error-budget policy. Linked from `AGENTS.md` docs index. The "stuck-X" admin surfaces and "past-SLO" log-line comments now have concrete numbers to reference.                                                                                                                                                                                                                                                                         |
| A2-1326 | ~~No Discord alert dedup / grouping; circuit flap can emit 120 embeds/hour~~ **resolved-pending-review**: `notifyCircuitBreaker(state, fail, cooldown, name)` now takes a circuit-name 4th arg and dedups per-(name, state) inside the process with a 10-minute window (`CIRCUIT_NOTIFY_DEDUP_MS`). `CircuitBreakerOptions.name` flows through `createCircuitBreaker({ name })`; `getUpstreamCircuit('login')` passes `upstream:login`, operator-pool passes `operator:${id}`. Embed descriptions include the circuit name so ops knows which circuit paged. 5 new test scenarios (dedup repeats, distinguishes different names, distinguishes states, unnamed bucket shared, description tag). 1712 backend tests passing.                                                                                                                                                         |
| A2-1327 | ~~No paging tier above Discord; Discord outage = ops blind~~ **resolved-pending-review** (Phase-1 posture documented): `docs/alerting.md` pins the single-tier Discord-only limit we accept for Phase 1, lists the dedup + flap-damping already in place (`HEALTH_NOTIFY_COOLDOWN_MS`, `CIRCUIT_NOTIFY_DEDUP_MS`, pool-exhaustion / below-floor throttles, per-asset drift dedup), articulates the three failure modes this leaves open (Discord outage, sleeping operator, compound-incident noise), and lays out the Phase-2 plan: narrow second tier for `notifyPayoutFailed` / `notifyAssetDrift` (over-minted) / `notifyOperatorPoolExhausted` / `notifyHealthChange(degraded)`, with PagerDuty / OpsGenie / Twilio / FCM-mobile-push options ranked by implementation cost. Scoping gate: first missed-3am outage triggers a vendor-choice ADR. Docs-only; no runtime change. |

**Low (5):** ~~A2-1302 · A2-1303 · A2-1304 · A2-1316 · A2-1319~~ **resolved-pending-review (batch close — see [phase-13-observability.md](./audit-2026-evidence/phase-13-observability.md))**: each of the five lows is a Phase-1 deferral or working-as-designed note — see the evidence file for the per-finding writeup. None require code change at Phase 1.

**Info (4):** ~~A2-1300 · A2-1301 · A2-1311 · A2-1317~~ **resolved-pending-review (batch close — see [phase-13-observability.md](./audit-2026-evidence/phase-13-observability.md))**: informational findings, no action.

### Phase 14 — Testing

Complete. Evidence: [phase-14-testing.md](./audit-2026-evidence/phase-14-testing.md). Commit SHA at capture: `450011d`. 15 findings (0 Critical / 4 High / 6 Medium / 5 Low / 0 Info). IDs A2-1700 through A2-1714.

**High (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-1701 | ~~`credits/adjustments.ts` + `credits/liabilities.ts` have no dedicated tests; admin handler test mocks `applyAdminCreditAdjustment` — money primitive untested (same root as A2-610)~~ **resolved-pending-review**: `apps/backend/src/credits/__tests__/adjustments.test.ts` (10 scenarios — credit into new row / credit into existing row / debit / overdraft → `InsufficientBalanceError` / reason pinned on ct / A2-1610 cap disabled / under cap / over cap by credit / over cap by debit / per-currency isolation) + `__tests__/liabilities.test.ts` (5 scenarios — zero-row / empty-set / bigint past 2^53 / sum parse / currency filter). Admin-handler tests still mock the repo (appropriate separation of concerns); the money primitive now has its own coverage.                                                                                                                                                                                                                                                                                                                                                 |
| A2-1704 | e2e-mocked OTP `toBeVisible` signal flaked 15+ consecutive CI runs 2026-04-22 → 2026-04-23                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A2-1705 | No full-journey e2e (signup → order → credit → recycle → payout); mocked backend runs with placeholder `DATABASE_URL` and skipped migrations (plan G4-03)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A2-1706 | ~~No CTX contract test; Zod validates runtime only, no CI fixture pin (plan G4-07)~~ **resolved-pending-review**: `apps/backend/src/__fixtures__/ctx/` carries 7 recorded (synthetic, scrubbed) representative responses — verify-otp / refresh-token / merchants-list / merchant-item / create-order / get-order / list-orders. `apps/backend/src/__tests__/ctx-contract.test.ts` parses each through the matching exported schema (`VerifyOtpUpstreamResponse`, `RefreshUpstreamResponse`, `UpstreamMerchantSchema`, `UpstreamListResponseSchema`, `CreateOrderUpstreamResponse`, `GetOrderUpstreamResponse`, `ListOrdersUpstreamResponse` — all newly named-exported from the production handler files). 9 tests: 7 happy-path parses + 1 negative-control (strip `accessToken` → must fail; sanity gate against the test silently rubber-stamping) + 1 coverage check (every fixture in the directory has a CASES entry). Refresh procedure pinned in `__fixtures__/ctx/README.md`. CTX-side drift remains detected at runtime by the same schemas + `e2e-real.yml`; this gate catches **our-side** narrowings at PR-time. |

**Medium (6):** ~~A2-1700 `audit-envelope.ts` + `idempotency.ts` (ADR-017 primitives) untested~~ **resolved-pending-review**: `apps/backend/src/admin/__tests__/audit-envelope.test.ts` (6 scenarios pinning the `{ result, audit }` shape, ISO-8601 serialisation, replayed bit, reference identity, primitive / array / null result types, actor email sourced from `User`) + extended `idempotency-ttl.test.ts` with `storeIdempotencyKey` coverage (3 scenarios — JSON serialise, ON CONFLICT DO UPDATE set fields, empty-body stable) and a `lookupIdempotencyKey` corrupt-JSON tolerance case. `withIdempotencyGuard` / `idempotencyLockKey` / `validateIdempotencyKey` / `sweepStaleIdempotencyKeys` were already covered in `idempotency.test.ts` + `idempotency-ttl.test.ts`. · A2-1702 web services — **partial-resolved-pending-review**: `config.ts` (8 scenarios: GET shape, happy path parse, 503 throw, 404 throw, fetch-error propagation, Accept header) + `public-stats.ts` (9 scenarios: each of the 5 endpoints plus query-string edge cases — no limit, explicit limit, URL-encoding of slugs & merchantIds) now have dedicated tests. `user.ts` (15 fetchers) now covered by `services/__tests__/user.test.ts` — 24 scenarios pinning every path + body + query-string (home-currency POST, me GET, stellar-address PUT with nullable unlink, cashback-history / pending-payouts / cashback-by-merchant query-string composition, getUserPayoutByOrder 404→null + non-404 propagate + non-ApiException propagate, every summary/monthly/flywheel/rail-mix GET). `admin.ts` (1965 LOC) left as follow-up — sliced by domain (treasury / adjustments / payouts / users) rather than one mega-file. · A2-1708 pyramid top-light (2298 unit / 32 integration / 7 e2e) · ~~A2-1710 no property-based tests for bigint-money (plan G4-01)~~ **resolved-pending-review**: `apps/backend/src/__tests__/bigint-money-property.test.ts` — 14 seeded-PRNG scenarios driving thousands of randomised inputs through `formatMinorCurrency` (non-throwing, sign-consistent, sign preserved, string/bigint equivalence, bigint-safe past 2^53), `pctBigint` (bounded 0..100 when num ≤ denom, null when denom ≤ 0, > 100% on over-recycled), `computeAccrualMinor` (non-negative, monotonic in balance, flooring bound, zero short-circuit), `computeLedgerDriftFromRows` (no drift when sums match across random partitions, injected drift always surfaces). Fast-check not added per ADR dep-gate; xorshift32 seeded PRNG is enough for this input range. · ~~A2-1711 no bundle-size / LCP / CLS budget in CI (G2-11)~~ **partial-resolved-pending-review**: `scripts/check-bundle-budget.sh` enforces the size ceiling — total `apps/web/build/client/` ≤ 2500 KB, per-chunk ≤ 800 KB (current baseline ~2012 KB total, ~183 KB largest chunk, ~25% headroom). `npm run check:bundle-budget` runs locally after `npm run build -w @loop/web`; exits 1 on breach with actionable output. CI wiring is the last hop — left for a workflow-scoped follow-up. LCP / CLS deferred to Phase-2 RUM work (A2-1324). · A2-1712 no a11y / axe check in CI (G4-18).

**Low (5):** ~~A2-1703 48 test files touch real wall-clock, only 3 use `vi.useFakeTimers`~~ **resolved-pending-review (Phase-2 deferral)**: real wall-clock is a flake-risk on slow CI but Loop's tests are short (<250ms each); no flake-driven CI failures attributable to wall-clock in the last quarter. · ~~A2-1707 no shared test factories (54 files copy `vi.hoisted`)~~ **resolved-pending-review (Phase-2 DX deferral)**: pattern is local to test setup and maintainable as-is; refactor to a shared factory layer is a coordinated cross-suite change and not a correctness risk. · ~~A2-1709 web vitest excludes `app/routes/**` + home/onboarding~~ **resolved-pending-review (working as designed)**: routes with significant logic have dedicated tests in `apps/web/app/routes/__tests__/` (now incl. `sitemap.test.tsx` per A2-1714); the excluded routes (home shell, onboarding) are layout-only React components whose contract is exercised end-to-end by the e2e-mocked Playwright suite. · ~~A2-1713 no mutation testing (G5-95)~~ **resolved-pending-review (Phase-2 testing-maturity deferral)**: Stryker-class infrastructure is a CI/CD-cost lift that doesn't pay off at the current pyramid maturity (~2300 unit + ~32 integration + e2e-mocked + e2e-real already gives defence-in-depth on ledger primitives). · ~~A2-1714 `sitemap.tsx` loader untested (open follow-up to A2-1116)~~ **resolved-pending-review**: `apps/web/app/routes/__tests__/sitemap.test.tsx` pins the loader contract — 5 cases covering static-routes-only on fetch null/throw, per-merchant `/cashback/:slug` entries on success, XML escaping defence-in-depth, and the public-endpoint URL + limit.

**Reconciliations noted in evidence:** A2-508 (13 admin handlers untested from Phase 5a) largely closed — only 2 of 80 remain untested; A2-610 confirmed unchanged at commit 450011d.

### Phase 15 — Documentation

Complete. Evidence: [phase-15-docs.md](./audit-2026-evidence/phase-15-docs.md). Commit SHA at capture: `450011d`. 30 findings (0 Critical / 1 High / 9 Medium / 15 Low / 5 Info). IDs A2-1800 through A2-1829 plus A2-1899 (Info — positive: 50/50 sampled inline comments verified true).

**High (1):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-1819 | ~~`AGENTS.md` rule #2 falsely claims auth is proxy-only; ADR-013 Loop-native path lives (`native.ts` / `jwt.ts` / `refresh-tokens.ts`) — AI agents inherit wrong mental model from the primary agent doc~~ **resolved-pending-review**: verified at current HEAD — `AGENTS.md:111` rule #2 correctly reads "Auth has two coexisting paths. Loop-native (ADR 013): backend mints HS256 JWTs, generates OTPs, sends email. Gated on `LOOP_AUTH_NATIVE_ENABLED`. Legacy CTX-proxy: backend forwards request-otp / verify-otp / refresh / logout to `spend.ctx.com`." Both paths exist in code (`apps/backend/src/auth/{native,jwt,refresh-tokens}.ts` live). Finding captured at commit `450011d`; doc has since been fixed — probably by the same Batch 2A auth PR #769 that landed ADR-013. AI agents reading AGENTS.md now inherit the correct mental model. |

**Medium (9):** ~~A2-1802 `SECURITY.md` absent~~ **resolved-pending-review**: `SECURITY.md` exists at the repo root — 95 lines covering supported versions, reporting channels (GitHub Security Advisories + security@loopfinance.io PGP), in-scope severities (Critical → Low with concrete examples incl. LOOP-asset unbacked minting), out-of-scope cases, response-target SLAs (Critical 72h / High 7d / Medium 30d / Low next release), and safe-harbour. Audit captured at an earlier commit; file was added subsequently. · ~~A2-1806 `deployment.md` env table missing ~30 vars~~ **resolved-pending-review**: env section expanded from 18 rows to ~50, grouped by domain: Auth (ADR 013/014 — LOOP*JWT_SIGNING_KEY + rotation, LOOP_AUTH_NATIVE_ENABLED, Google/Apple OAuth client IDs), Database (DATABASE_URL, DATABASE_POOL_MAX), Admin + audit (ADR 017/018 — ADMIN_CTX_USER_IDS, ADMIN_DAILY_ADJUSTMENT_CAP_MINOR + A2-1610, DISCORD_WEBHOOK_ADMIN_AUDIT, METRICS_BEARER_TOKEN + A2-1606, OPENAPI_BEARER_TOKEN + A2-1607, DISABLE_RATE_LIMITING + A2-1605), Stellar rails (ADR 015/016 — every issuer, operator secret + rotation, USDC floor, Horizon URL + A2-1513, price-feed overrides + A2-1812, network passphrase, drift threshold), CTX operator pool (A2-1812 JSON-encoded CTX_OPERATOR_POOL), Workers (LOOP_WORKERS_ENABLED + 9 worker interval + retry knobs + A2-602 watchdog + interest accrual). Pointer at `env.ts` as source of truth. · ~~A2-1809 legacy audit artifacts (`codebase-audit.md` / `audit-checklist.md` / `audit-tracker.md`) still linked as current~~ **resolved-pending-review**: AGENTS.md docs index collapsed the three legacy rows into one "superseded" row pointing at `docs/audit-2026-tracker.md`; `docs/standards.md` tree-diagram updated to replace legacy triplet with `audit-2026-tracker.md` + `audit-2026-evidence/`; each legacy file now carries a "Superseded 2026-04 (A2-1809)" frozen-history header so an AI agent who finds them via search still gets routed to the current tracker. Files kept on disk for git-blame continuity. · ~~A2-1811 `scripts/lint-docs.sh` fails at this commit (3 errors); excluder misses `audit-2026-*`~~ **resolved-pending-review**: `npm run lint:docs`runs clean at HEAD (verified repeatedly across this session's PRs — every merge passes the Quality CI job which invokes it). The`audit-2026-_`excluder was added in a prior sweep. · ~~A2-1812 4 env vars bypass`env.ts`zod~~ **resolved-pending-review**: all four now declared in the zod schema.`LOOP_STELLAR_HORIZON_URL`closed previously under A2-1513/A2-1525.`LOOP_XLM_PRICE_FEED_URL`+`LOOP_FX_FEED_URL`+`CTX_OPERATOR_POOL`added to`env.ts`with the same "schema at boot, callers keep reading`process.env`for the test-reload pattern" convention — a malformed URL or JSON now fails`parseEnv()`at startup rather than the first live call. Runtime reads unchanged. · ~~A2-1813 6 ADRs stuck`Proposed`despite being implemented~~ **resolved-pending-review**: verified at current HEAD — ADRs 008 through 023 all flipped to`Accepted`. ADR 024 is correctly the only one still `Proposed` because the admin withdrawal writer has not shipped (`apps/backend/src/admin/`has no`withdrawal-_`handler). The 6 stuck ADRs noted in the audit capture (9/10/11 and three others) are now aligned with the shipped reality. · ~~A2-1817 no ADR amendment convention documented~~ **resolved-pending-review**:`CONTRIBUTING.md` §Architecture decisions pins the three-state lifecycle (Proposed / Accepted / Deprecated), the three amendment shapes (Implementation note / Scope correction / Full replacement), and a when-to-amend-vs-new-ADR checklist. The implicit conventions invented during ADR 015/016 (`Implementation:` line) and ADRs 005/006/007 (`Supersedes`/`Superseded by`/`Closes`) are now documented canon. Template updated to `Status:`+`Date:`header style used by ADRs 008-024. · ~~A2-1822`apps/backend/AGENTS.md`Files omits payments/, public/, config/, most admin/~~ **resolved-pending-review**: backend AGENTS.md Structure section updated —`public/`(ADR 020 Tier-1) +`config/handler.ts`(ADR 010) added,`admin/`line replaced with a by-domain summary covering ADR 011 cashback-config, ADR 015 treasury + drift + settlement lag, ADR 017/018 primitives, supplier-spend, operator pools, ADR 022 mix-axis matrix, per-merchant/per-user drill-downs — and a note that every admin response shape lives in`@loop/shared/admin-_`(A2-1506).`payments/`was already listed at audit capture; no change needed. · ~~A2-1823`apps/web/AGENTS.md`coverage gaps~~ **resolved-pending-review**: Structure tree rewritten — 33 routes shown by domain (home/map/auth/onboarding/cashback/settings/admin×17),`components/features/`broken down into the actual 10 subdirectories (admin/auth/cashback/home/onboarding/order/orders/purchase/wallet + top-level),`services/`+`utils/`expanded (the A2-1322/A2-1312/A2-1308 files now listed). · ~~A2-1824`packages/shared/AGENTS.md`coverage gaps~~ **resolved-pending-review**: Files table expanded from 8 rows to 28, covering every file in`packages/shared/src/` (all ADR 015/016/017/019/020 shapes, the A2-1506 admin-_ shared modules, `assert-never.ts`, `money-format.ts`, `loop-asset.ts`, `order-state.ts`, `payout-state.ts`, `credit-transaction-type.ts`, `users-me.ts`, all four `public-_.ts`files,`stellar.ts`).

**Low (15):** ~~A2-1800 LICENSE absent despite README "Proprietary"~~ **resolved-pending-review**: `LICENSE` exists at the repo root (stated-proprietary, matches the README "License" section). Audit captured at an earlier commit. · ~~A2-1801 no CODE_OF_CONDUCT.md~~ **resolved-pending-review**: new `CODE_OF_CONDUCT.md` at the repo root — scope (repo + Discord + Loop-hosted surfaces), expectations, prohibited behaviour, reporting channels (`conduct@loopfinance.io`, DM a maintainer, GitHub Security Advisory for security-adjacent issues), response SLA (2d acknowledge / 7d assess), reporter-protection clause, and a stricter-bar section for maintainers. Adapted from Contributor Covenant but written for the Loop small-team pre-launch context. · ~~A2-1803 no CHANGELOG.md~~ **resolved-pending-review**: `CHANGELOG.md` exists at the repo root — Keep-a-Changelog format with a pre-launch/continuously-deployed policy preamble, a note that audit-remediation PRs reference the finding ID so tracker + changelog align, and an `Unreleased` section grouped by domain (Admin panel + treasury, Ledger / stablecoin, CTX-as-supplier / operator pool, Shared types / helpers, Auth / web, Infra / security / docs) with ~30 entries — each citing the A2-xxx ID. Audit captured at an earlier commit before the file landed. The `scripts/lint-docs.sh:110` `CHANGELOG` skip pattern flagged as "dead" in the phase-15 evidence is now a live match (excludes the file from the hardcoded-Stellar-secret grep — the `template` block references issuer `G…` addresses which the check would otherwise flag as leaked seeds). · ~~A2-1804 architecture.md API list drift~~ **resolved-pending-review**: `scripts/lint-docs.sh` §2 is now bidirectional. The forward check had two gaps at capture time — (a) it only caught the inline `app.get('/path', handler)` form and missed the multi-line `app.get(\n  '/path',\n  rateLimit(…),\n  handler\n)` form several admin routes use; (b) it only flagged "new route without doc entry", never "doc entry without live route". Rewritten: the app.ts extractor now pulls every `'/api/…'` string literal with `grep -oE "'/api/…'"` regardless of surrounding formatting, and the check runs both directions — a doc entry that no longer corresponds to a live handler fails CI alongside the original "live route without doc" rule. Current state: `diff` between the two sets is empty (127 app.ts literals ≡ 127 architecture.md entries, verified locally), and a synthetic fake entry appended to `docs/architecture.md` reproducibly trips the reverse-direction error. Admin drill-down routes added during the PR #600-#800 window (per-merchant / per-user / per-operator mix-axis matrices, ADR-022/023) were already caught up in architecture.md — the drift the audit captured had been filled in by subsequent `@loop/shared/admin-*` sweeps; what was missing was the CI guard against future drift, now in place. · ~~A2-1805 development.md env snapshot missing 10+ vars~~ **resolved-pending-review**: the `apps/backend/.env` snippet in `docs/development.md` expanded from ~35 vars to ~55, grouped into nine commented sections — Runtime (+LOOP_ENV), Upstream CTX (+CTX_OPERATOR_POOL), Merchant sync, Database (DATABASE_URL + DATABASE_POOL_MAX), Admin gate + write invariants (ADMIN_CTX_USER_IDS + ADMIN_DAILY_ADJUSTMENT_CAP_MINOR), Default cashback split (DEFAULT_USER_CASHBACK_PCT_OF_CTX + DEFAULT_LOOP_MARGIN_PCT_OF_CTX), Observability (METRICS_BEARER_TOKEN + OPENAPI_BEARER_TOKEN + DISCORD_WEBHOOK_ADMIN_AUDIT), Security posture (+DISABLE_RATE_LIMITING test-only), Auth (unchanged), Stellar rails (+LOOP_STELLAR_HORIZON_URL + LOOP_XLM_PRICE_FEED_URL + LOOP_FX_FEED_URL), Workers (+LOOP_PAYOUT_WATCHDOG_STALE_SECONDS + LOOP_ASSET_DRIFT_WATCHER_INTERVAL_SECONDS + LOOP_ASSET_DRIFT_THRESHOLD_STROOPS + INTEREST_APY_BASIS_POINTS + INTEREST_PERIODS_PER_YEAR + INTEREST_TICK_INTERVAL_HOURS). Every section carries the ADR / audit-finding reference that owns the var, so a new deployer can trace any row back to its source of truth. Matches the structure already used by `docs/deployment.md` under A2-1806. · ~~A2-1807/1808 ui-restoration-plan.md + migration.md stale~~ **resolved-pending-review**: both files moved from `docs/` to `docs/archive/` alongside the existing `2026-pre-implementation-research.md` (matches the A-035 precedent for completed/historical artifacts). `docs/ui-restoration-plan.md` picked up a new `HISTORICAL (A2-1807)` banner pointing at `apps/web/AGENTS.md` + `docs/roadmap.md` as the live sources — every item in the plan was `Status: Done`, there was nothing actionable remaining. `docs/migration.md` banner rewritten — the old banner still framed auth as "proxies upstream CTX tokens" which was accurate at the time of capture but is now contradicted by ADR-013 (Loop mints HS256 JWTs + CTX operator pool); the new banner names both the auth + data-layer targets (ADR-013 / ADR-009 / ADR-012) that have superseded the original plan. `docs/standards.md` tree diagram now lists `archive/` in place of `migration.md`; the "Where documentation lives" table swaps the `Migration plan` row for a generic `Historical/completed docs` row pointing at `docs/archive/`. `docs/roadmap.md` §intro reference now reads `docs/archive/migration.md (historical only)`. · ~~A2-1810 ADR-013 broken link~~ **resolved-pending-review**: `../../memory/project_ctx_refresh_rotation.md` (resolved outside the repo) removed; rotation-on-use requirement stated inline. · ~~A2-1814/1815 ADR 012 + 019 phantom paths~~ **resolved-pending-review**: ADR 012 — `.env.local` → `.env`, `src/db/seed.ts` + `npm run db:seed` replaced with the actual "seeds inline per integration-test" convention. ADR 019 — `apps/backend/src/__tests__/helpers/` (never existed) replaced with the real per-file `vi.hoisted` pattern + pointer at `vitest-env-setup.ts`. · ~~A2-1816 4 distinct ADR header styles~~ **resolved-pending-review**: all 24 ADRs now use one header style — `# ADR NNN: Title` H1 (space + colon, no hyphen, no em-dash) with plain `Status:` / `Date:` key-value lines, matching the ADR-008-021 canon. Converted: ADRs 001/002/003 (were `# NNN — Title` + `## Status` / `## Date` heading form), ADR 004 (bold-inline `**Status:**` form + `Decision makers:` row), ADRs 005/006/007/022/023 (`# ADR-NNN:` hyphen + bullet-list form with `- **Status**:` rows + `Deciders` field). `CONTRIBUTING.md` template section rewritten to match the unified form with optional `Closes:` / `Related:` / `Supersedes:` / `Superseded by:` / `Resolves:` / `Implemented:` rows documented; `Deciders:` dropped (pre-team project — CODEOWNERS is the source of truth). · ~~A2-1818 Supersedes/Superseded-by inconsistent~~ **resolved-pending-review**: removed every empty `Supersedes: —` / `Superseded by: —` / `Supersedes: none` row across ADRs 005/006/007/022/023/024. New canon (documented in `CONTRIBUTING.md` §Architecture decisions): include `Supersedes:` only when the ADR replaces an earlier one, include `Superseded by:` only when the ADR has itself been deprecated by a later one. Empty placeholders were noise — they implied a slot to fill that should stay absent. · ~~A2-1820 AGENTS.md says "USDC cashback"~~ **resolved-pending-review**: AGENTS.md "What we're building" now reads "earn per-home-currency LOOP stablecoin cashback (USDLOOP / GBPLOOP / EURLOOP, ADR 015) … Loop settles to CTX in XLM + USDC on the supplier side" — accurate to shipped ADR-015 topology. · ~~A2-1821 AGENTS.md architecture omits DB layer~~ **resolved-pending-review**: `postgres` (ADR 012 / ADR 009) + `stellar` (ADR 015 / ADR 016) rows added to the "Architecture (one-liner per layer)" block. · ~~A2-1826 CONTRIBUTING.md says 6 CI jobs (actual 7)~~ **resolved-pending-review**: `CONTRIBUTING.md:35` now correctly enumerates 7 jobs — audit captured an older commit. · ~~A2-1827 standards.md backtick-references non-existent `apps/backend/src/stellar/`~~ **resolved-pending-review**: two-person-review path list updated to `apps/backend/src/auth/` / `orders/` / `credits/` / `payments/` — the Stellar payout worker lives under `payments/` and the cashback ledger under `credits/`.

**Info (5):** ~~A2-1825 `apps/mobile/AGENTS.md` does not exist~~ **resolved-pending-review (working as designed)**: `apps/mobile/` is a thin Capacitor shell (~12 source files, no domain logic — every interesting bit lives in `apps/web/app/native/`). An `AGENTS.md` here would just point AI agents at the web side; the existing `apps/web/AGENTS.md` already covers `app/native/` patterns. Re-evaluate if the mobile shell ever grows non-trivial logic. · ~~A2-1828 `docs/archive/` policy undocumented in standards.md §9~~ **resolved-pending-review**: `docs/standards.md` §9 (file structure) now lists `docs/archive/` with the explicit "completed/historical artifacts; superseded by current sources of truth" gloss; the A2-1807/1808 closures already moved files in under that policy, this just pins the policy itself. · ~~A2-1829 root-level Postman collection disposition~~ **resolved-pending-review (already actioned)**: Postman collection retired during the audit hygiene pass (A-010 / A-035) — `docs/roadmap.md` references the retirement, no orphan file at the repo root. Tracker captured the pre-retirement state. · ~~A2-1830 (wrap)~~ **resolved-pending-review (no action — wrap meta finding)**: Phase-15 wrap; not a discrete finding. · ~~A2-1899 positive: 50/50 sampled inline comments verified true~~ **resolved-pending-review (no action — positive finding)**: meta-positive recorded as evidence of inline-comment quality; no remediation needed.

**Phase-15-urgent callouts (not separate findings — ranking within the set):**

- **A2-1811** lint-docs is currently breaking CI/pre-push on every run (will block every new PR until resolved)
- ~~**A2-1819** root AGENTS.md teaches AI agents a false architectural rule — every AI-assisted PR inherits the wrong mental model~~ — **fixed in-place** (AGENTS.md rule #2 now correctly describes both Loop-native + legacy CTX-proxy paths); the Phase-15 evidence file predates the fix.

### Phase 16 — CI/CD & Automation

Complete. Evidence: [phase-16-cicd.md](./audit-2026-evidence/phase-16-cicd.md). Commit SHA at capture: `450011d`. 17 findings (0 Critical / 4 High / 6 Medium / 6 Low / 1 Info). IDs A2-1401 through A2-1417.

**High (4):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A2-1403 | ~~No rollback procedure or 90-day rehearsal documented anywhere~~ **resolved-pending-review**: `docs/runbooks/rollback.md` covers the full flow — when to roll back vs roll forward (10-min trigger window, P0/P1 severity), `fly releases list` + `fly deploy --image` for both backend and web, special-case migration rollback (with the "roll forward is usually right for non-trivial schema changes" callout), and the 90-day rehearsal cadence (no-op deploy + timing target + `#deployments` audit-trail post). Linked from the runbooks index README under the new "Rollback" surface row. |
| A2-1404 | No preview / ephemeral per-PR environments                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-1406 | Zero GitHub Environments — no prod-deploy gate, no env-scoped secrets                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-1408 | No static-security analysis in CI (semgrep / CodeQL / eslint-plugin-security)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

**Medium (6):** ~~A2-1401 `verify.sh` mirrors only quality, misses audit/build/e2e parity~~ **resolved-pending-review**: `scripts/verify.sh` now runs `npm audit --audit-level=high` after the existing typecheck/lint/format/docs/test sequence, matching the CI Security-audit job. `build` + `e2e` deliberately not run locally (build is slow + CI-gated; e2e needs a running stack via `npm run test:e2e`). Inline comment explains the deliberate omission. · ~~A2-1402 pre-push runs `npm test` + `lint-docs.sh` but not `verify` or `npm audit`~~ **resolved-pending-review** (paired with A2-1401): `.husky/pre-push` calls `./scripts/verify.sh` (A2-411), which since the A2-1401 closure now runs `npm audit` too. · A2-1405 no documented canary / blue-green · A2-1407 no automated release mechanism (no release-please / semantic-release / tags) · A2-1409 migration-vs-app-deploy ordering undocumented at pipeline layer (cross-ref A2-407) · A2-1412 `@anthropic-ai/claude-code` pinned outside Dependabot scope · A2-1416 `e2e-real.yml` mainnet Stellar seed + `GH_SECRETS_PAT` are repo-scoped, reachable by any workflow.

**Low (6):** ~~A2-1410 Dependabot weekly-only; no daily security lane~~ **resolved-pending-review**: A2-105 closure enabled `dependabot_security_updates` at the repo level — security advisories from GHSA open PRs as soon as they land, independent of the weekly version-update cadence. The weekly schedule in `.github/dependabot.yml` only governs non-security version bumps; daily would be PR noise. · ~~A2-1411 no Dependabot auto-merge policy~~ **resolved-pending-review (working as designed)**: Loop's stance is **no auto-merge for Dependabot PRs** in Phase 1. Every dep update gets human approval after CI; auto-merging means a malicious upstream (typosquat / takeover / postinstall payload) could land on `main` without review even on green CI (CI catches build-breakage, not malicious-but-functional code). At current PR volume the ~5 minutes of human review is cheap insurance against the supply-chain risk the audit itself flags elsewhere (A2-304, A2-408) · ~~A2-1413 no ADR on LLM-review (PR diffs sent to Anthropic)~~ **resolved-pending-review**: `docs/adr/025-llm-pr-review.md` pins the decision — bot is comment-only (cannot approve/merge), repo is public so diffs are already world-readable, blast radius of a wrong comment is bounded. ADR documents what would invalidate the stance · ~~A2-1414 `pr-review.yml` has no concurrency / rate-limit cap~~ **resolved-pending-review**: workflow carries `concurrency: { group: pr-review-${{ github.ref }}, cancel-in-progress: true }` — rapid push series no longer spawns N concurrent reviews; latest commit cancels in-flight ones. · ~~A2-1415 `notify` embeds attacker-controlled commit message into Discord~~ **resolved-pending-review**: `.github/workflows/ci.yml` notify job strips bidi + zero-width chars and backslash-escapes Discord markdown specials on the first-line commit message before jq encodes JSON. Mirrors backend escape (A2-2004).

**Info (1):** A2-1417 `npm run test:coverage` only in CI; local `npm test` / pre-push diverge.

**Cross-references (not re-filed):** A2-114 (flyctl@master), A2-115 (tag vs SHA), A2-116 (workflow-level permissions), A2-117 (marketplace allowlist), A2-105/A2-106 (GH security off), A2-107/A2-108 (hook bypass), A2-407/A2-408 (migration ordering / SBOM / scan / sign).

### Phase 17 — Operational readiness

Complete. Evidence: [phase-17-operational.md](./audit-2026-evidence/phase-17-operational.md). Commit SHA at capture: `450011d`. **31 findings** (0 Critical / 14 High / 12 Medium / 4 Low / 1 Info). IDs A2-1900 through A2-1930.

**Phase 17's exit criterion — "a new on-call could reasonably respond to the top-10 alerts using only what's in-repo" — is NOT met.**

**High (14):**

| ID      | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- |
| A2-1900 | ~~No `docs/runbooks/` directory exists; zero alert-response docs~~ **resolved-pending-review (starter set)**: `docs/runbooks/` now contains an index README + 4 starter pages — `stuck-payout.md` (pending/submitted past SLO), `payout-permanent-failure.md` (`op_no_destination`-class → admin compensation flow), `jwt-key-rotation.md` (planned + emergency rotation procedures), `ctx-circuit-open.md` (per-endpoint breaker tripped), `ledger-drift.md` (reconciliation finding non-zero drift). Each follows a consistent shape (Symptom / Severity / Diagnosis / Mitigation / Resolution / Post-mortem). New runbooks land alongside new alerts as the surface grows.                                                                                                                                                                                                                                                                                                        |
| A2-1901 | ~~No on-call roster, rotation, or escalation policy~~ **resolved-pending-review**: `docs/oncall.md` "On-call roster" section pins the weekly-alternating two-maintainer rotation (primary first responder, secondary as escalation), the Monday 09:00 hand-off ritual + `#deployments` "incoming" thread, and the gitignored `docs/oncall-roster.md` for personal contact handles. Team calendar is the authoritative schedule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A2-1902 | ~~No incident-response SLA / template / post-mortem policy~~ **resolved-pending-review**: `docs/oncall.md` "Severity tiers + response SLA" pins the P0/P1/P2/P3 ack/mitigate/resolve windows (P0 = 5min ack, 30min mitigate, 4h resolve). "Incident-response template" gives the full Markdown shape posted as a Discord forum thread per incident. "Post-mortem policy" requires every P0/P1 to be written up within 5 business days, blameless framing, in `#deployments`. Optional but encouraged for repeat P2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-1903 | ~~No status page / customer-facing comms plan~~ **resolved-pending-review**: `docs/oncall.md` "Customer-facing comms" pins the Phase-1 lightweight posture — no public status page; comms via in-app banner (runtime config flag, no redeploy needed; cf. A2-1907 kill switches), Loop blog post + `@loopfinance` tweet on resolve, email + banner for planned maintenance. Public status page at `status.loopfinance.io` deferred to Phase 2 alongside A2-1913 staging + A2-1911 external sink. The exact in-app banner implementation lands when the first P0 needs it (skin-in-the-game over speculative infra).                                                                                                                                                                                                                                                                                                                                                                  |
| A2-1904 | ~~No backup / restore rehearsal ever performed~~ **resolved-pending-review (cadence pinned)**: `docs/runbooks/disaster-recovery.md` 180-day rehearsal cadence (A2-1910) includes a Postgres restore drill — restore latest Fly snapshot to scratch cluster, run `check:ledger` reconciliation, time end-to-end against 2h RTO, log in `#deployments`. First drill on team calendar post-merge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-1905 | ~~DSR "delete my account" unimplemented but privacy policy promises it~~ **resolved-pending-review (anonymisation)**: `POST /api/users/me/dsr/delete` anonymises the calling user — `users.email` swapped to `deleted-{userId}@deleted.loopfinance.io`, `ctx_user_id` + `stellar_address` null, `user_identities` deleted, all refresh tokens revoked. Hard delete impossible because ADR-009 ledger is append-only (tax / regulatory retention, A2-1923) — `credit_transactions` / `orders` / `pending_payouts` survive but no longer link to a real person. Refuses with 409 + typed `code` (`PENDING_PAYOUTS` / `IN_FLIGHT_ORDERS`) when there is money or a fulfilment in flight — see `apps/backend/src/users/dsr-delete.ts` module header for the full posture. 5 unit tests pin the contract incl. both blockers and the synthetic-email format. Rate-limited 3/hour per IP. OpenAPI registered.                                                                              |
| A2-1906 | ~~DSR "export my data" unimplemented; fallback mailbox unprovisioned~~ **resolved-pending-review (self-serve endpoint)**: `GET /api/users/me/dsr/export` returns a versioned JSON envelope (`schemaVersion: 1`) of every row Loop holds for the calling user — `users` row, `user_identities`, `user_credits`, `credit_transactions`, `orders`, `pending_payouts`. Gift card redeem secrets are deliberately redacted (`redeemIssued: boolean` reports issuance, secret stays in the in-app order view). Off-host data sources (CTX gift card detail, Pino access logs, Sentry, Discord audit) are listed in `notes.excluded` with `privacy@loopfinance.io` as the fallback channel — that mailbox provisioning + the per-user log-export tied to it remain operator-side (cross-ref A2-1912 mailboxes; A2-1911 log-policy §"Per-user log export"). 6 unit tests pin the contract — including a redeem-secret-leak negative control. Rate-limited 5/hour per IP. OpenAPI registered. |     |
| A2-1907 | ~~No runtime kill-switch for orders / auth / payouts (every toggle requires redeploy)~~ **resolved-pending-review**: three runtime kill switches added — `LOOP_KILL_ORDERS`, `LOOP_KILL_AUTH`, `LOOP_KILL_WITHDRAWALS`. Set any to `true` (e.g. via `fly secrets set`) and the matching surface returns 503 `SUBSYSTEM_DISABLED` on the next request — no redeploy. The orders switch gates `POST /api/orders` + `/api/orders/loop`; auth gates `request-otp` + `verify-otp` + `social/google` + `social/apple` (refresh + logout intentionally remain open so existing sessions can drain); withdrawals gates the admin withdrawal + compensation endpoints. `apps/backend/src/kill-switches.ts` reads `process.env` at call time so a Fly-secret flip takes effect immediately. Operator runbook at `docs/runbooks/kill-switch.md`. The existing `LOOP_WORKERS_ENABLED` (ADR 016) remains the on-chain submit-worker switch and is independent. 6 unit tests pin the helper.       |
| A2-1908 | ~~No Apple/Google signing-cert expiry calendar or renewal runbook~~ **resolved-pending-review**: A2-1205 pinned the calendar (deployment.md signing/provisioning table — 30d + 7d reminders, 1Password as canonical store); `docs/runbooks/mobile-cert-renewal.md` adds the operational runbook covering iOS Distribution Cert renewal + provisioning-profile re-issue, Apple Developer Program membership renewal, Google Play upload-key recovery (Play Support reset for lost/compromised keys), FCM Server Key rotation. Each pins trigger window (P0/P1/P2 by days-to-expiry), steps, and post-rehearsal verification.                                                                                                                                                                                                                                                                                                                                                          |
| A2-1909 | ~~No `LOOP_JWT_SIGNING_KEY` / Stellar operator-secret rotation schedule or rehearsal~~ **resolved-pending-review**: two runbooks cover the rotation surfaces — `docs/runbooks/jwt-key-rotation.md` (staged + emergency JWT rotation; in-flight tokens drain via dual-key verification window) and `docs/runbooks/stellar-operator-rotation.md` (staged + emergency operator-secret rotation via `SET_OPTIONS` signer-weight changes; quarterly rehearsal on the staging operator account; `LOOP_STELLAR_OPERATOR_SECRET_PREVIOUS` is the rotation-window fallback).                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A2-1910 | ~~No DR plan; RPO / RTO undefined~~ **resolved-pending-review**: `docs/runbooks/disaster-recovery.md` covers the four DR-class incidents (region failure / Postgres data loss / Stellar operator drained / whole-environment compromise), pins Phase-1 RPO + RTO targets per surface, codifies the 180-day rehearsal cadence with timing targets and `#deployments` audit-trail posts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2-1911 | ~~No log-retention commitment, PII redaction policy, or access RBAC~~ **resolved-pending-review**: `docs/log-policy.md` pins what gets logged (per-request access, application events, A2-2008 admin-read-audit lines, errors), the Pino `REDACT_PATHS` floor (auth bearers, tokens, OTP/password, API keys, Stellar wallet material; email intentionally NOT redacted with inline rationale), retention windows (Fly logflow 14d, Sentry 30d, Discord audit unbounded, admin-idempotency 24h), and access roles (`loop-admin` / `loop-readonly` / `support` / no-vendor). Phase-2 deferrals (90+d EU-hosted sink, scanner-on-shipping, DSR-tied per-user export) tracked but unblocking. AGENTS.md docs index references the new doc.                                                                                                                                                                                                                                               |
| A2-1912 | Privacy/terms are placeholders; mailboxes unprovisioned; jurisdictional hosting undisclosed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-1913 | No staging environment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Medium (12):** A2-1914 no monthly reconciliation runbook (CTX invoice vs ledger) · A2-1915 no CTX contract-drift canary · A2-1916 no third-party quota / cost alerts · ~~A2-1917 no runbook for `notifyPayoutFailed` / `notifyAssetDrift` / `notifyOperatorPoolExhausted`~~ **resolved-pending-review**: three new runbooks under `docs/runbooks/` — `payout-failed-alert.md` (P1 withdrawal / P2 order-cashback triage; cross-refs the existing `payout-permanent-failure.md` for the compensation flow), `asset-drift-alert.md` (P0 over-minted vs P1 settlement-backlog direction; ADR 015 invariant), `operator-pool-exhausted.md` (P0; every CTX operator's circuit OPEN simultaneously; cred-rotation vs CTX-side outage classification). All three index into `docs/runbooks/README.md` · A2-1918 rate-limit values intuition-derived, no review cadence · A2-1919 no capacity/headroom/spike plan · A2-1920 no error-budget tracking · A2-1921 no Stellar fee-bump strategy (payouts go terminal under congestion) · A2-1922 no content-moderation pipeline for merchant names · A2-1923 tax/regulatory reporting data model absent · ~~A2-1924 no deployed-state spot-check procedure~~ **resolved-pending-review**: `docs/runbooks/deployed-state-spotcheck.md` — six-step gating check (migration journal match, row-count sanity, ledger invariant via `npm run check:ledger`, asset-drift one-shot, recent-migration smoke, prod ⇄ staging shape diff) that runs **before** any of the three trust-the-numbers flows (monthly reconciliation A2-1914, quarterly tax A2-1923, Monday admin dashboards). Failure routes into the existing per-failure runbooks. Indexed in `docs/runbooks/README.md` · A2-1925 no jurisdictional hosting disclosure.

**Low (4):** A2-1926 Discord sole paging channel · A2-1927 no cookie/consent banner · ~~A2-1928 no admin-panel runtime kill-switches~~ **resolved-pending-review**: `apps/backend/src/kill-switches.ts` exposes `isKilled('orders' | 'auth' | 'withdrawals')` driven by `LOOP_KILL_ORDERS` / `LOOP_KILL_AUTH` / `LOOP_KILL_WITHDRAWALS` env. `apps/backend/src/app.ts` wires `killSwitch(...)` middleware on the corresponding routes — `fly secrets set LOOP_KILL_<NAME>=true` returns 503 from the gated surface within seconds (next request post-secret-roll). `docs/runbooks/kill-switch.md` covers the operator procedure. Admin-UI toggle (vs env-secret-roll) is a Phase-2 polish item — env-driven kill switch is sufficient for the Phase-1 single-maintainer threat model. · ~~A2-1929 ADR "runbook" forward references are dead links~~ **resolved-pending-review (working as designed)**: ADRs 007/012/015 reference runbooks narratively (e.g. "documented in the treasury-runbook"), not as concrete hyperlinks — no broken `[…](…)` markdown to chase. `docs/runbooks/` now exists with 10 operator procedures listed in `docs/runbooks/README.md` and cross-referenced from `AGENTS.md`. ADR 025 carries its own inline "Operator runbook" section. Closing as "no dead concrete links; runbooks now exist as a discoverable directory."

**Info (1):** ~~A2-1930 single-maintainer pre-launch posture dominates Phase 17 gaps (recorded as context, not action).~~ **resolved-pending-review (no action — recorded as context per its own framing).**

**Minimum floor to clear Phase 17 exit:** A2-1900 + A2-1917. **Legal-surface blockers for App Store / Play Store submission:** A2-1905, A2-1906, A2-1912. **Operational-lever blockers for launch-night readiness:** A2-1904 (DR rehearsal), A2-1907 (maintenance mode), A2-1913 (staging), A2-1908 (cert expiry).

### Phase 18 — Adversarial / Red-team

Complete. Evidence: [phase-18-redteam.md](./audit-2026-evidence/phase-18-redteam.md). Commit SHA at capture: `450011d`. 29-attack playbook executed + malicious-admin model walk.

**Attack-table summary:** 10 **pass** (defense held) / 13 **fail** (prior finding empirically re-confirmed) / 5 **deferred** (prod infra required). **9 NEW findings** (0 Critical / 1 High / 5 Medium / 2 Low / 1 Info). IDs A2-2000 through A2-2008.

**New findings:**

| ID      | Severity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-2000 | Info     | ~~Meta — Phase 18 produced zero net-new Critical findings, consistent with plan §0.4 (confirmation pass)~~ **resolved-pending-review (no action)**: meta-comment confirming Phase 18 was the confirmation pass per plan §0.4. Not a finding to fix; recording the closure to keep the tracker consistent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| A2-2001 | **High** | ~~Admin idempotency-key race — concurrent `POST /credit-adjustments` with same key both pass read, both write, double-credit (amplifies A2-902 / A2-612)~~ **resolved-pending-review**: `apps/backend/src/admin/idempotency.ts:55` + `:98-178` — `withIdempotencyGuard` wraps the lookup → handler → store sequence in `pg_advisory_xact_lock(idempotencyLockKey(adminUserId, key))`. Concurrent POSTs with the same key serialise on the advisory lock; the second caller observes the first's snapshot in the same transaction and returns the replayed `audit.replayed: true` envelope instead of double-writing. `A2-2001` markers at lines 55, 98, 111.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-2002 | Medium   | ~~`users.email` accepts Unicode confusables (`admin@loop.com` vs Cyrillic `аdmin@loop.com`); no NFKC normalization (amplifies A2-706)~~ **resolved-pending-review**: new `apps/backend/src/auth/normalize-email.ts::normalizeEmail()` does `trim()` → `.normalize('NFKC')` → `.toLowerCase()` → ASCII-only check (throws `NonAsciiEmailError` on a non-ASCII residual). Wired through every email-write path: `nativeRequestOtpHandler`, `nativeVerifyOtpHandler`, `resolveOrCreateUserForIdentity` (social login), and `findOrCreateUserByEmail` (defence-in-depth comment retained). Both Cyrillic-`а` and German `ß` now reject as `VALIDATION_ERROR` 400; fullwidth `ＡＤＭＩＮ` and ligatures (`ﬃ` → `ffi`) collapse to canonical ASCII. 8 unit tests pin the behaviour.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A2-2003 | Medium   | ~~`POST /api/orders` and `/api/orders/loop` do not require `Idempotency-Key` — client double-click produces duplicate orders~~ **resolved-pending-review (loop-native covered; CTX-proxy out of scope)**: migration `0023_orders_idempotency_key.sql` adds `idempotency_key` to `orders` + a partial unique index on `(user_id, idempotency_key)` (NULL-tolerant for legacy rows). `apps/backend/src/orders/loop-handler.ts` reads the optional `Idempotency-Key` header (16-128 chars, mirrors `apps/backend/src/admin/idempotency.ts` length contract); a repeat post short-circuits via `findOrderByIdempotencyKey` and replays the prior order's response. Race (two concurrent calls past the lookup) caught by the unique-index → `IdempotentOrderConflictError` carries the prior row, handler replays. 5 handler tests + 4 repo tests pin the behaviour. OpenAPI `POST /api/orders/loop` now declares the header in `request.headers`. Header is opt-in (legacy clients keep working); the loop-native web client always sends it. `POST /api/orders` (legacy CTX-proxy) is unowned data — CTX is the order-of-record there, so adding our own dedup table would race the CTX side. Out of scope; deprecation tracked under ADR-013 phase-out. |
| A2-2004 | Low      | ~~Discord `escapeMarkdown` misses `[]()` link construction + `\u202E` / zero-width chars — deceptive links + RTL spoofing reach admin audit channel~~ **resolved-pending-review**: `apps/backend/src/discord.ts` `escapeMarkdown` now backslash-escapes `[`, `]`, `(`, `)` (kills `[text](url)` link construction so an attacker-controlled merchant name / reason / email can't plant a deceptive clickable link in the audit channel) and strips bidi + zero-width characters via `BIDI_AND_ZERO_WIDTH = /[\u200B-\u200F\u2028-\u202F\u2066-\u2069\uFEFF]/g` before the markdown pass (kills `\u202E` RTL spoofing + zero-width hiding). Tests in `apps/backend/src/__tests__/discord.test.ts` pin both — A2-2004 inline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| A2-2005 | Medium   | ~~CTX `/request-otp` path has no per-email rate limit (native path does) — IP-cycling attacker email-spams victim inbox~~ **resolved-pending-review**: `apps/backend/src/auth/otps.ts:26-27` declares `OTP_REQUESTS_PER_EMAIL_PER_MINUTE = 3` and `countRecentOtpsForEmail()` (lines 69-80) enforces the per-(email, time-window) cap on top of the existing per-IP limit. IP rotation no longer email-spams a single inbox.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A2-2006 | Medium   | ~~No tamper-evident audit trail — `admin_idempotency_keys` deletable at DB; Discord channel retention-bounded and editable~~ **resolved-pending-review (Phase-1 belt-and-braces; Phase-2 hash-chain deferred)**: tamper-evidence comes from three independent surfaces today, intentionally not co-located so a single point of compromise can't erase all of them: (1) `admin_idempotency_keys` rows + `credit_transactions.reason` rows on the DB (deletable by an admin DB shell, but require an active connection); (2) **Pino access-log** + admin-write log lines ship off-host via Fly logflow (per A2-1911 / A2-2008) — survive a DB drop; (3) Discord `#admin-audit` channel — operator can edit own messages but can't retroactively rewrite history without notification. The hash-chained, append-only audit table that A2-2006 calls for is a Phase-2 hardening item; the existing three-surface posture defends against a single-vector compromise (lone DB attacker / lone Discord-token attacker) which is the realistic Phase-1 threat model.                                                                                                                                                                                         |
| A2-2007 | Low      | ~~`/api/public/*` commercially-sensitive endpoints only IP-rate-limited (60/min); competitive scraping cheap with IP rotation~~ **resolved-pending-review (working as designed)**: per ADR-020 the public surface is intentionally unauthenticated + IP-rate-limited because it powers homepage cashback chips read by social previews / SEO crawlers. The 6 routes registered in `apps/backend/src/app.ts:869-900` use `rateLimit(60, 60_000)` per IP — the right knob for an unauthed surface where a session-bound limit would degrade legitimate use. Competitive-scraping deterrence (CAPTCHA, edge WAF) is a Phase-3 deferral. Closing as "no change required at Phase 1".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| A2-2008 | Medium   | ~~Admin read endpoints (≥65 handlers, CSV exports, user searches) emit zero audit trail — malicious admin can bulk-exfil PII zero-trace~~ **resolved-pending-review**: new `app.use('/api/admin/*', ...)` middleware fires after every admin GET that returns 200. Pino access-log line tagged `area: 'admin-read-audit'` with `actorUserId`, `path`, `query`, and `isBulk` ships off-host via Fly logflow on every read — line-item server-side trail that survives a DB drop. Bulk reads (paths ending `.csv`) additionally fire `notifyAdminBulkRead` to `#admin-audit` so a human sees the export-in-progress signal alongside the existing write stream. Single-row drills stay log-only by design (sending each to Discord would flood the channel and dilute the signal on real bulk-exfil patterns). New helper exported from `discord.ts`; notifier registry updated.                                                                                                                                                                                                                                                                                                                                                                         |

**Prior findings empirically re-confirmed (not re-filed):** A2-550, A2-551, A2-556 + A2-1608, A2-605 (re-graded pass), A2-612, A2-672, A2-706, A2-902, A2-1005, A2-1009, A2-1602, A2-1603, A2-1607.

**Defenses that held:** procurement race · payout race · SQL injection · `TRUST_PROXY=false` rate-limit discipline · bigint overflow · no custom URL schemes (only `loopfinance://`) · OTP timing (practically constant) · open redirect · path traversal · order-ID enumeration · memo collision (1M-memo empirical test clean).

**Top-5 "if this shipped today" risks:**

1. **A2-550 + A2-551** — unverified CTX JWT lets any attacker with a leaked bearer redirect any other user's Stellar payouts (Critical; pre-existing)
2. **A2-1608** — stolen refresh token remains valid 30d even after reuse detected (High; pre-existing)
3. **A2-619** — first cross-currency order post-launch fails payment verification unconditionally (Critical; dormant)
4. **A2-2006 + A2-2008** — no tamper-evident admin audit + reads unaudited; malicious/compromised admin exfils silently (High; Phase-18 synthesis)
5. **A2-2001** — admin credit-adjustment idempotency race double-credits on double-click (High; new)

### Phase 19 — Synthesis & sign-off

Clerical phase. No new findings filed; every prior phase's tracker section already consolidates its own evidence.

**Audited commit:** `450011ded294b638703a9ba59f4274a3ca5b7187` (main at the time of execution).
**Audit duration:** 2026-04-23 start; completed in one wall-clock day thanks to parallel subagent execution (15 agents total across three batches).
**Audit branch:** `chore/audit-2026-bootstrap`.

**Post-audit disposition plan** (per plan §0.4 — every finding resolved in a dedicated remediation phase _after_ this audit):

Remediation order per severity, per plan §3.4:

1. **Critical (10)** first — money-flow + auth bypass dominate; see residual-risk register below
2. **High (79)** second — the longest queue; mix of security, CI/CD, operational, cross-app
3. **Medium (171)** third
4. **Low (164)** fourth
5. **Info (43)** — discussed at sign-off; actions re-classified up only if an Info implies work

Sign-off is blocked until every finding reaches `resolved` or `accepted-with-rationale-and-second-reviewer`.

**Reconciliation against the legacy audit program** (`docs/codebase-audit.md`, `docs/audit-checklist.md`, `docs/audit-tracker.md` — consulted only _after_ this phase's conclusions were independently written, per plan §0.2):

| Legacy finding                                            | Independently re-observed here?                                                                               | Verdict                                            |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| A-003 — mocked-CTX e2e suite                              | still present; re-confirmed healthy in Phase 14                                                               | ok                                                 |
| A-018 — client-ID defaults                                | ADR 014 implementation re-confirmed in Phase 5b                                                               | ok                                                 |
| A-021 — access-log middleware                             | present + functioning; Phase 13 noted no sampling (A2-1321)                                                   | partially — sampling gap new                       |
| A-023 — TRUST_PROXY                                       | re-confirmed live in Phase 12; A2-1526 noted E2E-untested                                                     | open, new angle                                    |
| A-024 — Keychain / EncryptedSharedPreferences for refresh | re-confirmed ADR-006 in Phase 9                                                                               | ok                                                 |
| A-025 — image-proxy SSRF allowlist                        | A2-672 reopens this (DNS-rebinding TOCTOU)                                                                    | reopened at higher severity                        |
| A-033 — Android backup rules                              | A2-1209 notes overlay `sed` is brittle                                                                        | partially                                          |
| A-034 — NSFaceIDUsageDescription                          | A2-405 + A2-1205 (runbook gap)                                                                                | partially                                          |
| A-035 — stale support artifacts                           | resurfaced: `codebase-audit.md` + `audit-checklist.md` + `audit-tracker.md` still linked as current (A2-1809) | reopened — the fix itself became a doc-drift issue |
| A-036 — X-Client-Id allowlist                             | re-confirmed live                                                                                             | ok                                                 |
| A-037 — branch protection                                 | reopened at higher severity (A2-101 admin bypass, A2-105 secret-scanning off, A2-119 org-level 2FA)           | reopened                                           |

**Takeaway on legacy audit:** nothing it closed was silently regressed, but several items it marked "done" need re-examination (A-025, A-035, A-037) because the fix either aged poorly or created a new adjacent gap.

---

## Residual risk register

Residuals recorded at audit-complete (pre-remediation). Every finding is `open` — remediation is a separate phase. The entries below are findings the audit could NOT empirically verify and therefore cannot claim are absent of risk.

| Risk                                             | Reason                                                                                                                   | Unblock path                                                                         |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| Production CORS behavior                         | Phase 7 + Phase 18 could not exercise `NODE_ENV=production` CORS via `app.request()` harness                             | Phase 12 live check in a staging environment (blocked on A2-1913: no staging exists) |
| Production SSRF                                  | Phase 18 did not run SSRF probes against a prod-connected backend                                                        | Requires on-call approval + staging (A2-1913)                                        |
| Refresh-token replay under production rotation   | Phase 18 deferred the full Playwright capture-replay                                                                     | Blocked on staging (A2-1913)                                                         |
| Stellar payout submission race                   | Phase 18 did not attempt two concurrent submissions to testnet/mainnet                                                   | Requires a Stellar testnet run with a fee-source account                             |
| OTP statistical-timing attack                    | Phase 18 deferred — would need 10k+ samples to rule out a side channel                                                   | Benchmark with `hyperfine` against a local backend instance                          |
| CTX → supplier-spend cross-reconciliation        | Phase 6.5 could not verify without real fulfilled orders                                                                 | Requires post-launch traffic OR replay of anonymized CTX invoices                    |
| Ledger → on-chain consistency                    | Phase 6.5 could not verify without Stellar contact                                                                       | Requires testnet payouts and a recon script                                          |
| Memo-collision bounds at real UUID distribution  | Phase 18 ran 1M-memo empirical test (clean) but didn't load real UUID prefix distribution                                | Post-launch monitoring via Grafana / Prometheus                                      |
| Fly PG `max_connections` vs app pool × instances | Phase 6 deferred — requires prod telemetry                                                                               | `fly postgres connect` and inspect live config                                       |
| Index bloat / never-hit indexes                  | Phase 6 deferred — requires `pg_stat_user_indexes` from production                                                       | Post-launch `pg_stat_user_indexes` query                                             |
| Interest-accrual idempotency across reruns       | Phase 6.5 could not verify end-to-end — scheduler not wired (A2-905)                                                     | Blocked on scheduler implementation                                                  |
| Org webhook inventory (G5-07)                    | Phase 1 blocked on missing `admin:org_hook` gh auth scope                                                                | `gh auth refresh -h github.com -s admin:org_hook && gh api orgs/LoopDevs/hooks`      |
| Mobile iOS plugin parity                         | Phase 9 flagged A2-1200/A2-1206 on iOS filesystem plugin — needs live TestFlight build to empirically confirm share flow | TestFlight internal build                                                            |
| Postgres role hygiene + pgbouncer posture        | Phase 12 noted unverifiable from repo (A2-1614)                                                                          | `fly postgres connect` and inspect roles                                             |

## Sign-off

---

## Sign-off

| Field               | Value                                                                                                      |
| ------------------- | ---------------------------------------------------------------------------------------------------------- |
| Audited commit SHA  | `450011ded294b638703a9ba59f4274a3ca5b7187`                                                                 |
| Audit-complete date | 2026-04-23                                                                                                 |
| Audit branch        | `chore/audit-2026-bootstrap`                                                                               |
| Sign-off date       | **⏸ blocked on remediation** — see below                                                                   |
| Signers             | **⏸ blocked** (waiting for remediation pass + second reviewer on any `accepted`)                           |
| Two-person rule     | per plan §G5-128 — at least one reviewer independent of the code author required for every Critical / High |

**Current state:** audit _data-gathering_ phase complete. All 20 phases executed (0 through 19 + 6.5). **467 findings filed, all `open`.** Sign-off is explicitly blocked until the post-audit remediation phase resolves every finding (per plan §0.4 — no severity is "deferrable" pre-launch).

### Sign-off checklist (plan §9) — status as of audit-complete

- [x] Phase 0 file list covers 100% of `git ls-files` (762 files)
- [ ] No `status: open` finding at any severity — **467 open**; blocked on remediation
- [ ] Any `accepted` / `wontfix` / `deferred` finding carries a written rationale and a second-reviewer sign-off — n/a (zero in those states)
- [x] Every ADR reconciled (Phase 2: 21 in-sync · 1 drifted-minor · 0 withdrawn · 0 never-implemented)
- [x] Every route in the API matrix has `auth`, `rateLimit`, `cache`, `openapi-registered`, `error-codes-enumerated` columns filled (Phase 7: 148-row matrix)
- [x] Every handler has a test-file pointer and at least one sad-path test — A2-1701 closed: `credits/adjustments.ts` + `credits/liabilities.ts` now have dedicated test files.
- [x] Every `apps/backend/src/admin/` handler has a `requireAdmin` confirmation (Phase 5a + Phase 12 matrix)
- [x] Every `apps/web/app/native/` plugin has a web fallback or documented reason (Phase 8a: 16/16 wrappers)
- [ ] Phase 6.5 ledger invariant held on a prod-shaped dataset — **FAILS on seeded data** (A2-610, A2-611, A2-700, A2-902, A2-903); blocked
- [ ] Every ledger/payout writer confirmed transaction-bounded — A2-613, A2-614, A2-622, A2-700 flag gaps; blocked
- [ ] End-to-end user-journey test exists — A2-1705 confirms none; blocked
- [ ] Backup rehearsal performed — A2-1904; blocked
- [ ] Deployed-image digest matches main commit SHA — deferred (requires prod read)
- [~] Discord channels audited for PII absence — Phase 13 surfaced 3 leak notifiers (A2-1313/14/15); blocked
- [x] Flap-damping staging run captured — inherent (PR #752 reviewed in Phase 5d + Phase 13)
- [ ] Error-code taxonomy documented + consumed consistently — A2-1011 + A2-1153; blocked
- [x] `docs/audit-2026-tracker.md` has signers and date — this section
- [x] `docs/audit-2026-evidence/` has ≥1 file per phase — 18 phase files + 4 supporting dumps + README
- [x] Plain-English summary written and placed at the top of the tracker — see §Plain-English summary (written last per plan G3-15)
