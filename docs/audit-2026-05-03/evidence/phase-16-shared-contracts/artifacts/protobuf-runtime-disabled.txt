Phase 16 artifact: cluster protobuf path is disabled by generated/runtime API mismatch
Date: 2026-05-03
Scope: current code only; Claude audit workspace excluded.

Verified source and generation:
- apps/backend/proto/clustering.proto defines ProtobufClusterResponse at fields 1-6.
- package.json:30 runs `cd apps/backend && npx buf generate && ... prettier`.
- apps/backend/buf.gen.yaml:1-6 uses `buf.build/bufbuild/es` with `target=ts`.
- packages/shared/src/proto/clustering_pb.ts:1 says generated by protoc-gen-es v2.11.0 with target=ts.
- packages/shared/src/proto/clustering_pb.ts exports `ProtobufClusterResponseSchema`, not a `ProtobufClusterResponse` class.

Runtime probe:
- `node -e "import('./packages/shared/src/proto/clustering_pb.ts').then(m=>{ console.log(Object.keys(m)); console.log(typeof m.ProtobufClusterResponse); console.log(typeof m.ProtobufClusterResponseSchema) })"`
- Output keys: BoundsSchema, ClusterPointSchema, ClusterPropertiesSchema, CoordinatesSchema, GeometrySchema, LocationPointSchema, LocationPropertiesSchema, ProtobufClusterResponseSchema, file_proto_clustering.
- Output: `ProtobufClusterResponse undefined`; `Schema object`.

Backend current behavior:
- apps/backend/src/clustering/handler.ts:100-112 dynamically imports `@loop/shared/src/proto/clustering_pb.js` and reads `mod.ProtobufClusterResponse`.
- apps/backend/src/clustering/handler.ts:121-156 only emits protobuf when `typeof ProtobufClusterResponse === 'function'`, then constructs `new ProtobufClusterResponse(...)` and calls `msg.toBinary()`.
- With the current generated file, that symbol is undefined, so the handler falls through to JSON even when `Accept: application/x-protobuf`.

Web current behavior:
- apps/web/app/services/clusters.ts:62-64 attempts protobuf decode only when the response Content-Type includes `x-protobuf`.
- apps/web/app/services/clusters.ts:73-78 dynamically imports `ProtobufClusterResponse` and calls `.fromBinary(...)`.
- If the backend ever did send protobuf with the current generated module shape, the web decode path would fail for the same reason: the class-style symbol and static `.fromBinary` method do not exist.

Test gap:
- apps/backend/src/clustering/__tests__/handler.test.ts:194-208 accepts either JSON or protobuf for `Accept: application/x-protobuf`; it only asserts status 200 and a non-empty content type.
- Therefore the test suite does not prove that protobuf negotiation is active, nor that bytes can be decoded by the web client.

Reasoning:
- The current code is written for an older/generated class API while the checked-in generated file uses protobuf-es v2 schema descriptors.
- This contradicts the critical architecture rule that clients request protobuf and JSON is fallback/debug only.
