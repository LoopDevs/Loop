CONTRIBUTING.md:37:5. **Get review.** `.github/CODEOWNERS` lists reviewer expectations for auth, ledger, admin, Stellar, CTX-operator, DB schema, and shared-type code. Note: the `@LoopDevs/engineering` team referenced there hasn't been created yet (audit A2-103), so GitHub silently skips the required-reviews rule today — branch protection allows an admin squash-merge without approval. Treat a reviewer as mandatory on those paths anyway; enforcement flips on once the team lands.
CONTRIBUTING.md:88:- `Deciders:` is not used (Loop is a pre-team project; see `.github/CODEOWNERS` for the active maintainer).
AGENTS.md:268:- **Never push directly to `main`** — all changes via PR. Branch protection is now enforced by GitHub (audit A-037 closed after the repo went public): required passing status checks are `Quality (typecheck, lint, format, docs)`, `Unit tests`, `Security audit`, `Build verification`, `E2E tests (mocked CTX)`; force-push and branch deletion are blocked; stale reviews dismiss on new commits. The `gh api repos/LoopDevs/Loop/branches/main/protection` endpoint now returns the active ruleset. Admins can still squash-merge without a required approval because the project is pre-team, but the passing-checks gate is non-negotiable.
docs/development.md:383:- `main` — always deployable. GitHub branch protection is active (audit A-037 closed once the repo went public): required passing checks are Quality, Unit tests, Security audit, Build verification, and E2E tests (mocked CTX); force-push and branch deletion are blocked. See `docs/standards.md §15 CI/CD` for the exact ruleset.
docs/standards.md:531:Phase-1 posture is "policy-by-convention": branch protection requires PR + a passing CI gate, but does not require `required_signatures = true` on the GitHub side. The current operator pair both push from machines they control end-to-end, so the marginal value of the org-side gate is low; the rule flips on once a third push-capable member joins.
docs/standards.md:546:Once the third push-capable member is on the rotation, flip the org rule on via `gh api` (`enforce_signed_commits` on the branch protection block) and update this section.
docs/standards.md:584:- **`main` is mechanically protected.** No direct pushes. All changes via PR. GitHub branch protection is live (audit A-037 closed after the repo went public): required passing checks are `Quality`, `Unit tests`, `Security audit`, `Build verification`, and `E2E tests (mocked CTX)`; force-push and branch deletion are blocked; stale reviews dismiss on new commits. See §15 CI/CD for the exact ruleset and the API pointer.
docs/standards.md:862:- **Auth and payment code requires two-person review** (team convention, not mechanically enforced). Any PR touching `apps/backend/src/auth/`, `apps/backend/src/orders/`, `apps/backend/src/credits/`, `apps/backend/src/payments/` (Stellar payout worker, Horizon reads, price feed), or any Capacitor secure-storage call should get approval from a second engineer who reviews security implications specifically. `.github/CODEOWNERS` auto-requests `@LoopDevs/engineering` on those paths, but the branch-protection ruleset on `main` sets `required_approving_review_count: 0` today so a solo maintainer can land a hot-fix without a round-trip. Once the team grows past one active maintainer, bump the required-approvals count and require code-owner review (see `.github/CODEOWNERS` for the enforced paths).
