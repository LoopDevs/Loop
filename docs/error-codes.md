# Error-code taxonomy

Single source of truth for every `{ code, message }` response the
Loop backend emits. Kept in sync with `packages/shared/src/api.ts`
(`ApiErrorCode` enum) — the enum is the TypeScript surface, this
doc is the operator + integrator reference.

Closes A2-1011. Previously, handlers shipped codes ad-hoc and
client switch-ladders tracked drift informally in code review.

## How to read the table

- **Code** — the literal string in the response body's `code` field.
- **Status** — the HTTP status the handler pairs with it. A client
  should branch on `code`, not status — multiple codes can share a
  status (e.g. several 400s), and status alone is coarser than
  needed for UX branching.
- **Where** — the handler families that emit it. Not exhaustive;
  `grep "code: 'X'"` gives the current list.
- **Client guidance** — what the web / mobile UX is expected to do
  when it receives this code. "Retryable" = safe to try again after
  some backoff; "terminal" = don't retry without fixing input /
  state first.

## Request-validation family (400)

| Code                                      | Status  | Where                                                                                                                                                                                                   | Client guidance                                                                                                                                                                                                                                                                           |
| ----------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `VALIDATION_ERROR`                        | 400     | Every handler — generic body / param / query validation                                                                                                                                                 | Terminal. Surface `message` to the user; fix input before retry.                                                                                                                                                                                                                          |
| `IDEMPOTENCY_KEY_REQUIRED`                | 400     | All admin writes (ADR 017)                                                                                                                                                                              | Terminal. The client is missing the `Idempotency-Key` header or sent one outside the 16-128 char range. Generate a UUID + retry.                                                                                                                                                          |
| `HOME_CURRENCY_LOCKED`                    | 409     | `POST /api/users/me/home-currency` after first order                                                                                                                                                    | Terminal. User placed an order; support must unlock. Surface the "contact support" path.                                                                                                                                                                                                  |
| `INSUFFICIENT_CREDIT`                     | 400     | `POST /api/orders/loop` with `paymentMethod=credit`                                                                                                                                                     | Terminal. User's cashback balance is below the order amount. Prompt them to top up via another method.                                                                                                                                                                                    |
| `CREDIT_METHOD_RETIRED`                   | 400     | `POST /api/orders/loop` with `paymentMethod=credit` by a wallet-activated user (ADR-036 OQ3)                                                                                                            | Terminal. The balance IS the tokens once the wallet is activated — spend via token redemption (`loop_asset` / `POST /api/orders/loop/:id/redeem`).                                                                                                                                        |
| `LOOP_ASSET_UNAVAILABLE_PHASE_1`          | 400     | `POST /api/orders/loop` with `paymentMethod=loop_asset`, and `POST /api/orders/loop/:id/redeem` for any `loop_asset` order, while `LOOP_PHASE_1_ONLY=true` (AUDIT-2 finding B, 2026-07 hardening)       | Terminal while the flag is on. loop_asset is a Phase-2 spend surface (redemption of on-chain LOOP); use `credit`, `xlm`, or `usdc` instead. Goes away when `LOOP_PHASE_1_ONLY` flips to `false`.                                                                                          |
| `INSUFFICIENT_BALANCE`                    | 400     | `POST /api/admin/users/:userId/emissions` (ADR-024/036); `POST /api/orders/loop/:id/redeem` (ADR-030 C3) when the on-chain LOOP balance is below the charge                                             | Terminal. Admin path: emission larger than the mirror balance. Redeem path: prompt the user to pay another way.                                                                                                                                                                           |
| `ORDER_NOT_PAYABLE`                       | 400     | `POST /api/orders/loop/:id/redeem` (ADR-030 C3)                                                                                                                                                         | Terminal. The order is not a `loop_asset` order awaiting payment (wrong method, or terminal failed/expired state). Already-paid states replay 200.                                                                                                                                        |
| `PAYMENT_IN_FLIGHT`                       | 400     | `POST /api/orders/loop/:id/redeem` (ADR-030 C3)                                                                                                                                                         | Retryable after the in-flight call resolves (seconds). A concurrent redeem for the same order is being processed.                                                                                                                                                                         |
| `WALLET_NOT_ACTIVATED`                    | 400     | `POST /api/orders/loop/:id/redeem` (ADR-030 C3)                                                                                                                                                         | Retryable once provisioning completes. The embedded wallet isn't `activated` yet — poll `GET /api/me/wallet` and re-offer the button when it is.                                                                                                                                          |
| `REFUND_ALREADY_ISSUED`                   | 409     | `POST /api/admin/users/:userId/refunds`                                                                                                                                                                 | Terminal. DB partial unique index caught a duplicate refund for the same order. Surface the existing refund's id.                                                                                                                                                                         |
| `DEPOSIT_NOT_REFUNDABLE`                  | 409     | `POST /api/admin/deposits/:paymentId/refund` (A6) — the skip is not `abandoned`, its stored payment is unparseable, or it has no sender/amount, or the operator Stellar signer is unconfigured          | Terminal for this deposit. Not an abandoned late deposit (or missing data). Surface the detail; don't retry blindly.                                                                                                                                                                      |
| `REFUND_SUBMIT_FAILED`                    | 502     | `POST /api/admin/deposits/:paymentId/refund` (A6) — the Stellar submit failed; the claim is released back to `abandoned`                                                                                | Retryable. Transient Horizon/network or a rebuild-needed submit error — re-trigger the refund; idempotency (CF-18 hash) prevents a double-send.                                                                                                                                           |
| `ORDER_NOT_REDRIVABLE`                    | 400     | `POST /api/admin/orders/:orderId/redrive` (A5-1) — order is not `paid`                                                                                                                                  | Terminal for the current order state. `fulfilled`/`failed`/`expired` orders are already terminal; `pending_payment` orders haven't been paid yet — nothing to redrive. (`procuring` has its own 409.)                                                                                     |
| `ORDER_NOT_REFUNDABLE`                    | 400/409 | `POST /api/admin/orders/:orderId/refund` (A5-4) — order is `pending_payment`/`expired` (400), a not-yet-stale `procuring` order still actively procuring (400), or its state changed concurrently (409) | Terminal-ish. `pending_payment`/`expired` have nothing to reverse. A fresh `procuring` order may have a live worker mid-flight paying CTX — wait for it to fulfil/fail or age into the 15-min recovery sweep. On a concurrent state change, refresh + retry with a fresh Idempotency-Key. |
| `ORDER_REFUND_ATTESTATION_REQUIRED`       | 400     | `POST /api/admin/orders/:orderId/refund` (A5-4) — a `fulfilled` order with no attestation in the body                                                                                                   | Terminal until re-sent. Refunding a fulfilled order (customer already has the code) requires a code-unused attestation: `{ codeUnused: true, attestationNote }`. Confirm the code is unused, then retry.                                                                                  |
| `ORDER_ALREADY_REFUNDED`                  | 409     | `POST /api/admin/orders/:orderId/refund` (A5-4) — a refund already exists for this order (INV-8)                                                                                                        | Terminal. The order was already refunded (the migration-0013 partial unique index / the deposit-refund claim guard caught the duplicate). Surface the existing refund.                                                                                                                    |
| `ORDER_REFUND_CTX_ALREADY_PAID`           | 409     | `POST /api/admin/orders/:orderId/refund` (A5-4) — the order is `procuring` and Loop has already paid CTX                                                                                                | Terminal for this endpoint. Refunding now would double-lose money (CTX paid AND the customer refunded). Escalate for manual reconcile; a genuinely-stuck row is resolved by the recovery sweep.                                                                                           |
| `ORDER_REFUND_UNSUPPORTED_PAYMENT_METHOD` | 409     | `POST /api/admin/orders/:orderId/refund` (A5-4) — the order is `loop_asset`-funded                                                                                                                      | Terminal for this endpoint. `loop_asset` refunds need coordinated on-chain re-mint/re-credit (not built — matches the R3-2 fail-closed posture). Escalate for a manual money-review refund.                                                                                               |
| `ORDER_REFUND_SUBMIT_FAILED`              | 502     | `POST /api/admin/orders/:orderId/refund` (A5-4) — the on-chain refund-to-sender submit failed, or the order predates the payment-snapshot columns                                                       | Retryable. Transient Horizon/network, or a pre-migration order with no payment snapshot (refund manually). The order is already fenced to `failed`, so a retry via this endpoint is safe.                                                                                                 |
| `PAYLOAD_TOO_LARGE`                       | 413     | bodyLimit middleware (A2-1005)                                                                                                                                                                          | Terminal. Body exceeded 1 MB. Client should split the payload.                                                                                                                                                                                                                            |

## Auth family (401 / 403 / 404)

| Code                       | Status | Where                                                                                                                                 | Client guidance                                                                                                                          |
| -------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `UNAUTHORIZED`             | 401    | `requireAuth` middleware, admin handlers without actor context                                                                        | Retryable after re-auth. Trigger refresh-token rotation, then retry once. If still 401, sign the user out.                               |
| `STEP_UP_REQUIRED`         | 401    | `requireAdminStepUp` gate (ADR 028) — no `X-Admin-Step-Up` header                                                                     | Re-prompt the admin to confirm (OTP), mint a step-up token, replay the request with the header set.                                      |
| `STEP_UP_INVALID`          | 401    | `requireAdminStepUp` — token malformed / expired / wrong signature / wrong audience, or no Loop-native subject to pin against         | Same flow as `STEP_UP_REQUIRED`: re-prompt and replay. If the session is legacy CTX-proxy auth, sign in through Loop-native auth first.  |
| `STEP_UP_SUBJECT_MISMATCH` | 401    | `requireAdminStepUp` — the token's `sub` is a different admin than the bearer                                                         | Sign the admin out — a step-up minted for another session was replayed.                                                                  |
| `STEP_UP_PURPOSE_MISMATCH` | 401    | `requireAdminStepUp(action)` (CF-08) — a token narrowed to one action class was replayed against a different one                      | Re-prompt and mint a step-up for the correct action, then replay.                                                                        |
| `NOT_FOUND`                | 404    | Merchant / order / payout not found. Also: feature-flag-off (`LOOP_AUTH_NATIVE_ENABLED=false` surfaces the Loop-native routes as 404) | Terminal. Don't retry — the resource doesn't exist or the caller isn't authorised to see it (enumeration-defence masks the distinction). |
| `ORDER_NOT_FOUND`          | 404    | `POST /api/admin/users/:userId/refunds` (CF-06) — the bound order id matches no order                                                 | Terminal. Verify the order id before retrying the refund.                                                                                |
| `USER_NOT_FOUND`           | 404    | `POST /api/admin/users/:userId/home-currency` (ADR 015 support-mediated change) — the path user id matches no user                    | Terminal. Verify the user id before retrying.                                                                                            |
| `MERCHANT_NOT_FOUND`       | 404    | `POST /api/users/me/favorites` — the merchant id is not in the live catalog                                                           | Terminal. The merchant id is stale or evicted; refresh the catalog before retrying.                                                      |

## Well-known verification family (404)

| Code                        | Status | Where                                                                                                                                                                  | Client guidance                                                                                                                                                                                                                                                                                                                           |
| --------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `WELL_KNOWN_NOT_CONFIGURED` | 404    | `GET /.well-known/apple-app-site-association` when `APPLE_TEAM_ID` is unset; `GET /.well-known/assetlinks.json` when `ANDROID_CERT_SHA256` is unset (M-3 deep linking) | Terminal until ops sets the gating env var post-enrollment (go-live-plan L1-4 / L1-5). Deliberately 404, not 503 — both Apple's and Google's link verifiers treat a missing file as "not offered," the correct pre-enrollment state; a distinct code (not a reuse of `NOT_CONFIGURED`, which is 503 elsewhere) keeps one status per code. |

## Resource-state family (409)

| Code                                  | Status | Where                                                                                                                                                                        | Client guidance                                                                                                                                                                                                                                                                                                           |
| ------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ALREADY_COMPENSATED`                 | 409    | `POST /api/admin/payouts/:id/compensate` (ADR-024 §5)                                                                                                                        | Terminal. The failed payout already has a compensation marker; don't compensate it again.                                                                                                                                                                                                                                 |
| `PAYOUT_NOT_COMPENSABLE`              | 409    | Same endpoint — payout is not `kind='emission'`, lacks the legacy at-send debit (post-ADR-036 emission), or is not currently a failed row                                    | Terminal. Compensation is strictly for failed LEGACY pre-ADR-036 withdrawal payouts (the only ones that debited at send).                                                                                                                                                                                                 |
| `ORDER_USER_MISMATCH`                 | 409    | `POST /api/admin/users/:userId/refunds` (CF-06)                                                                                                                              | Terminal. The bound order belongs to a different user — IDOR / fabrication guard. Refund the order against its real owner.                                                                                                                                                                                                |
| `REFUND_CURRENCY_MISMATCH`            | 409    | `POST /api/admin/users/:userId/refunds` (CF-06)                                                                                                                              | Terminal. The refund currency differs from the order's charge currency. Refund in the currency the user was charged.                                                                                                                                                                                                      |
| `REFUND_EXCEEDS_CHARGE`               | 409    | `POST /api/admin/users/:userId/refunds` (CF-06)                                                                                                                              | Terminal. The refund amount exceeds what the order charged. Cap the refund at the order's charge amount.                                                                                                                                                                                                                  |
| `EMISSION_ALREADY_ISSUED`             | 409    | `POST /api/admin/users/:userId/emissions` (ADR-024/036)                                                                                                                      | Terminal for the attempted duplicate. Reuse or resolve the existing active emission instead of issuing another.                                                                                                                                                                                                           |
| `EMISSION_EXCEEDS_UNEMITTED_BALANCE`  | 409    | `POST /api/admin/users/:userId/emissions` (hardening A1)                                                                                                                     | Terminal for this amount: prior payouts/emissions already materialised the liability on-chain. The message names the remaining headroom — emit at most that, or explain why the liability grew.                                                                                                                           |
| `PENDING_PAYOUTS`                     | 409    | `POST /api/users/me/dsr/delete` while cashback payout rows are still pending/submitted                                                                                       | Terminal until the payout settles or support intervenes.                                                                                                                                                                                                                                                                  |
| `IN_FLIGHT_ORDERS`                    | 409    | `POST /api/users/me/dsr/delete` while an order is still pending/paid/procuring                                                                                               | Terminal until fulfilment or expiry completes.                                                                                                                                                                                                                                                                            |
| `FAILED_UNCOMPENSATED_WITHDRAWALS`    | 409    | `POST /api/users/me/dsr/delete` while a failed LEGACY pre-ADR-036 withdrawal payout (at-send-debited) awaits admin compensation (A4-078); post-ADR-036 emissions never block | Terminal until support compensates or writes off the failed withdrawal — surface the "contact support" path; waiting alone will not clear it.                                                                                                                                                                             |
| `BALANCE_NOT_ZERO`                    | 409    | `POST /api/users/me/dsr/delete` while any `user_credits` row has a non-zero balance (PLAT-30-03)                                                                             | Terminal until the balance is spent or support withdraws it — a never-linked-wallet user can hold cashback with none of the other three DSR preconditions ever tripping.                                                                                                                                                  |
| `HOME_CURRENCY_UNCHANGED`             | 409    | `POST /api/admin/users/:userId/home-currency` (ADR 015 support-mediated change) — the target currency equals the current one                                                 | Terminal. No write needed; the user is already on that home currency.                                                                                                                                                                                                                                                     |
| `HOME_CURRENCY_HAS_LIVE_BALANCE`      | 409    | `POST /api/admin/users/:userId/home-currency` — the user has a non-zero credit balance in the current currency                                                               | Terminal. Zero the balance via a credit-adjustment before changing home currency; message carries the residual minor amount.                                                                                                                                                                                              |
| `HOME_CURRENCY_HAS_IN_FLIGHT_PAYOUTS` | 409    | `POST /api/admin/users/:userId/home-currency` — the user has pending/submitted payouts                                                                                       | Terminal. Wait for the in-flight payout(s) to clear (or fail + compensate) before changing home currency.                                                                                                                                                                                                                 |
| `CONCURRENT_CHANGE`                   | 409    | `POST /api/admin/users/:userId/home-currency` — a concurrent write changed the home currency mid-transaction                                                                 | Retryable after re-reading current state. Optimistic-concurrency guard; re-fetch the user and reissue if the change is still wanted.                                                                                                                                                                                      |
| `FAVORITES_LIMIT_EXCEEDED`            | 409    | `POST /api/users/me/favorites` — the user is already at the per-user favourites cap                                                                                          | Terminal. Remove a favourite before adding another; message carries the cap.                                                                                                                                                                                                                                              |
| `WEBHOOK_NOT_CONFIGURED`              | 409    | `POST /api/admin/discord/test` when the target channel's webhook env var is unset                                                                                            | Terminal for the attempt. Surface "webhook not configured" rather than a silent success; ops sets `DISCORD_WEBHOOK_*` then retries.                                                                                                                                                                                       |
| `STAFF_SELF_REVOKE`                   | 409    | `PUT`/`DELETE /api/admin/staff/:userId/role` when the actor targets their own admin role (ADR 037)                                                                           | Terminal. Another admin has to demote/revoke you — that's the audit trail working as designed.                                                                                                                                                                                                                            |
| `STAFF_LAST_ADMIN`                    | 409    | Same endpoints when the write would leave zero effective admins (ADR 037 last-admin protection)                                                                              | Terminal. Grant another admin first, then retry the demotion/revocation.                                                                                                                                                                                                                                                  |
| `SKIP_NOT_ABANDONED`                  | 409    | `POST /api/admin/watcher-skips/:paymentId/reopen` when the row is `pending`/`resolved` (ADR 037)                                                                             | Terminal for this row state. Re-read the row — only abandoned rows can be reopened.                                                                                                                                                                                                                                       |
| `WALLET_ALREADY_ACTIVATED`            | 409    | `POST /api/admin/users/:userId/wallet/reprovision` when provisioning already reached `activated` (ADR 037)                                                                   | Terminal. Nothing to re-drive; check the wallet card for the live state.                                                                                                                                                                                                                                                  |
| `REDEMPTION_NOT_REFETCHABLE`          | 409    | `POST /api/admin/orders/:orderId/refetch-redemption` — order not fulfilled, no `ctx_order_id`, or payload already present (ADR 037)                                          | Terminal for the current order state. The message says which precondition failed.                                                                                                                                                                                                                                         |
| `ORDER_REDRIVE_IN_PROGRESS`           | 409    | `POST /api/admin/orders/:orderId/redrive` (A5-1) — the order is `procuring` (paid-only scope)                                                                                | Terminal for a redrive. Force-re-procuring an in-flight order is a double-pay / stranding risk (money-review 2026-07-09); stuck procuring orders are auto-recovered by the recovery sweep (fail + refund when CTX is unpaid, hold + page when it is). The re-drive lever is for `paid` orders the worker never picked up. |

## Rate-limit family (429)

| Code                              | Status | Where                                                                                                                                                                                                                                                                                                                                                                                                      | Client guidance                                                                                                                                                                                                                                         |
| --------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RATE_LIMITED`                    | 429    | Every rate-limited route (see AGENTS.md middleware stack entry #6)                                                                                                                                                                                                                                                                                                                                         | Retryable. Respect `Retry-After`. The limiter uses a fixed-window counter per IP — a burst that hits the limit waits the remainder of the window.                                                                                                       |
| `DAILY_LIMIT_EXCEEDED`            | 429    | `POST /api/admin/users/:userId/credit-adjustments` (per-admin cap on signed adjustments), `POST /api/admin/payouts/:id/compensate` (fleet-wide A4-020 compensation cap), `POST /api/admin/users/:userId/refunds` (fleet-wide CF-06 refund cap), and `POST /api/admin/users/:userId/emissions` (hardening A1 — fleet-wide per-currency cap; failed emission rows created today still count against the day) | Terminal for the current UTC day unless an operator raises the cap; retrying the same write will not help.                                                                                                                                              |
| `TOO_MANY_ATTEMPTS`               | 429    | `POST /api/auth/verify-otp` (native path) — hardening B5. The per-EMAIL failed-attempt counter crossed its threshold (10 wrong guesses per 15-min window), so verify is locked for that email regardless of how many fresh codes are requested. Decoupled from the per-IP rate limit and from OTP rows.                                                                                                    | Retryable after the lockout. Respect `Retry-After` (15 min). A successful verify (or the window lapsing) clears the counter.                                                                                                                            |
| `OTP_LOCKOUT_CLEAR_RATE_EXCEEDED` | 429    | `POST /api/admin/users/:userId/clear-otp-lockout` (A5-3). The PER-TARGET velocity cap: this account's B5 lockout has already been cleared `CLEAR_LOCKOUT_MAX_PER_TARGET_PER_DAY` (5) times in the trailing 24h. This is the control that bounds a compromised admin bearer's clear→guess→clear loop against one account (the per-IP route limit can't).                                                    | Terminal for ~24h for this target. If a legitimate user is still locked out after 5 clears in a day, escalate rather than retry — repeated clears won't apply.                                                                                          |
| `ORDER_VELOCITY_EXCEEDED`         | 429    | `POST /api/orders/loop` (ADR 045 / B-3). The authenticated user already has `LOOP_ORDER_VELOCITY_MAX_PER_WINDOW` orders, or `LOOP_ORDER_VELOCITY_MAX_VALUE_MINOR` of summed charge value in one currency, within the rolling `LOOP_ORDER_VELOCITY_WINDOW_HOURS` window. Per-USER (not per-IP) — the separate `RATE_LIMITED` 429 on this route is per-IP and doesn't bound one account's blast radius.      | Retryable once the rolling window ages the oldest counted order out (no fixed `Retry-After` — it's a sliding window, not a fixed bucket). If a legitimate user hits this routinely, it's a support/limits-review conversation, not a client retry loop. |

## Image-proxy family (413 / 502)

| Code                | Status | Where                                                                  | Client guidance                                                                          |
| ------------------- | ------ | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `IMAGE_TOO_LARGE`   | 413    | `GET /api/image` — upstream response > 10 MB                           | Terminal. The source is oversized; the client should not retry with the same URL.        |
| `NOT_AN_IMAGE`      | 502    | `GET /api/image` — upstream returned a non-`image/*` Content-Type      | Terminal. The URL didn't point at an image (or the upstream's `Content-Type` was wrong). |
| `UPSTREAM_REDIRECT` | 502    | `GET /api/image` — upstream returned a 3xx (not followed; SSRF-safety) | Terminal. The original URL is stale.                                                     |

## Upstream family (502 / 503)

| Code                                       | Status | Where                                                                                                                                                                                                                                                                                                                                                                           | Client guidance                                                                                                                                                                                                                                              |
| ------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UPSTREAM_ERROR`                           | 502    | Every CTX-proxying handler when the upstream returned a non-auth non-5xx but the response didn't parse                                                                                                                                                                                                                                                                          | Retryable with backoff (human-scale — upstream shape drift is an ops-level issue, not a flaky-network issue).                                                                                                                                                |
| `UPSTREAM_UNAVAILABLE`                     | 503    | Upstream circuit-breaker is open                                                                                                                                                                                                                                                                                                                                                | Retryable. Cooldown window typically < 1 min.                                                                                                                                                                                                                |
| `SERVICE_UNAVAILABLE`                      | 503    | `verify-otp`/`refresh` on proxy-path circuit-open; social-login JWKS/anti-replay infra degradation                                                                                                                                                                                                                                                                              | Retryable with backoff (minutes, not seconds). The breaker cooldown window is 30s; social-login 503s are provider/infra outages.                                                                                                                             |
| `SUBSYSTEM_DISABLED`                       | 503    | Runtime kill switch on `auth`, `orders-legacy`, `orders-loop`, or `emissions` surfaces                                                                                                                                                                                                                                                                                          | Retryable only after operators reopen the subsystem. Honour `Retry-After` when present.                                                                                                                                                                      |
| `NOT_CONFIGURED`                           | 503    | Required server config missing, including admin emissions without a configured LOOP issuer                                                                                                                                                                                                                                                                                      | Retryable after ops fixes configuration. Not a user-input problem.                                                                                                                                                                                           |
| `STEP_UP_UNAVAILABLE`                      | 503    | `requireAdminStepUp` gate / `POST /api/admin/step-up` when `LOOP_ADMIN_STEP_UP_SIGNING_KEY` is unset (ADR 028)                                                                                                                                                                                                                                                                  | Terminal until ops generates the step-up signing key and redeploys. Destructive admin writes fail closed.                                                                                                                                                    |
| `OTP_LOCKOUT_CLEAR_RATE_CHECK_UNAVAILABLE` | 503    | `POST /api/admin/users/:userId/clear-otp-lockout` (A5-3) when the per-target velocity-cap COUNT query itself fails. Fails CLOSED — no clear is performed — so a transient DB fault can't hand an attacker a free, uncounted clear.                                                                                                                                              | Retryable. Retry shortly; if it persists the ledger/DB is degraded and the clear genuinely could not be safely evaluated.                                                                                                                                    |
| `CURRENCY_NOT_AVAILABLE`                   | 503    | `POST /api/orders/loop` with an ADR-035 extended-market currency (AED/INR/SAR/AUD/MXN) the fiat FX feed (`payments/price-feed-fx.ts`, Frankfurter/ECB) doesn't serve a live USD rate for yet (CF-19). Confirmed 2026-07-10: AUD/INR/MXN are served (live, not gated); AED/SAR are not (both USD-pegged Gulf currencies absent from the ECB reference-rate table) — see ADR 035. | Not a user-input error and not a transient outage — the market is SEO-promoted but ordering isn't live yet. Surface "ordering for this market is coming soon"; do NOT spin a retry loop. Resolves automatically (no deploy) when the feed adds the currency. |
| `ORDER_VELOCITY_CHECK_UNAVAILABLE`         | 503    | `POST /api/orders/loop` (ADR 045 / B-3) when the velocity check's own bounded count/sum query fails. Fails CLOSED — no order is created — so a transient DB fault can't hand an attacker a free, uncounted order.                                                                                                                                                               | Retryable. Retry shortly; if it persists the DB is degraded and the order genuinely could not be safely evaluated.                                                                                                                                           |
| `INTERNAL_ERROR`                           | 500    | The catch-all. Indicates a bug — ops should be paged.                                                                                                                                                                                                                                                                                                                           | Retryable once; if it repeats, surface "we hit an issue, please try again in a bit" rather than a technical message.                                                                                                                                         |
| `IDEMPOTENCY_SNAPSHOT_CORRUPT`             | 500    | Any ADR-017 admin write routed through `withIdempotencyGuard` when the stored replay snapshot for the supplied `Idempotency-Key` is unreadable (unparseable / empty body)                                                                                                                                                                                                       | Terminal — page ops. The original write committed but its response cannot be replayed. Do NOT retry with a fresh key (that would re-execute the financial write); an operator must inspect `admin_idempotency_keys` and the ledger first.                    |

## Client-only codes (never sent by the backend)

Two entries on the shared enum exist for the client's own fetch
layer so its error path carries the same switch-ladder shape:

- `NETWORK_ERROR` — transport-level failure (DNS, connection refused).
- `TIMEOUT` — client-side `AbortSignal.timeout` tripped.

## Adding a new code

1. Add the literal to `ApiErrorCode` in `packages/shared/src/api.ts` — grouped under the appropriate category comment block, with a trailing inline comment if the handler-side rationale isn't obvious.
2. Emit it from the handler as `{ code: 'XYZ', message: '...' }` paired with an HTTP status consistent with its row in this table (or add a new row if it's a new family).
3. Register the response in `apps/backend/src/openapi.ts` against the endpoint — status code, ErrorResponse schema, description that names the code literal. The A2-1507 drift check (`scripts/lint-docs.sh` §9) catches handlers that ship without openapi registration.
4. Update this doc. A missing row here is a tracker-sweep item waiting to happen.
