# Loop Backend — Environment Variables
# Copy to .env and fill in real values. Never commit .env.

# Server
PORT=8080
NODE_ENV=development
LOG_LEVEL=info

# Upstream Gift Card API
GIFT_CARD_API_BASE_URL=https://spend.ctx.com

# Optional: API credentials for endpoints that require auth (e.g. /locations)
# GIFT_CARD_API_KEY=your_api_key_here
# GIFT_CARD_API_SECRET=your_api_secret_here

# CTX client IDs for auth — one per platform (defaults shown)
# CTX_CLIENT_ID_WEB=loopweb
# CTX_CLIENT_ID_IOS=loopios
# CTX_CLIENT_ID_ANDROID=loopandroid

# Dev mode: include disabled merchants so UI can be tested
# INCLUDE_DISABLED_MERCHANTS=true

# A2-1922: comma-separated CTX merchant IDs filtered out of the
# catalog at sync time. Loop's deny-list for merchants we refuse to
# resell — slurs in brand name, upstream entries hidden during a
# dispute, commercial relationships not yet agreed. Filter applies
# before in-memory store so denied IDs never reach the public API or
# admin catalog. Empty / absent → no filter.
# LOOP_MERCHANT_DENYLIST=merchant-id-1,merchant-id-2

# Cache refresh intervals (optional — defaults shown)
REFRESH_INTERVAL_HOURS=6
LOCATION_REFRESH_INTERVAL_HOURS=24

# Image proxy security — REQUIRED in production (audit A-025).
# Comma-separated upstream image hostnames. Only URLs hosted on one of
# these hosts will be fetched by the image proxy; anything else gets
# 403'd before any network call (SSRF + DNS-rebinding mitigation).
# Boot fails in production if this isn't set. Dev/test: optional.
# IMAGE_PROXY_ALLOWED_HOSTS=spend.ctx.com,ctx-spend.s3.us-west-2.amazonaws.com
#
# Path to an operator-provided MaxMind GeoLite2-Country .mmdb (ADR 033). Powers the
# GET /api/public/geo first-guess for the region selector. Unset → returns the US
# default and the web client falls back to navigator.language.
# MAXMIND_GEOLITE2_PATH=/etc/loop/GeoLite2-Country.mmdb
#
# Mobile deep-linking domain verification (M-3). Each gates its
# GET /.well-known/* file 404 WELL_KNOWN_NOT_CONFIGURED until set —
# fill in only once the corresponding credential exists: APPLE_TEAM_ID
# after Apple Developer Program enrollment (go-live-plan L1-4),
# ANDROID_CERT_SHA256 after the release keystore is created
# (go-live-plan L1-5; comma-separated to list a debug + release
# fingerprint side by side during rollout).
# APPLE_TEAM_ID=ABCDE12345
# ANDROID_CERT_SHA256=AA:BB:CC:...,DD:EE:FF:...
#
# Emergency override (production only, do not use casually):
# DISABLE_IMAGE_PROXY_ALLOWLIST_ENFORCEMENT=1

# Rate limiter trust boundary (audit A-023). Set to `true` only when
# running behind a trusted edge proxy (Fly.io, nginx, Cloudflare, etc.)
# that rewrites X-Forwarded-For. Otherwise leave unset — the rate
# limiter falls back to the TCP socket IP, which a client cannot spoof.
# Default: false.
# TRUST_PROXY=true

# Rate-limit escape hatch for e2e test harnesses (#658). Set to `1`
# ONLY in test configs (playwright.mocked.config.ts, etc.). When
# set, every per-IP limit on every route is bypassed — production
# must never enable this.
# DISABLE_RATE_LIMITING=1

# TEST-ONLY (AUDIT-2-E). Second, independent control required — in
# addition to NODE_ENV=test — before test-endpoints.ts mounts the
# `/__test__/*` surface (notably `/__test__/mint-loop-token`, which
# mints a full session token pair with zero credential check). Every
# request under `/__test__/*` must send this exact value via the
# `X-Test-Endpoints-Secret` header. Unset → the router never mounts,
# even under NODE_ENV=test. Min 16 chars. Set ONLY in test configs
# (playwright.mocked.config.ts, playwright.flywheel.config.ts) —
# env.ts refuses to boot in production if this is set at all.
# LOOP_TEST_ENDPOINTS_SECRET=<at-least-16-char-test-only-secret>

# CF2-10 (2026-06-30 cold audit) → S4-4 (2026-07-09 dynamic fix):
# rateLimitMap is in-memory and per-machine, so every configured
# per-route budget is actually max × (live Fly machine count).
# middleware/fleet-size.ts now derives that count LIVE from Fly's
# `.internal` private DNS zone (one AAAA record per started machine,
# refreshed every 30s) and uses it as the divisor whenever it's fresh.
# This var is now only the fallback: used when FLY_APP_NAME is unset
# (local dev/CI/non-Fly hosts) or the DNS refresh has failed past a
# 5-minute grace period. Default 1 (no division) — same posture as
# TRUST_PROXY. Production still sets this explicitly via `flyctl
# secrets set` as the floor for whenever DNS is unavailable.
# RATE_LIMIT_MACHINE_COUNT_ESTIMATE=2

# S4-4: Fly injects FLY_APP_NAME into every Machine's runtime
# automatically — nothing to set here, never configured by hand. Named
# the app's private `.internal` DNS zone that fleet-size.ts queries to
# count live machines fleet-wide. Declared in env.ts (like PORT/
# NODE_ENV) purely so it flows through the validated `env` object;
# absent outside Fly, which is the expected local dev/CI posture.
# FLY_APP_NAME=loopfinance-api

# A2-1610: per-admin per-currency daily cap on credit adjustments
# (minor units). Guards against a stolen admin session draining the
# treasury via many sub-per-request-cap writes inside the token TTL.
# Default 100_000_000 (£1M / $1M / €1M) — a volume in-flight ops
# would never hit in a single UTC day. Set to `0` to disable (dev
# / test). The per-request ±10M cap is separate and always on.
# ADMIN_DAILY_ADJUSTMENT_CAP_MINOR=100000000

# ADM-01 (2026-06-30 cold audit): same shape as above, but for admin
# withdrawals — previously had NO daily aggregate cap at all despite
# being the one admin write that moves real on-chain value.
# ADMIN_DAILY_WITHDRAWAL_CAP_MINOR=100000000

# ADR 045 (B-3): Phase-1 fraud/abuse velocity limits on order creation
# (POST /api/orders/loop), per-USER (not per-IP — see ADR 045 for why
# the per-IP rate limiter doesn't bound this threat). Two independent
# dimensions; `0` disables a dimension. Defaults sized against the
# existing $50k single-order face-value ceiling, not normal usage.
# LOOP_ORDER_VELOCITY_MAX_PER_WINDOW=20
# LOOP_ORDER_VELOCITY_WINDOW_HOURS=24
# LOOP_ORDER_VELOCITY_MAX_VALUE_MINOR=500000

# A2-1606 / A2-1607: shared-secret bearer tokens for /metrics and
# /openapi.json. When set, requests must carry
# `Authorization: Bearer <token>`. Unset in development / test leaves
# those routes open (local tooling + vitest); unset in production
# makes them 404 so an anonymous probe can't fingerprint us. Minimum
# 16 chars — typical Prometheus scraper token is 32+.
# METRICS_BEARER_TOKEN=<32+ random chars>
# OPENAPI_BEARER_TOKEN=<32+ random chars>

# Discord webhooks (optional — for notifications)
# DISCORD_WEBHOOK_ORDERS=https://discord.com/api/webhooks/...
# DISCORD_WEBHOOK_MONITORING=https://discord.com/api/webhooks/...
# ADR 017 / 018 admin-write audit fanout. Fire-and-forget AFTER DB
# commit of every admin mutation. Leave unset in dev; set in prod.
# DISCORD_WEBHOOK_ADMIN_AUDIT=https://discord.com/api/webhooks/...

# Error tracking (optional — get DSN from sentry.io)
# SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz
# A2-1310: logical-env tag for Sentry (paired with VITE_LOOP_ENV on
# web). Set to `staging` on staging deploys that run NODE_ENV=production
# so Sentry bucketing matches between backend and web. Unset → falls
# back to NODE_ENV.
# LOOP_ENV=staging
# A2-1309: release tag for Sentry (paired with VITE_SENTRY_RELEASE on
# web). CI/CD sets this to the git SHA (or version+SHA) so Sentry
# can pivot from an event to the exact deploy artifact. Absent →
# Sentry omits `release` on every event (pre-launch default).
# SENTRY_RELEASE=<git-sha or v1.2.3+<sha>>

# Database (ADR 012). Required. `docker compose up -d db` brings up a
# local Postgres on :5433 matching the default below.
DATABASE_URL=postgres://loop:loop@localhost:5433/loop
# Pool size per Node process. Tune if a machine hosts more workers.
# Before raising: docs/deployment.md → "Database pool sizing &
# PgBouncer" (S4-5) — sizing formula (pool × machines ≤ Postgres
# max_connections) + the ⚠️ PgBouncer risk (transaction-mode pooling
# silently disables withAdvisoryLock's session locks).
# DATABASE_POOL_MAX=10

# A2-724: per-session statement_timeout (milliseconds). Sent as a
# startup parameter on every connection so a runaway query can't
# monopolise a pool slot. 30s baseline is well above every
# documented admin endpoint with current indexes (A2-708/709/716);
# 0 disables. Heavy backfill migrations would need a per-migration
# override (`SET LOCAL statement_timeout = 0` at the top of the SQL).
# DATABASE_STATEMENT_TIMEOUT_MS=30000

# Admin panel (ADR 011). Comma-separated list of CTX user IDs (the
# `sub` claim of the user's bearer token) granted admin privileges.
# Match on CTX sub (not email) so the upsert path stays synchronous
# with the JWT — no round-trip to CTX /me on every admin request.
# Leave empty to disable admin endpoints entirely.
# ADMIN_CTX_USER_IDS=abc-123-xyz,def-456-uvw

# CF-30 / ADR 013. Comma-separated list of verified emails granted admin
# on the LOOP-NATIVE auth path. ADMIN_CTX_USER_IDS above is keyed on
# ctx_user_id, which Loop-native (UUID-anchored) users never carry — so
# without this every native session is is_admin=false and /api/admin/*
# is unreachable once LOOP_AUTH_NATIVE_ENABLED=true. Match is
# case-insensitive; admin is granted only on a verified email (OTP /
# provider email_verified). Leave empty to disable native admin.
# ADMIN_EMAILS=ops@loopfinance.io,admin@loopfinance.io

# Defaults for the cashback split applied to newly-synced merchants
# that don't yet have an admin-set merchant_cashback_configs row
# (ADR 011). Expressed as percent-of-face-value (e.g. "8.00" = 8%).
# The sum must be ≤ 100 — wholesale is residual. Default 0/0 means a
# new merchant earns zero cashback until ops configures them.
# DEFAULT_USER_CASHBACK_PCT_OF_CTX=0.00
# DEFAULT_LOOP_MARGIN_PCT_OF_CTX=0.00

# Loop-signed JWT secret (ADR 013). Used to sign + verify
# Loop-issued access and refresh tokens. Absent → Loop-native auth
# is disabled and the legacy CTX-proxy auth path is the only route.
# Minimum 32 chars of entropy; rotate by moving the old value to
# LOOP_JWT_SIGNING_KEY_PREVIOUS for the access-token TTL, then drop
# it. Generate a fresh secret with `openssl rand -base64 48`.
# LOOP_JWT_SIGNING_KEY=<at-least-32-char-random-secret>
# LOOP_JWT_SIGNING_KEY_PREVIOUS=<prior-secret-during-rotation-only>

# RS256 JWT signing keys (ADR 030 Phase A). PEM-encoded PKCS8 RSA
# private key. When set, newly-minted Loop JWTs sign RS256 with a
# `kid` header (RFC 7638 thumbprint) and the public keys publish at
# GET /.well-known/jwks.json so an external wallet provider (Privy
# custom auth — or any JWKS consumer) can verify Loop tokens without
# a shared secret. Unset → HS256 signing above continues unchanged.
# Malformed / non-RSA PEM fails boot; escaped "\n" sequences are
# normalised to real newlines at parse time.
# Generate: openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048
# Rotation + the one-time HS256→RS256 cutover procedure:
# docs/runbooks/jwt-key-rotation.md.
# LOOP_JWT_RSA_PRIVATE_KEY=<PKCS8 PEM>
# LOOP_JWT_RSA_PRIVATE_KEY_PREVIOUS=<prior PKCS8 PEM during rotation only>

# Admin step-up auth signing key (ADR 028, A4-063). Separate from
# LOOP_JWT_SIGNING_KEY by design: a JWT-key compromise must NOT
# widen to step-up. When unset the destructive admin endpoints
# (/api/admin/users/:userId/credit-adjustments,
# /api/admin/users/:userId/withdrawals,
# /api/admin/payouts/:id/retry) fail closed with HTTP 503
# STEP_UP_UNAVAILABLE — generate the key in production before
# wiring the admin UI's step-up modal. Same rotation pattern as
# LOOP_JWT_SIGNING_KEY: move the old value to PREVIOUS for the
# 5-minute step-up TTL window, then drop.
# Hardening B3 (2026-07 plan): production boot FAILS without the key —
# a keyless production admin surface looks healthy while every
# destructive write 503s. DISABLE_ADMIN_STEP_UP_ENFORCEMENT=1 is the
# explicit opt-out for a staging deploy that deliberately ships the
# surface disabled; only the literal "1" is accepted.
# LOOP_ADMIN_STEP_UP_SIGNING_KEY=<at-least-32-char-random-secret>
# LOOP_ADMIN_STEP_UP_SIGNING_KEY_PREVIOUS=<prior-secret-during-rotation-only>
# DISABLE_ADMIN_STEP_UP_ENFORCEMENT=1

# Gift-card redeem-secret envelope key (CF-25 / X-PRIV-03 / NS-10). When
# set, orders.redeem_code + redeem_pin are AES-256-GCM-encrypted at rest
# so a logical DB read (leaked DATABASE_URL, rogue loop_readonly SELECT,
# backup exfiltration) sees ciphertext, not spendable bearer codes.
# redeem_url stays plaintext (it's the redemption landing page, not the
# secret). 32 bytes as base64 or hex; validated at boot. NS-10: REQUIRED
# in production — boot fails closed if it is unset in prod. Absent in
# dev/test → encryption disabled (legacy plaintext storage) + a single
# boot warn. Backward-safe: old plaintext rows still decrypt, so setting
# the key activates encryption for new writes; run
# `scripts/backfill-redeem-encryption.ts` once (a deploy step) to encrypt
# any pre-existing plaintext rows. Keep separate from LOOP_JWT_SIGNING_KEY.
# Generate with `openssl rand -base64 32`.
# LOOP_REDEEM_ENCRYPTION_KEY=<32-byte-base64-or-hex-secret>
# NS-10: emergency opt-out for the production redeem-encryption boot
# guard. Only the literal "1" is accepted. Ships production storing
# redeem codes/PINs in PLAINTEXT at rest — rollback / staging only,
# never with live redemption data.
# DISABLE_REDEEM_ENCRYPTION_ENFORCEMENT=1

# Loop-native auth feature flag (ADR 013). When true, /request-otp
# (and, as they ship, /verify-otp + /refresh) take the Loop-native
# path: Loop sends the OTP email and mints its own JWTs. R3-7:
# production boot fails unless this is true, so an unset flag cannot
# silently fall back to the legacy CTX-proxy path. Emergency rollback /
# staging only: DISABLE_NATIVE_AUTH_ENFORCEMENT=1.
# LOOP_AUTH_NATIVE_ENABLED=true
# DISABLE_NATIVE_AUTH_ENFORCEMENT=1

# Tranche 1 (MVP) launch gate. When true, the web client hides
# every Phase 2+ surface — cashback navbar links, /settings/wallet,
# /settings/cashback, /cashback, /calculator, the onboarding currency
# picker + wallet-intro screens, LinkWalletNudge, "you've earned X
# cashback" copy. Also swaps the onboarding marketing trust-screen
# copy (native + web) from cashback/bank-transfer framing to discount
# framing (U-2 / UX-01, docs/ux-pass-2026-07-09.md). Discount badges
# stay (they ARE the Tranche 1 user proposition). Pair with
# LOOP_WORKERS_ENABLED=false + unset LOOP_STELLAR_*_ISSUER +
# INTEREST_APY_BASIS_POINTS=0 (those are the defaults). Flip back to
# false when launching Tranche 2 — server-side change, no app store
# resubmission.
# LOOP_PHASE_1_ONLY=false

# ── Transactional email (ADR 013) ────────────────────────────────────
# Required when LOOP_AUTH_NATIVE_ENABLED=true in production. The
# `console` provider logs OTPs to stdout — DEV ONLY; production
# refuses to start with it set. Add a real provider before
# enabling Loop-native auth in production.
# Currently supported: `resend` (https://resend.com)
# EMAIL_PROVIDER=resend

# Resend API key. Format: re_…
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# Sender address. Domain MUST be verified (DKIM/SPF) at the
# provider's dashboard before delivery succeeds. Defaults to
# noreply@loopfinance.io when EMAIL_PROVIDER=resend is set.
# EMAIL_FROM_ADDRESS=noreply@loopfinance.io

# Display name in the From header. Defaults to "Loop".
# EMAIL_FROM_NAME=Loop

# Optional Reply-To address for OTP emails. Set to a monitored inbox
# (production uses hello@loopfinance.io via fly.toml [env]) so user
# replies to the no-reply sender land somewhere a human reads. Unset →
# the reply_to key is omitted from the provider payload entirely.
# Email-validated at boot by env.ts.
# EMAIL_REPLY_TO_ADDRESS=hello@loopfinance.io

# Social login — Google (ADR 014). One OAuth client id per platform.
# At least one must be set for /api/auth/social/google to be live;
# the id_token's `aud` must match one of these. Create in Google
# Cloud Console → APIs & Services → Credentials.
# GOOGLE_OAUTH_CLIENT_ID_WEB=<web-client>.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_ID_IOS=<ios-client>.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_ID_ANDROID=<android-client>.apps.googleusercontent.com

# Social login — Apple (ADR 014). The Sign in with Apple service id
# (web) or bundle id (native). Apple's id_token `aud` must match.
# Absent → /api/auth/social/apple returns 404.
# APPLE_SIGN_IN_SERVICE_ID=io.loopfinance.app

# CTX operator-account pool (ADR 013). JSON array of service accounts
# Loop uses to talk to CTX once the identity takeover is live. Each
# entry is `{ "id": "<human name>", "bearer": "<ctx-jwt>" }`. At least
# 2 entries are recommended in production — a single operator account
# is a single point of failure for rate-limits, lockouts, and key
# rotation (ADR 013 for the full reasoning). Absent → pool is inert
# and any call through it returns 503; safe while the principal
# switch (ADR 010) hasn't wired it in yet.
# CTX_OPERATOR_POOL=[{"id":"primary","bearer":"eyJ..."},{"id":"backup-1","bearer":"eyJ..."}]

# Loop's Stellar deposit address for Loop-native orders (ADR 010).
# Users paying with XLM / USDC send to this address, encoding the
# order's payment memo in the transaction's memo_text. The payment
# watcher polls Horizon and transitions matching orders to `paid`.
# Absent → /api/orders/loop returns 503 for xlm / usdc methods;
# credit-funded orders still work.
# LOOP_STELLAR_DEPOSIT_ADDRESS=GA7QYNF7SOWQ3GLR2BGMZEHXAVIRZA4KVWLTJJFC7MGXUA74P7UJVSGV

# Stellar Horizon URL the payment watcher polls. Defaults to public
# Mainnet; set to `https://horizon-testnet.stellar.org` for staging.
# Not validated by env.ts — the horizon client reads process.env
# directly so test suites can flip it without a module reload.
# LOOP_STELLAR_HORIZON_URL=https://horizon.stellar.org

# XLM price oracle URL. Response shape must match CoinGecko's
# /simple/price: { stellar: { usd: number, gbp?: number, eur?: number } }.
# Default is the public CoinGecko endpoint; operators override with
# a self-hosted / paid feed via this var. The watcher caches 60s.
# LOOP_XLM_PRICE_FEED_URL=https://api.coingecko.com/api/v3/simple/price?ids=stellar&vs_currencies=usd,gbp,eur

# Fiat FX feed URL for USDC-against-non-USD orders. Response shape
# must match Frankfurter: { base: "USD", rates: { GBP: number, EUR: number } }.
# Default is the public Frankfurter endpoint (ECB-sourced, no key);
# operators override with a self-hosted / paid feed. 60s cache.
# LOOP_FX_FEED_URL=https://api.frankfurter.app/latest?from=USD&to=GBP,EUR

# USDC issuer account the watcher pins as an asset-match guard.
# Circle on mainnet is GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN.
# AUDIT-2 finding A: Stellar asset codes aren't unique — anyone can
# self-issue an asset called "USDC". Absent → the watcher's
# issuer-match guard (payments/horizon.ts: isMatchingIncomingPayment)
# fails CLOSED and matches NO USDC deposit at all, never "any issuer"
# (an unpinned issuer used to accept an attacker's self-issued fake
# "USDC" and mark a real order paid). Boot warns when set on mainnet
# to anything other than the canonical Circle issuer — a typo here
# makes the watcher silently ignore every legitimate USDC deposit
# (launch-runbook tripwire). Hardening: production boot FAILS without
# this var set, mirroring LOOP_ADMIN_STEP_UP_SIGNING_KEY above; set
# DISABLE_USDC_ISSUER_ENFORCEMENT=1 to deliberately ship the USDC
# rail disabled (staging only; only the literal "1" is accepted).
# LOOP_STELLAR_USDC_ISSUER=GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN
# DISABLE_USDC_ISSUER_ENFORCEMENT=1

# CFG-01: emergency opt-out for the production DISCORD_WEBHOOK_MONITORING
# boot guard. Same "1"-only shape as its siblings so a deploy typo fails
# at parse time. Setting it ships production with the monitoring/alert tier
# deliberately un-wired — every drift / circuit / stuck-sweeper /
# ledger-invariant page is dropped silently. For a staging or throwaway
# deploy that genuinely wants no alerting, never for a real production launch.
# DISABLE_MONITORING_WEBHOOK_ENFORCEMENT=1

# Issuer accounts for the three LOOP-branded Stellar assets (ADR 015).
# Loop issues USDLOOP / GBPLOOP / EURLOOP 1:1-backed against fiat
# reserves; cashback is paid to the user in the asset matching their
# home currency. Absent → payout worker skips the Stellar-side
# transfer for that currency and accrues cashback off-chain only.
# Without these set, the watcher also refuses to allowlist the
# matching LOOP asset as an inbound user payment (prevents a spoofed
# "USDLOOP" from an unrelated issuer being accepted).
# LOOP_STELLAR_USDLOOP_ISSUER=
# LOOP_STELLAR_GBPLOOP_ISSUER=
# LOOP_STELLAR_EURLOOP_ISSUER=

# Per-asset ISSUER secret keys (ADR 031 / ADR 036 Phase D). A payment
# FROM the issuer account is a native Stellar mint — the payout worker
# signs `kind='interest_mint'` rows (nightly on-chain interest) with
# the matching issuer keypair; every other payout kind keeps signing
# with the operator key. parseEnv boot-fails when a secret is set
# without its LOOP_STELLAR_<ASSET>_ISSUER address or when the keypair
# derived from the secret doesn't match that address (a mismatch
# would "mint" from an unrelated account). Never logged (pino
# redaction). Absent → interest_mint rows queue until configured.
# LOOP_STELLAR_USDLOOP_ISSUER_SECRET=SXXXXX...
# LOOP_STELLAR_GBPLOOP_ISSUER_SECRET=SXXXXX...
# LOOP_STELLAR_EURLOOP_ISSUER_SECRET=SXXXXX...

# Procurement USDC-reserve floor (ADR 015). When our operator USDC
# balance drops below this many stroops (7 decimals; 10^7 = 1 USDC),
# procurement pays CTX in XLM instead so fulfillment doesn't stall
# while the USDC top-up is in flight. Absent → fallback disabled,
# procurement always uses USDC. Below-floor events get ops-flagged
# in admin/treasury.
# Example: 1_000_000_000 stroops = 100 USDC minimum reserve.
# LOOP_STELLAR_USDC_FLOOR_STROOPS=1000000000

# Operator Stellar secret for outbound cashback payouts (ADR 016).
# Signs LOOP-asset Payments to users' linked wallets. Never log,
# never surface via API; pino redaction paths are set at
# `secret`, `operatorSecret`, and both env names. Absent →
# payout worker is inert and pending_payouts rows queue until
# ops plumbs this.
# Rotation: move the active key to _PREVIOUS for the access-token
# TTL, deploy with both set, then drop the old. Mirrors the JWT
# signing-key rotation pattern.
# LOOP_STELLAR_OPERATOR_SECRET=SXXXXX...
# LOOP_STELLAR_OPERATOR_SECRET_PREVIOUS=SYYYYY...

# Payout channel accounts (ADR 044 / S4-1 — Stellar payout throughput).
# Comma-separated list of pre-funded Stellar secret keys used as the
# payout worker's transaction SOURCE (sequence number + fee payer),
# NOT the funding account — the standard Stellar "channel account"
# scale pattern. Payments still debit the operator (or, for
# interest_mint rows, the asset issuer); channels never hold or move
# the LOOP asset, just enough XLM to exist + pay fees. List length IS
# the channel count N — empty/unset (default) → N=0 → the worker's
# original fully-serial single-account path, unchanged. Boot fails on
# a malformed entry, a duplicated channel, or a channel that collides
# with the operator/issuer accounts. Never log; pino redaction is set
# at `channelSecret` and both env-key shapes. See
# docs/adr/044-payout-throughput.md for the full design + provisioning
# guidance.
# LOOP_STELLAR_PAYOUT_CHANNEL_SECRETS=SCHANNEL1...,SCHANNEL2...

# Network passphrase for payout signing (ADR 016). PUBLIC mainnet
# is the default; set to "Test SDF Network ; September 2015" for
# testnet. Self-hosted networks set their own passphrase.
# LOOP_STELLAR_NETWORK_PASSPHRASE=Public Global Stellar Network ; September 2015

# Payout-worker tick interval (seconds, ADR 016). 30s matches the
# pacing guidance — slower than the watcher/procurement because
# each Stellar submit + ledger close runs ~5s and sequence numbers
# serialise on the operator account.
# LOOP_PAYOUT_WORKER_INTERVAL_SECONDS=30

# Max transient-failure retries before a payout row promotes to
# terminal `failed` (ops intervention via admin retry). ADR 016
# default 5.
# LOOP_PAYOUT_MAX_ATTEMPTS=5

# A2-602 watchdog: rows stuck in `submitted` past this (seconds) are
# re-picked by the worker. Prevents rows from stalling forever if the
# pod dies between submit and confirm, or the mark-confirmed DB write
# fails. Idempotency pre-check converges already-landed payments to
# `confirmed`; otherwise a fresh submit is issued with new sequence.
# Default 300s (5m) — well above the normal submit→seal window (~5s).
# LOOP_PAYOUT_WATCHDOG_STALE_SECONDS=300

# A2-1921 fee-bump strategy. The submit worker scales the Stellar
# transaction fee per attempt so a congested network drains naturally
# instead of going terminal at base fee:
#   attempt 1 → BASE
#   attempt 2 → BASE * MULTIPLIER
#   attempt N → BASE * MULTIPLIER^(N-1)  (capped at CAP)
# Defaults: 100 → 200 → 400 → 800 → 1600 stroops at MULTIPLIER=2,
# CAP=100_000 (any fee under the cap is well below $0.001 even on
# mainnet — cheap insurance against a stuck row).
# LOOP_PAYOUT_FEE_BASE_STROOPS=100
# LOOP_PAYOUT_FEE_CAP_STROOPS=100000
# LOOP_PAYOUT_FEE_MULTIPLIER=2

# Loop-native order workers (ADR 010). When true, the backend starts
# the payment watcher (polls Horizon for deposits → transitions orders
# to `paid`) and procurement worker (calls CTX via operator pool →
# transitions `paid` → `fulfilled`). Default false.
# LOOP_WORKERS_ENABLED=false

# ADR 031 §Detailed design D9: vault-subsystem master switch for the
# LOOPUSD/LOOPEUR DeFindex-vault path. V1 shipped the schema + read
# layer; V2 adds the Soroban deposit/withdraw/transfer client
# (credits/vaults/vault-client.ts) but does NOT wire it into any
# emission/withdraw flow yet (§D5/§D6 are later PRs) — every function
# there still checks this flag before doing anything. Distinct from
# LOOP_PHASE_1_ONLY, which gates the user-facing cashback/wallet
# surface generally. Default false — an empty `loop_vaults` registry
# table + this flag off is byte-identical to pre-migration.
# LOOP_VAULTS_ENABLED=false

# ADR 031 §Detailed design D2/D9, V2 (Soroban vault client). Soroban
# RPC endpoint the vault client uses for account loads,
# simulateTransaction/prepareTransaction/sendTransaction/getTransaction
# — distinct from LOOP_STELLAR_HORIZON_URL (Horizon is the classic-
# ledger REST API; Soroban RPC is a separate JSON-RPC endpoint, even
# on the same network). REQUIRED when LOOP_VAULTS_ENABLED=true — boot
# fails otherwise (ADR 031). Nullable/unset otherwise.
# LOOP_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Hardening A6: auto-refund late deposits. A deposit that lands just
# after its order expires is recorded + abandoned; an operator can
# always refund it to the sender via
# POST /api/admin/deposits/:paymentId/refund (admin + step-up). When
# this is true (and the operator Stellar signer is configured), the
# skip-sweep ALSO refunds such late deposits automatically — same
# refundDeposit() path + idempotency, no button press. Default false
# (admin-triggered only). Read live so a flip takes effect next sweep.
# LOOP_DEPOSIT_REFUND_AUTO=false

# A2-1907: runtime kill switches. Set any to `true` and the matching
# surface returns 503 SUBSYSTEM_DISABLED on the next request — no
# redeploy. Operator runbook at docs/runbooks/kill-switch.md.
# - ORDERS gates POST /api/orders + /api/orders/loop combined
# - ORDERS_LEGACY / ORDERS_LOOP gate one path each; when set they
#   override LOOP_KILL_ORDERS for that path, when unset they fall back
#   to it (fully backward compatible)
# - AUTH gates request-otp + verify-otp + social/google + social/apple
#   (refresh + logout intentionally remain open so existing sessions can
#   drain on next refresh)
# - EMISSIONS gates the admin emission (ADR 036, ex-withdrawal) +
#   compensation endpoints
# LOOP_KILL_ORDERS=false
# LOOP_KILL_ORDERS_LEGACY=false
# LOOP_KILL_ORDERS_LOOP=false
# LOOP_KILL_AUTH=false
# LOOP_KILL_EMISSIONS=false

# Tick intervals for the workers, in seconds. Defaults: 10s watcher /
# 5s procurement.
# LOOP_PAYMENT_WATCHER_INTERVAL_SECONDS=10
# LOOP_PROCUREMENT_INTERVAL_SECONDS=5

# R3-5 CTX settlement sanity band. Before paying CTX, procurement
# refuses a SEP-7 payment amount above this many basis points of the
# expected wholesale XLM quote. Default 12500 = 125%.
# LOOP_CTX_PAYMENT_MAX_BPS_OF_EXPECTED=12500

# NS-13 settlement-reconciliation cutover. The missing-settlement
# reconciliation watchdog only flags a fulfilled, on-chain-funded order
# with NO ctx_settlements row when the order was created at or after this
# instant; orders created before the settlement system went live
# (migration 0045, "hardening A4") legitimately have none. Optional — when
# unset, the watchdog uses the earliest ctx_settlements.created_at as the
# empirical cutover. Set it to the exact A4 go-live instant (ISO-8601) for
# a precise, backfill-proof baseline.
# LOOP_SETTLEMENT_RECONCILE_SINCE=2026-05-14T00:00:00Z

# Asset-drift watcher (ADR 015). Polls Horizon for each configured
# LOOP asset, compares against the off-chain ledger liability, and
# pages the monitoring Discord channel when |drift| exceeds the
# threshold. 300s tick matches accounting cadence — drift is not
# latency-sensitive. Threshold in stroops: 1e8 = 10 whole LOOP units
# (=$10 for USDLOOP) — covers normal in-flight payout drift while
# catching accounting divergence.
# LOOP_ASSET_DRIFT_WATCHER_INTERVAL_SECONDS=300
# LOOP_ASSET_DRIFT_THRESHOLD_STROOPS=100000000

# R3-1: operator XLM/USDC float reconciliation. This indexes Horizon
# movements from operator-created baselines and pages when the current
# operator balance no longer conserves or a movement is unclassified.
# XLM has a wider default threshold for Stellar fees; USDC should be
# exact unless an approved manual movement explains it.
# LOOP_OPERATOR_FLOAT_RECONCILIATION_INTERVAL_HOURS=24
# LOOP_OPERATOR_FLOAT_XLM_THRESHOLD_STROOPS=10000000
# LOOP_OPERATOR_FLOAT_USDC_THRESHOLD_STROOPS=1

# A2-905 / ADR 009: interest accrual scheduler. Off by default
# (0 bps) — ADR 009 gates this behind legal sign-off per target
# market. Set to a positive integer-basis-points value to enable
# (400 = 4.00% APY). Scheduler runs under LOOP_WORKERS_ENABLED.
# Per-year period denominator: 365 daily, 12 monthly, 52 weekly.
# Tick interval in hours — kept independent from periodsPerYear so
# restart-safety leans on the per-cursor DB unique index rather
# than the schedule cadence.
# INTEREST_APY_BASIS_POINTS=0
# INTEREST_PERIODS_PER_YEAR=365
# INTEREST_TICK_INTERVAL_HOURS=24

# ADR 031 / ADR 036 Phase D: nightly ON-CHAIN interest mints. When
# true (with LOOP_WORKERS_ENABLED, INTEREST_APY_BASIS_POINTS > 0 and
# at least one LOOP_STELLAR_*_ISSUER_SECRET configured) the
# interest-mint worker replaces the legacy off-chain accrual
# scheduler: each UTC day it snapshots activated-wallet LOOP balances
# from Horizon (audited in `interest_mint_snapshots`), credits the
# user_credits mirror and enqueues an issuer-signed on-chain mint in
# ONE transaction per user. The legacy scheduler is hard-gated off
# while this is true — two interest writers must never coexist.
# APY source is the same INTEREST_APY_BASIS_POINTS above; the
# nightly rate is APY/365 floored to 7 decimals, paid out in whole
# minor units with the sub-penny remainder carried per user.
# LOOP_INTEREST_ONCHAIN_ENABLED=false

# Interest forward-mint pool account (ADR 009 / 015).
# When the daily interest scheduler is on, on-chain LOOP-asset
# issuance must grow with the off-chain liability. The operator
# pre-mints batches into this account; daily accrual sub-allocates
# off-chain. Defaults to the operator account when unset (operator
# already holds LOOP-asset custody and submits payouts from there).
# LOOP_INTEREST_POOL_ACCOUNT=GXXXXX...

# Days-of-cover threshold for the pool depletion alert. The pool
# watcher pages the Discord monitoring channel when on-chain pool
# balance can cover fewer than this many days of forecast daily
# interest. Default 7 days.
# LOOP_INTEREST_POOL_MIN_DAYS_COVER=7

# CF-26 / X-PRIV-07/08: auth-row retention purge sweeper. Periodic
# DELETE-only sweep (runs under LOOP_WORKERS_ENABLED, same as the
# other background workers) that reaps expired/consumed OTP rows and
# dead (expired or long-revoked) refresh-token rows past the retention
# grace, so neither PII-bearing table grows without bound. Interval in
# hours (retention hygiene isn't latency-sensitive). Retention window
# in days — must sit comfortably past the refresh horizon so a live
# session is never reaped, and long enough that the token-theft reuse
# signal + the just-expired-OTP 401 edge stay intact. Runbook:
# docs/runbooks/dsr.md.
# LOOP_AUTH_ROW_PURGE_INTERVAL_HOURS=1
# LOOP_AUTH_ROW_RETENTION_DAYS=30

# NS-03: retention window (days) for the durable admin money-move AUDIT
# trail. admin_idempotency_keys is both the idempotency-replay store AND
# the sole durable record of every admin money-move (refunds, emissions,
# credit-adjustments — surfaced by admin/audit-tail.ts +
# admin/user-audit-timeline.ts). Its hourly sweep previously reaped rows
# at the 24h replay TTL, so the forensic/regulatory audit self-deleted
# after a day. Retention is now decoupled from the (unchanged) 24h
# replay-hit window. Default 2555 days (~7 years) is a conservative,
# defensible baseline for financial audit records; set to the exact
# period your compliance/regulator mandates. Prefer longer over shorter.
# LOOP_ADMIN_AUDIT_RETENTION_DAYS=2555

# Hardening C1 (2026-07 plan): cadence of the scheduled ledger-invariant
# check (user_credits mirror == SUM(credit_transactions) per user+currency).
# Pages the Discord monitoring channel every tick while drift persists.
# Full-table aggregate — daily by default; single-flighted across
# machines via an advisory lock. Runs under LOOP_WORKERS_ENABLED.
# LOOP_LEDGER_INVARIANT_INTERVAL_HOURS=24

# ADR 031 §Detailed design D4 (V5): vault drift + solvency watcher
# (credits/vaults/vault-drift-watcher.ts) — the Soroban LOOPUSD/LOOPEUR
# twin of the classic asset-drift watcher above. 300s (5m) default,
# same cadence reasoning. Runs under LOOP_WORKERS_ENABLED AND
# LOOP_VAULTS_ENABLED.
# LOOP_VAULT_DRIFT_WATCHER_INTERVAL_SECONDS=300
# INV-V1 threshold, in the vault share token's 7-decimal smallest
# unit. 1e8 = 10 whole shares.
# LOOP_VAULT_DRIFT_SHARES_THRESHOLD_STROOPS=100000000
# INV-V2 threshold, in the vault's underlying-asset 7-decimal smallest
# unit. 1e8 = $10 of tolerance on user-share value vs vault-redeemable
# backing + hot float.
# LOOP_VAULT_DRIFT_SOLVENCY_THRESHOLD_STROOPS=100000000

# ADR 031 §Detailed design D4 (V5): vault-aware hot-float
# reconciliation (treasury/hot-float-reconciliation.ts) — checks the
# operator's actual on-chain vault-share balance against the emission/
# redemption bookkeeping, catching the V4-accepted slow-withdraw-race /
# phantom-share residual. Daily default, matching R3-1's cadence. Runs
# under LOOP_WORKERS_ENABLED AND LOOP_VAULTS_ENABLED.
# LOOP_VAULT_FLOAT_RECONCILIATION_INTERVAL_HOURS=24
# Share-count tolerance for the check above, same 7-decimal unit as
# LOOP_VAULT_DRIFT_SHARES_THRESHOLD_STROOPS. Tighter than the drift
# watcher's threshold since these two figures should track exactly in
# normal operation.
# LOOP_VAULT_FLOAT_SHARES_THRESHOLD_STROOPS=1000000

# ADR 031 §Detailed design D8 (V5b): APY snapshot cron
# (credits/vaults/vault-apy-snapshot.ts) — periodically records each
# active vault's live share price into vault_share_price_snapshots so
# GET /api/me/vault-apy can compute a past-30d/90d APY from history
# instead of hitting Soroban per request. 24h default (daily
# resolution is enough for a 30/90-day annualised figure). Runs under
# LOOP_WORKERS_ENABLED AND LOOP_VAULTS_ENABLED, single-flighted
# fleet-wide like the drift watcher above.
# LOOP_VAULT_APY_SNAPSHOT_INTERVAL_HOURS=24

# ADR 030: provider-agnostic embedded-wallet layer. '' (the
# default) disables the layer entirely — getWalletProvider() returns
# null and no vendor code path is reachable. Set to `privy` to enable
# the Privy REST adapter (plain fetch + Zod against api.privy.io —
# no SDK dependency); PRIVY_APP_ID + PRIVY_APP_SECRET then become
# required and parseEnv refuses to boot without both. Phase C wires
# the layer into flows: signup-time provisioning + activation sweeper
# (LOOP_WORKERS_ENABLED), payout targeting, pay-with-balance, and
# GET /api/me/wallet.
# LOOP_WALLET_PROVIDER=

# Privy app credentials (ADR 030). Used as HTTP Basic auth
# (PRIVY_APP_ID:PRIVY_APP_SECRET) plus the privy-app-id header on
# every Privy REST call. The secret is never logged (pino redaction
# paths cover PRIVY_APP_SECRET / appSecret).
# PRIVY_APP_ID=your-privy-app-id
# PRIVY_APP_SECRET=your-privy-app-secret

# P2-14: force-update gate. Minimum supported native app version per
# platform (semver). A client older than this, hitting /api/config,
# is hard-blocked by the native ForceUpdateGate with an update prompt.
# Optional — unset means no minimum is enforced (the gate fails open).
# Bump on a breaking native release.
# MIN_SUPPORTED_APP_VERSION_IOS=1.0.0
# MIN_SUPPORTED_APP_VERSION_ANDROID=1.0.0
# NS-06: hot-float USDC-backing reconciler. Runs daily, comparing the
# recorded vault_hot_float balance against real on-chain USDC and
# paging Discord on a shortfall past the threshold.
# Reconciliation cadence in hours (default 24).
# LOOP_HOT_FLOAT_BACKING_RECONCILIATION_INTERVAL_HOURS=24
# Drift threshold in stroops before paging (default 100000000 = $10 USDC).
# LOOP_HOT_FLOAT_BACKING_THRESHOLD_STROOPS=100000000
