# Loop Web — Production Environment (committed, no secrets)
VITE_API_URL=https://api.loopfinance.io
# FE-22: Phase-1 launch gate SSR/first-paint default. Mirror the backend's
# LOOP_PHASE_1_ONLY so a crawler (SSR HTML) and a real runtime gate on the
# same value. `true` = Phase-1 (gated), matching prod today.
VITE_PHASE_1_ONLY=true
